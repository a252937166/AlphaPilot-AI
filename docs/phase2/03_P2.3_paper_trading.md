# P2.3 模拟交易执行闭环 — 实施规格

> 前置：P2.1 完成（P2.2 可并行）。**本里程碑只放开富途 SIMULATE 环境**；REAL 路径的三重门禁一个字符都不许改，且本里程碑要为它补回归测试（S6）。
> 富途硬约束：下单类接口每 30s 最多 15 次；模拟账户此前实测 `get_acc_list` 证券 2 行/期货 4 行可用。

---

## P2.3-S1 配置与账户/持仓只读同步 ✅ done 2026-07-21

**config.py 增加**：
```python
paper_trading_enabled: bool = False      # 执行器总开关（独立于 futu_enable_trade）
trading_halted: bool = False             # Kill Switch：True 时执行器拒绝一切新单
demo_equity: float = 1_000_000.0         # P2.2-S9 已用，如未加在此补
```
本机 `.env`：`ALPHAPILOT_FUTU_ENABLE_TRADE_QUERY=true`（只读查询）；`ALPHAPILOT_FUTU_ENABLE_TRADE=true` + `ALPHAPILOT_PAPER_TRADING_ENABLED=true` 在 S2 联调时再开。
**`.env.example` 同步这三个键并注释清楚含义。**

**新增** `services/broker.py`：

```python
def get_simulate_account(client: FutuClient, market: str = "CN") -> dict[str, Any]:
    """trade_call('security','get_acc_list') → 过滤 trd_env==SIMULATE 且市场匹配的 acc_id。
    找不到 → BrokerError('无可用模拟账户')。结果进程内缓存。"""

def fetch_positions(client: FutuClient) -> list[dict[str, Any]]:
    """trade_call('security','position_list_query', kwargs={'acc_id':...}, environment='SIMULATE')
    → [{symbol(去市场前缀), qty, cost_price, market_val, pnl_ratio}]"""

def fetch_account_funds(client: FutuClient) -> dict[str, Any]:
    """accinfo_query → {total_assets, cash, market_val}"""
```
注意：A股模拟账户市场过滤字段以实测 `get_acc_list` 返回列为准（`trd_env/acc_type/sim_acc_type/trdmarket_auth`），把实际选择逻辑写 DEVIATION。

> ⚠ DEVIATION: 2026-07-21 使用当前 Futu SDK `10.09.6908` 与 OpenD 实测，CN
> 证券模拟账户只返回 1 行，而非架构背景记录的 2 行；该行是
> `trd_env=SIMULATE`、`sim_acc_type=STOCK`、`acc_status=ACTIVE`，且
> `trdmarket_auth` 实际为包含 `CN` 的 list、`acc_id` 实际为 int。实现因此按
> SIMULATE + STOCK + ACTIVE + CN auth 唯一匹配，不依赖行序、不硬编码或对外暴露
> `acc_id`，匹配不到或匹配多行均以中文错误拒绝。当前真实持仓为 0 行，接口如实返回
> `positions=[]`，不以 `demo_equity` 或其他演示数据补位；真实资金仍来自 `accinfo_query`。
> Futu 的 `pl_ratio` 是百分数值（SDK 已乘 100），对外 `pnl_ratio` 统一除以 100 转为
> 小数比例，避免与项目内收益率口径混用。开启只读查询后，旧通用 HTTP 代理原本也能调用
> `get_acc_list` 并原样暴露 `acc_id`；本步将该方法收为进程内专用，HTTP 调用固定返回 403，
> 对外统一走脱敏后的 `/v1/portfolio/account`。

**API**：`GET /v1/portfolio/account` → 资金+持仓（futu 不可用/未开 trade_query → 503 中文说明）。

**验证**
```bash
curl -s localhost:8000/v1/portfolio/account | python3 -m json.tool   # cash/total_assets 数值合理
```
**完成标准**：质量门 + 账户接口出真实模拟账户数据；关闭 `futu_enable_trade_query` 时返回 403/503 而非崩溃。

**验证记录（2026-07-21）**：

- OpenD 保持运行并重启无 `--reload` 的后端后，按“验证”原命令实测
  `/v1/portfolio/account` 返回 `market=CN`、`environment=SIMULATE`、
  `total_assets=1000000.0`、`cash=1000000.0`、`market_val=0.0`、`positions=[]`；
  响应不含 `acc_id`，数据均来自真实模拟账户查询。
- 当前本机安全开关仅 `futu_enable_trade_query=true`；`futu_enable_account_mutation`、
  `futu_enable_trade`、`live_trading_enabled`、`paper_trading_enabled` 均为 false，
  `trading_halted` 初始值也为 false。用关闭 query 的临时配置复验，账户接口返回中文 503，
  未尝试连接 OpenD、未崩溃。
- 安全审计实证旧通用路由在修复前会从 `get_acc_list` 泄露 `acc_id`；修复后真实
  `POST /v1/futu/trade/security/get_acc_list` 固定返回中文 403，递归检查响应不含
  `acc_id`，同时专用账户接口仍为 200。离线回归断言 HTTP 层在调用 client 前即阻断，
  broker 的进程内账户发现不受影响。
- broker/Futu 专项 18 个测试覆盖真实字段唯一筛选、按 client/market 缓存、资金和持仓
  规范化、百分数收益率转换、空持仓、非有限数拒绝、关闭开关降级及通用路由防泄露。
  完整质量门通过：`ruff check src tests`、strict `mypy src/alphapilot`（86 个源文件）、
  `pytest -q`（108 tests）；本步 7 个 Python 文件另经 `ruff format --check`，
  `git diff --check` 通过。

---

## P2.3-S2 订单表 + 执行器（SIMULATE 下单） ✅ done 2026-07-22

**models.py 增加**：

```python
class BrokerOrder(Base):
    __tablename__ = "broker_orders"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    proposal_id: Mapped[str] = mapped_column(String(64), index=True)     # 关联 trade_proposals.proposal_id
    futu_order_id: Mapped[str | None] = mapped_column(String(64), unique=True)
    symbol: Mapped[str] = mapped_column(String(24))
    side: Mapped[str] = mapped_column(String(8))
    order_type: Mapped[str] = mapped_column(String(16), default="MARKET")  # v1 只用限价单模拟市价：last*1.01/0.99
    price: Mapped[float | None] = mapped_column(Float)
    qty: Mapped[float] = mapped_column(Float)
    status: Mapped[str] = mapped_column(String(24), default="submitting")
    # submitting/submitted/filled/partial/cancelled/failed
    filled_qty: Mapped[float] = mapped_column(Float, default=0)
    avg_fill_price: Mapped[float | None] = mapped_column(Float)
    environment: Mapped[str] = mapped_column(String(12), default="SIMULATE")  # 永远 SIMULATE，见守则
    error: Mapped[str | None] = mapped_column(Text)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow)
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow, onupdate=utcnow)
```

trade_proposals 状态机扩展（迁移无需改表，status 是 String）：
`pending → approved → executing → executed | exec_failed`；网关未开时批准仍落 `approved_no_execution`（兼容一期）。

**新增** `services/executor.py`：

```python
def execute_proposal(session: Session, client: FutuClient, record: TradeProposalRecord) -> BrokerOrder:
    """前置断言（任一不满足抛 ExecutionBlocked，中文 message）：
       settings.paper_trading_enabled and settings.futu_enable_trade
       and not settings.trading_halted and record.status == 'approved'
    1) 幂等：broker_orders 已有该 proposal_id 且状态非 failed → 直接返回既有单
    2) 重跑风控：TradeGuardrails.evaluate(原 proposal, 实时组合状态 fetch_account_funds/positions 换算)
       不过 → record.status='exec_failed'，error=风控理由
    3) 价格：富途快照 last；qty=proposal.quantity 按 100 股取整（A股一手）
    4) trade_call('security','place_order', kwargs={code,price(限价滑点带),qty,trd_side,
       order_type=NORMAL, acc_id}, environment='SIMULATE')   # 绝不传 REAL，绝不带 confirmation
    5) 成功 → BrokerOrder(status=submitted, futu_order_id=...)，record.status='executing'
    """
```

**API**（`api/routes/trades.py` 增加）：
- `POST /v1/trades/proposals/{id}/execute` → 调 executor（403 当开关未开，409 当状态不对）；
- `GET /v1/orders?limit=50`、`GET /v1/orders/{id}`。
前端 08 页"确认执行"按钮接 execute（P2.4 接线，本步 curl 验证即可）。

**验证**（开着三开关，盘中或富途模拟撮合时段）
```bash
# 用一期演示流生成并批准一个 proposal，然后：
curl -s -X POST localhost:8000/v1/trades/proposals/1/execute | python3 -m json.tool
curl -s localhost:8000/v1/orders | python3 -m json.tool     # status=submitted, futu_order_id 非空
# 富途牛牛客户端模拟账户里能看到这笔委托（人工核对一次）
```
**完成标准**：重复 execute 幂等返回同一单；`trading_halted=true` 时 100% 拒绝；代码中 `environment` 字面量只出现 `SIMULATE`（grep 验证）。

> ⚠ DEVIATION: S2 联调必须打开 `futu_enable_trade`，但旧通用
> `/v1/futu/trade/{context}/{method}` 会随之直接放行 `place_order`、改单和撤单，绕过
> `paper_trading_enabled`、实时风控、幂等与 Kill Switch；实测当前 SDK 的
> `position_list_query` 还会返回内部 `acc_id`。本步先在 HTTP 层固定阻断全部
> `TRADE_MUTATION_METHODS`，并将 `get_acc_list`、`position_list_query` 收为内部方法；
> 专用 executor 仍可调用底层白名单。由于执行前资金、持仓和未完成委托都依赖只读交易
> 查询，实际前置开关是 `trade_query + futu_enable_trade + paper_trading_enabled` 三项，缺一
> 均以 403 拒绝；REAL 三重门禁与 `unlock_trade` 永久阻断未改动。

> ⚠ DEVIATION: 规格中的“先断言 proposal.status=approved，再做幂等”会让首次成功后状态已
> 为 `executing` 的重复请求无法返回原单。实现保持所有全局开关和 halt 最先检查，但随后先按
> `proposal_id` 返回非 failed 既有单，仅在没有既有单时要求 `approved`。同时把
> `proposal_id` 设为唯一外键，下单前提交 `submitting` 预约，并使用不超过 64 字节的确定性
> Futu remark；单进程锁与数据库唯一约束共同挡住并发双击。网络结果不确定时保留
> `submitting` 并禁止自动重试，避免外部订单已成功但本地盲目重下。
> 既有 reject 路由原本还能把 `executing` 提案改成 `rejected`、却无法撤回外部订单；本步将
> reject 收紧为仅 `pending` 可用，执行中或终态请求固定 409，保证后续 S3 对账状态不失真。

> ⚠ DEVIATION: 原规格只写“实时组合状态换算”，但现有公开持仓 DTO 丢弃了执行风控必需的
> `today_pl_val`，且本地订单表不能代表 Futu 侧真实未完成委托。本步增加仅内部使用的风险
> 持仓读取及 `order_list_query(refresh_cache=true)`，资金也强制刷新；目标股票与全部持仓
> 行业、关键数值任一缺失即 fail closed。执行风控使用实时快照、整手数量与 1% 滑点限价
> 重算金额，不信任提案内可低报的旧 `estimated_notional`。既有 SELL 行业风控的保守行为
> 不属于本步，本次未放松；P2.2 R2 的风格分类阈值也按要求只观察，未改码。

> ⚠ DEVIATION: 本机应用清单没有富途牛牛桌面客户端，只有已登录的 OpenD，因此无法诚实
> 完成“牛牛客户端人工核对”。改用 OpenD 对同一模拟账户执行只读 `order_list_query`：订单
> `6975125` 唯一存在，remark 与本地提案哈希一致，状态 `FILLED_ALL`、成交 100 股、均价
> 8.84；该替代证据不冒充 App 截图。后续可由用户在手机/另一终端补一次界面核对。

**验证记录（2026-07-22 盘中）**：

- 全量质量门通过：`ruff check src tests`、strict `mypy --no-incremental src/alphapilot`
  （98 个源文件）、`pytest -q`（286 tests）及 `git diff --check`；新增模型约束、专用执行器、
  订单列表/详情 API、HTTP 通用路由封口及并发/失败不确定性均有离线回归。
- 三个纸上交易开关开启且明确保持 `live_trading_enabled=false`、`trading_halted=false`；
  OpenD 的行情与交易登录均健康。真实创建并批准提案
  `p23-s2-live-20260722T021152Z-600000`，专用 execute 提交浦发银行 100 股、限价 8.93，
  返回本地 order#1、Futu order `6975125`、`environment=SIMULATE`、本地
  `status=submitted`，proposal 联动为 `executing`。
- 重复 execute 返回同一个本地/Futu 订单；SQLite 仅 1 行，OpenD 按订单号对账也仅 1 条，
  没有第二次 place。临时把 `trading_halted=true` 并重启受管 API 后，重复请求实测 423，
  订单行仍为 1；随后已恢复 halt=false，LaunchAgent/API/OpenD 均健康。
- `/v1/orders` 与 `/v1/orders/1` 均返回 UTC 时间和非空 Futu ID；executor 静态 grep 仅有
  `environment="SIMULATE"`，没有 `REAL` 或 confirmation。通用 HTTP `place_order` 与
  `unlock_trade` 实测均为 403。

---

## P2.3-S3 订单状态轮询与成交回填 ✅ done 2026-07-22

**新增** `jobs/order_sync.py::sync_orders()`：
- 对 status ∈ {submitting, submitted, partial} 的 broker_orders：`trade_call('security','order_list_query', kwargs={'acc_id':..,'order_id':..}, environment='SIMULATE')` → 映射富途 order_status → 本地状态；filled_qty/avg price 回填；终态时把关联 proposal 置 `executed`（全部成交或部分成交后手动撤单）/保持 executing。
- 注册 interval 30s，`enabled_key="paper_trading_enabled"`。
- 状态映射表写成模块级 dict（富途枚举字符串 → 本地），未知状态记 warning 不崩。

**验证**：S2 下的单在 1-2 个轮询周期内变 `filled`（模拟盘一般即时成交）；proposal 状态联动为 executed。

> ⚠ DEVIATION: S2 为防止下单结果不确定时盲目重试，先落库的
> `submitting` 预约可能没有 `futu_order_id`；原规格只按 `order_id` 查询无法恢复
> 这类外部已成功订单。实现对有订单号的记录仍做单号精确查询；仅对无订单号
> 预约做当日 SIMULATE 委托全量只读查询，再以 S2 的确定性 `remark` 唯一匹配，
> 并二次校验账户环境、代码、方向和数量；0 条、多条或任一字段不符均只记
> warning，保留 `submitting` 且绝不重下。

> ⚠ DEVIATION: OpenD 真实只读 `order_list_query` 在联调前曾出现一次
> `PacketErr.Timeout`，重查成功；因此轮询不在网络调用期间持有 DB 事务，并对单笔
> 查询失败做隔离。同轮其他订单的成功回填照常提交，任务最终以携带脱敏
> stats 的 `JobExecutionError` 失败收口，下一周期安全重查；不在 stats 中放
> `acc_id` 或富途原始响应。

**验证记录（2026-07-22 盘中）**：

- 重启 launchd 受管 API 后手动运行 `POST /v1/jobs/sync_orders/run`，对 S2 真实
  SIMULATE 订单 `6975125` 只读查询成功：JobRun#1105 为 `ok`、
  `active=1/queried=1/updated=1/filled=1/query_errors=0`；本地 order#1 从
  `submitted` 回填为 `filled`，成交 100 股、均价 8.84，proposal 原子联动为
  `executed`。该任务只调用账户发现与 `order_list_query`，未调用任何富途
  交易写接口。
- 终态后再手动运行返回 `active=0/queried=0/updated=0`，不再打开富途客户端；
  最新代码重启后的两个 30 秒调度周期（JobRun#1131/#1133）均为 `ok`、
  `active=0/query_errors=0`，证明 interval 注册、`paper_trading_enabled` 开关与幂等终态
  均生效。
- 实现显式覆盖 16 个富途状态，未知状态仅 warning；成交数量非有限、
  越界、倒退，部分成交为 0/全量，或成交均价无效均 fail closed。执行器与
  轮询共用进程级串行保护，下单网络返回后再 refresh ORM；双 Session 竞态用例证明
  已回填的 `partial/filled/cancelled/failed` 不会被旧提交请求覆盖回
  `submitted`。
- 完整质量门通过：`ruff check src tests`、strict
  `mypy --no-incremental src/alphapilot`（99 个源文件）、`pytest -q`（327 tests）以及
  `git diff --check`；最新 LaunchAgent/API/OpenD 均健康。P2.2 R2 风格分类阈值仍仅
  观察，本步未修改。

---

## P2.3-S4 组合快照与收益归因（09 稿四卡数据源）

**models.py**：

```python
class PortfolioSnapshot(Base):
    __tablename__ = "portfolio_snapshots"
    trade_date: Mapped[date] = mapped_column(Date, primary_key=True)
    total_value: Mapped[float]; cash: Mapped[float]
    positions: Mapped[list[Any]] = mapped_column(JSON, default=list)   # [{symbol,qty,mv,industry}]
    daily_return: Mapped[float | None] = mapped_column(Float)
    benchmark_return: Mapped[float | None] = mapped_column(Float)      # SH.000300 当日
    excess_return: Mapped[float | None] = mapped_column(Float)
    drawdown: Mapped[float | None] = mapped_column(Float)              # 净值相对历史峰值
    source: Mapped[str] = mapped_column(String(16), default="futu-sim")
```

**job** `snapshot_portfolio`（cron 交易日 15:10）：fetch_account_funds + positions → 写当日行；daily_return = total_value/前日 -1；benchmark 从 daily_bars 的 SH.000300 取；drawdown 由净值序列滚动峰值算。持仓行业从 securities.industry_csrc 联出（donut 用）。
**API**：
- `GET /v1/portfolio/overview` → 最新快照 + 行业分布（05 稿 donut：总市值、行业占比）；
- `GET /v1/portfolio/attribution?days=60` → `{dates[], nav[], benchmark_nav[], excess_cum, max_drawdown, benchmark_drawdown}`（09 稿"收益归因/风险控制"两卡 + 净值对比图）。

**验证**：连续两个交易日后 daily_return 与富途 App 模拟账户涨跌一致（±0.1pp）；归因接口序列长度=天数。

---

## P2.3-S5 信号级归因（09 稿机会/错误复盘）

**models.py**：

```python
class AlertOutcome(Base):
    __tablename__ = "alert_outcomes"
    alert_id: Mapped[int] = mapped_column(Integer, ForeignKey("alerts.id"), primary_key=True)
    horizon_days: Mapped[int] = mapped_column(Integer, default=5)
    realized_return: Mapped[float | None] = mapped_column(Float)
    hit: Mapped[bool | None] = mapped_column(Boolean)
    contribution: Mapped[float | None] = mapped_column(Float)   # suggested_position_change × realized
    evaluated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow)
```

**job** `evaluate_alerts`（cron 19:40）：对 created_at ≥5 个交易日前且无 outcome 的提醒：realized = 该股 5 交易日收盘收益（daily_bars）；hit 定义：买类(BUY_CANDIDATE/ADD) realized>0；减类(REDUCE/EXIT/STOP) realized<0；WATCH/HOLD/REVIEW 不评（hit=None 不计入命中率）。
**reports.py 改造**：复盘 payload 增加 `signal_attribution: {hit_rate_directional, top_hits: [...], top_misses: [...], by_action: {...}}`；`hit_rate` 环比 = 与前一日报告的差（daily_reports 里翻昨日）。

**验证**：运行若干天后 top_hits/top_misses 非空且 contribution 排序正确；单测用构造数据覆盖买/减两类 hit 判定。

---

## P2.3-S6 REAL 拦截回归测试 + Kill Switch（安全交付物）

**tests/test_trading_safety.py（新）**，全部离线（FakeFutuModule 复用 test_futu_client.py 的）：
1. `paper_trading_enabled=False` 时 execute_proposal 抛 ExecutionBlocked；
2. `trading_halted=True` 时同样拒绝（即使其他开关全开）；
3. FutuClient：`trade_call(..., environment='REAL')` 在 live_trading_enabled=False 时抛 FutuFeatureDisabledError（已有用例，加一条经 executor 路径不可能传 REAL：静态断言 executor 源码常量 + 运行时 FakeTradeContext 记录 trd_env=='SIMULATE'）；
4. `unlock_trade` 仍被 PERMANENTLY_BLOCKED（既有用例保留）；
5. 幂等：同一 proposal 二次 execute 不产生第二笔 place_order 调用（Fake 计数）。

**API**：`POST /v1/trades/halt` / `POST /v1/trades/resume`（写运行时开关：直接改 get_settings() 不可行——settings 是 lru_cache 只读；实现为 DB 单行表 `runtime_flags(key pk, value bool, updated_at)`，executor 读它；halt 立即生效）。顺带把 `trading_halted` 从 config 移到 runtime_flags（config 键保留作初始值）。

**验证**：`pytest tests/test_trading_safety.py -q` 全绿；halt 后 execute 返回 423（Locked，中文理由）；resume 恢复。

---

## P2.3-S7 里程碑验收

- [ ] 全流程 E2E：提醒 → 生成提案 → 批准 → 执行(SIMULATE) → 订单 filled → 组合快照变动 → 次日归因接口反映
- [ ] 富途牛牛 App 模拟账户可见每笔委托（人工抽查 2 笔）
- [ ] test_trading_safety 全绿；grep 全仓库无新 `REAL` 执行路径
- [ ] Kill Switch 实测生效
- [ ] 质量门 + 一期全部旧测试仍绿

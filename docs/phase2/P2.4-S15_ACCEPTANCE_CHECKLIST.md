# P2.4-S15 全站验收清单

> 状态：**✅ 验收通过 2026-07-23**
>
> 硬前置：P2.3-S7 的第二个真实组合快照、归因 ±0.1pp 对账、文档标记与独立提交全部完成。
> 安全边界：验收只读；不得创建提案、批准提案、执行订单、调用账户变更或 `unlock_trade`。

## 0. 开始门

- [x] 当前时间晚于 2026-07-23 15:10 Asia/Shanghai。
- [x] P2.3-S7 标题已标 `✅ done 2026-07-23`，并存在独立 S7 commit `72ce55f`。
- [x] S7 复核记录已写入：“提醒→提案→执行端到端链路仅离线测试覆盖，未经真实提醒信号验证，留待补验”。
- [x] `portfolio_snapshots` 有 2026-07-22、2026-07-23 两个真实交易日；归因已由架构师批准越门。
- [x] `com.alphapilot.api`、OpenD、DB 健康；`live_trading_enabled=false`、`unlock_trade_endpoint_exposed=false`。
- [x] 验收基线 `72ce55f`；最终 API PID 91498，OpenD SDK 10.09.6908/server 1009，数据库最新交易日 2026-07-23；证据未记录密钥或账户标识。

## 1. 九稿逐项对照

统一截图视口为设计稿原尺寸 **1672×941**，证据保存到
`docs/phase2/screenshots/p2.4-s15/`。每个真实页面同时检查：

- 浏览器 console 无未处理 error；Network 无未解释的 4xx/5xx；
- `document.documentElement.scrollWidth <= clientWidth`，无横向溢出；
- 数字使用等宽/数字字形，日期、来源、口径、模型版本与降级原因可见；
- 空值显示 `—` 或中文原因，不显示 `undefined`、`NaN`、假 0 或示例数；
- 只读浏览，不点击重算、已读、提案批准、执行、拒绝或 Kill Switch。

| 稿 | 路由/处置 | 必验内容 | 证据 |
|---|---|---|---|
| 01 市场总览 | `/` | 风格堆叠、全行业热力、环比箭头、通知铃铛、AI/模板来源 | `01_市场总览_1672x941.png` |
| 02 登录欢迎页 | 范围处置，见下方偏差 | 对照视觉稿并确认二期不引入伪账号/伪登录 | `02_登录欢迎页_SCOPE_DISPOSITION.png` + `.md` |
| 03 AI选股 | `/screening` | 全市场筛选、胜率口径、真实因子权重、状态 diff、CSV 导出 | `03_AI选股_1672x941.png` |
| 04 个股分析 | `/stock/600519` | 行情头、多周期 K+B/S、五维雷达、AI 解读、事件 tabs、目标区间 | `04_个股分析_1672x941.png` |
| 05 自选追踪 | `/watchlist` | 漂移摘要、事件图标/事件流、分组管理、市值 donut | `05_自选追踪_1672x941.png` |
| 06 板块预测 | `/sectors` | 5/10/20 日、整体胜率口径、生命周期、超买/反转、资金流、联动榜 | `06_板块预测_1672x941.png` |
| 07 大盘监控 | `/market` | 情绪、宽度、涨跌停/炸板、同刻量能环比、分时、监测 feed、跨市场 | `07_大盘监控_1672x941.png` |
| 08 交易提醒 | `/alerts` | 目标区间、建议金额、有效期、失效条件、SIMULATE 三态流水；不执行 | `08_交易提醒_1672x941.png` |
| 09 AI复盘 | `/review` | 归因四卡、净值 vs 沪深300、机会/错误、AI/统计建议、事件时间线 | `09_AI复盘_1672x941.png` |

> ⚠ DEVIATION（已知范围差异）：`docs/PHASE2_DESIGN.md` §7 明确交付为“8 页升级”，§8.5
> 将 02 登录页列为待拍板项；当前路由也只有 8 个产品页。S15 必须对九张稿全部给出处置，但不得
> 为凑“9 个页面”伪造账号体系。02 稿以范围裁定文件留证；若架构师要求真实第九张浏览器截图，
> 必须先明确授权新增本地欢迎/锁屏页，再实施并单独验证。

补充响应式烟测（不替代 1672×941 主证据）：

- [x] 8 个真实路由在 1440、1121、760、392px 均无页面横向溢出。
- [x] 1121px 双栏正确下沉；760px 表格可控滚动；392px 指标单列且日期不截断。
- [x] K 线、分时、净值图 tooltip 在窄屏不越出视口，日期和单位完整。

## 2. 无 LLM 降级

执行时只使用**进程级临时覆盖**，不编辑 `.env`、不打印或复制 key：

- [x] 记录正常模式页面来源与最近 `llm_calls` 审计行数量。
- [x] 临时令 `ALPHAPILOT_LLM_API_KEY`、`ALPHAPILOT_LLM_BASE_URL` 为空并重启受管 API。
- [x] `/health` 仍为 200，DB/Futu 健康，交易安全标志不变。
- [x] 逐页检查总览、个股、监测 feed、复盘：规则/模板/统计降级可用，并明确生成方式。
- [x] 页面不写死 Qwen/DashScope；无 LLM 时不出现空白卡、无限 spinner 或伪 AI 文本。
- [x] 验证 `market_summary`、`stock_insight`、`review_advice` 等失败不污染已保存的真实数值。
- [x] 清除临时进程覆盖、重启 API；确认原 LLM 配置恢复但不显示密钥。

证据：

- `degrade_no_llm_overview.png`
- `degrade_no_llm_stock.png`
- `degrade_no_llm_market.png`
- `degrade_no_llm_review.png`
- `degrade_no_llm_evidence.md`（命令、HTTP 状态、source 字段与恢复结果，全部脱敏）

## 3. 无 Futu / OpenD 降级

仅在 S7 提交后执行，避开组合快照时间门：

- [x] 记录正常 `/health`、OpenD PID/版本与 qot/trd 登录状态。
- [x] `make futu-stop` 后确认 127.0.0.1:11111 不可达；不得改交易配置。
- [x] 8 个真实路由均可进入，无白屏；缓存数据标日期/来源，实时缺口给中文原因。
- [x] 总览/个股/自选/大盘中的独立模块有界失败，不因一个 Futu 请求拖死整页。
- [x] `/v1/portfolio/account`、行情与跨市场接口诚实返回 503/空值，前端正确承接。
- [x] 未用 mock、随机数、昨日值冒充实时值；5 日分时继续明确“接口未提供”。
- [x] `make futu-start` 恢复后，`/health` 的 OpenD/qot/trd 全部健康，受管 API 仍在线。

证据：

- `degrade_no_futu_overview.png`
- `degrade_no_futu_stock.png`
- `degrade_no_futu_market.png`
- `degrade_no_futu_alerts.png`
- `degrade_no_futu_evidence.md`（停止、端口、接口、恢复健康证据）

## 4. 功能与安全回归

```bash
.venv/bin/ruff check src tests
.venv/bin/mypy src/alphapilot
.venv/bin/python -m pytest -q

PATH="$HOME/.nvm/versions/node/v22.23.1/bin:$PATH" \
  npm --prefix apps/web run build

git diff --check
```

- [x] 全量质量门全绿：Ruff 0、strict mypy 117、pytest 544、前端 build；既有测试没有跳过或删除。
- [x] `tests/test_trading_safety.py`：REAL 三重拦截、永久屏蔽 `unlock_trade`、Kill Switch 回归全绿。
- [x] `tests/test_phase2_executor.py`、`tests/test_phase2_paper_trading_e2e.py` 全绿；三文件定向共 29 项通过。
- [x] `/health` 最终为 v0.3.0、DB/OpenD healthy、live=false、unlock=false。
- [x] `/v1/alerts` 是 2026-07-23 13:15 CST 已刷新数据；`/v1/reports/daily?report_date=2026-07-23`
  的 `sector_call_excess.available=true`，预测日/兑现日和 Top3 明细可审计。
- [x] S15 前后 `trade_proposals=1`、`broker_orders=1`，最大 id 均为 1，验收未新增提案/委托。

> ⚠ DEVIATION: 当前 schema 不存在规格清单所写的独立 `broker_fills` 表；成交状态存于
> `broker_orders.status/filled_qty/avg_fill_price`。验收以 `broker_orders` 的 1 条已成交模拟订单为
> 基线并复核未增长，没有为凑表名静默新增重复存储。

## 5. CHANGELOG 0.3.0

- [x] 新增 `## 0.3.0 - 2026-07-23`，覆盖四个二期里程碑：
  - P2.1：全市场数据底座、调度/审计、断点续传、写锁与熔断修复；
  - P2.2：多因子、风格、板块预测、五维评分、情绪与漂移；
  - P2.3：仅 SIMULATE 的券商订单同步、组合快照与归因，REAL 安全门不变；
  - P2.4：可降级 LLM、事件/解读/复盘、通知中心与 8 页 UI 收口。
- [x] 写明 Qwen 是当前 OpenAI-compatible HTTP 部署，不宣称 Claude CLI/Codex CLI 已支持；
  CLI transport adapter 明确留到 P3。
- [x] 写明 02 登录稿未进入本地单用户二期范围，避免把范围差异包装成已实现。

## 6. README

- [x] “当前状态”升级为 v0.3 二期版，并同步测试数量与安全边界。
- [x] 补运行环境：`make futu-start`、受管 `com.alphapilot.api`、arm64 Node v22.23.1。
- [x] LLM 配置说明保持 provider-neutral：OpenAI-compatible base URL/key/model/per-purpose model；
  `enable_thinking=false` 仅对当前 Qwen 请求保留，不宣称任意 CLI transport 可用。
- [x] API 表补齐二期实际接口：市场监控、板块预测、五维/解读/事件、筛选 diff、风格、自选摘要、
  portfolio、proposal execute/orders、notifications/events/jobs。
- [x] 明确日报 `sector_call_excess`、双降级行为、北向停发和 5 日分时缺口。
- [x] 所有本地绝对路径、密钥位置、账户标识和真实账号信息不得进入 README。

## 7. 收官门

- [x] `docs/PHASE2_DESIGN.md` §9 每一项均有对应证据并勾选。
- [x] 04 文档 S15 标题仅在全部检查完成后标 `✅ done 2026-07-23`。
- [x] S15 实现、截图索引、CHANGELOG、README 与验收记录组成一个独立提交。
- [x] 最终工作区只保留用户原有的无关改动；未夹带数据库、Futu 安装包或设计原稿。
- [x] 最终报告明确区分：已验证、诚实降级、已知限制、P3 后续。

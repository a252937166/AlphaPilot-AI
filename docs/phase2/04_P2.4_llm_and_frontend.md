# P2.4 LLM 层 + 前端二期 + 通知中心 — 实施规格

> 前置：P2.2 与 P2.3 完成（前端消费它们的 API）。
> 前端约定：沿用 `apps/web/src/style.css` 设计系统（`.panel/.stat-card/.badge/.heat-grid/.tab-pills/.glow-*/.icon-chip/.timeline/.kv/.feed-row/.skeleton`）、`chartTheme.ts`（tooltipStyle/categoryAxis/valueAxis/glowLine/areaGradient/SERIES_PALETTE）、lucide-vue-next 图标（**禁止 emoji 图标**）、`api.ts` 统一请求、`format.ts` 格式化。所有新数字类展示加 `.num`。每页完成 = `npm run build` 通过 + 浏览器该页无 console error + 数据不可用时有中文降级提示。

---

## P2.4-S1 LLM 客户端与降级框架 ✅ done 2026-07-22

**新增** `src/alphapilot/llm/__init__.py`、`llm/client.py`、`llm/prompts.py`。

```python
# llm/client.py
class LLMUnavailable(RuntimeError): ...

def chat_json(purpose: str, system: str, user: str, schema: dict[str, Any],
              *, timeout: float | None = None, max_retries: int = 1,
              settings: Settings | None = None,
              session: Session | None = None) -> dict[str, Any]:
    """settings.llm_* 未配置 → 抛 LLMUnavailable（调用方必须有降级分支）。
    timeout=None 时按 purpose 使用 45s/30s/60s 分级超时；model 默认 settings.llm_model，
    settings.llm_purpose_models 可按 purpose 覆写。
    调 {llm_base_url}/chat/completions，要求 JSON 输出；返回后用 jsonschema 校验
    （pyproject 加 "jsonschema>=4,<5"），不合规重试一次仍失败 → LLMUnavailable。
    每次调用写 llm_calls 表：purpose/model/latency_ms/ok/tokens(如响应带 usage)。"""
```
**models.py**：`LLMCall(id, purpose, model, ok, latency_ms, prompt_tokens, completion_tokens, error, created_at)`。
`llm/prompts.py`：模板常量集中管理（EVENT_EXTRACT / STOCK_INSIGHT / REVIEW_ADVICE），版本号写在模板 docstring。
**改造** `services/ai_text.py` 用新客户端（保留现有模板降级路径不变）。
**验证**：无 LLM 配置时 pytest 全绿且 dashboard ai_summary.source=="template"；配置 Qwen 后
`curl /v1/dashboard/overview` source=="llm"。两种模式均须真实请求验证，且 `llm_calls` 分别记录
`not_configured` 与成功调用；相同 context 的第二次摘要请求不得新增 LLM 调用。

> ⚠ DEVIATION: 原规格的 `chat_json` 未接收当前 DB session。实测 dashboard 外层 SQLite 事务中
> 另开 session 写 `llm_calls` 会触发锁竞争并丢失审计，因此客户端增加可选 `session` 注入，dashboard
> 与 reports 复用当前事务；无调用方 session 时仍由客户端自开 session。未改变 API 响应契约。
>
> 验证记录（2026-07-22）：Ruff、strict mypy、382 个 pytest 全绿；隔离无 key API 返回
> `source=template` 且审计 `not_configured`；本机 Qwen 返回 `source=llm`，审计模型
> `qwen3.6-flash`、`ok=1`；真实相同 context 连续两次仅新增 1 条 `llm_calls`。

---

## P2.4-S2 公告事件抽取 ✅ done 2026-07-22

**engines 无关，属 llm 应用**。新增 `services/event_extract.py`：

```python
EVENT_SCHEMA = {  # jsonschema
  "type": "object", "required": ["event_type", "direction", "strength", "summary", "source_quote"],
  "properties": {
    "event_type": {"enum": ["earnings", "dividend", "holder_change", "regulation",
                             "contract", "buyback", "personnel", "other"]},
    "direction": {"type": "number", "minimum": -1, "maximum": 1},
    "strength": {"type": "number", "minimum": 0, "maximum": 1},
    "horizon_days": {"type": "integer", "minimum": 1, "maximum": 250},
    "summary": {"type": "string", "maxLength": 120},
    "source_quote": {"type": "string", "maxLength": 200}}}

def extract_disclosure_event(session, disclosure: Disclosure) -> DomainEvent | None:
    """LLM: system=EVENT_EXTRACT 模板(要求 source_quote 必须是标题原文子串，校验不通过即降级)，
    user=标题(正文暂无)。LLMUnavailable/校验失败 → 关键词规则降级：
    含'预增|中标|回购|分红' direction=+0.5；'预减|问询|处罚|减持' -0.5；其余 0。
    结果 emit 到 events（source_ref=f'disclosure:{id}', direction/strength 入库）。"""
```
挂进 `sync_disclosures`（新公告逐条抽取）；补一个 job `backfill_events` 对存量公告跑一次。
**验证**：同步茅台公告后 `/v1/events?symbol=600519` 的 disclosure 事件带非零 direction 与 summary；`llm_calls` 有记录；断开 LLM 后新公告仍产生（规则降级）事件。

> ⚠ DEVIATION: P2.1-S8 已把 `DomainEvent.event_type` 定义为生产者粗分类，并由现有
> `types=disclosure` API/UI 精确过滤；S2 Schema 的 `earnings/dividend/...` 实际是公告子类型。
> 为避免破坏既有契约且不擅自加表列，持久化仍使用 `event_type=disclosure`，子类型以
> `<中文类型标签>｜<中文摘要>` 写入 summary；英文 LLM 摘要强制降级。回填会原位升级旧事件，
> `source_ref` 与事件行数不变；预提交实测产生的英文前缀仅作为兼容输入识别并无 LLM 地转中文。
>
> ⚠ DEVIATION: `backfill_events` 是一次性存量迁移，注册为 `trigger=None` 的手动任务，
> 仅通过 `POST /v1/jobs/backfill_events/run` 执行；scheduler 显式跳过，避免周期性重复消耗 LLM。
>
> ⚠ DEVIATION: 原始“新公告逐条抽取”若在同一 SQLite session 内边抽取边写入，首条
> `llm_calls/events` DML 会让第二条 Qwen 网络等待全程持有 writer lock。同步链路因此拆成
> “先完成整批分类与独立短事务审计，再集中持久化”；存量回填则在每条 LLM 调用前关闭读取
> session，调用后另开短写事务。任一回填行失败会抛带完整 stats 的 `JobExecutionError`，
> JobRun 如实记 failed，已成功行保持幂等、失败行下次重试。并发 HTTP sync/backfill 对
> `uq_disclosures_symbol_url` 与 `uq_events_source_ref` 使用 SQLite 原子
> `INSERT ... ON CONFLICT DO NOTHING`，消除先查后插竞态。
>
> ⚠ DEVIATION: 真实同步验证发现 httpx 的 INFO 请求日志会完整记录巨潮 `access_token`
> 查询参数，违反执行指南的密钥安全不变量。已将 `httpx/httpcore` 传输日志固定为 WARNING，
> OAuth 异常不再拼接响应 payload，并对本机 API 日志中的既有 token 值完成定向脱敏；重启后
> 再次同步未新增任何含 `access_token` 的日志行。
>
> 验证记录（2026-07-22）：Ruff、strict mypy、403 个 pytest 全绿；批量双公告回归证明两次
> 分类结束前调用方事务未写 disclosures/events（LLM 审计各自短事务提交），重复同步不新增
> 分类调用；双 session 同步同一新公告只留下 1 条公告和 1 条事件；英文模型摘要按中文规则
> 降级。无 key 隔离实测生成分红规则事件 `direction=+0.5`，审计为 `not_configured`；本机
> Qwen 实测返回 `subtype=buyback/direction=+1.0/source=llm`，`qwen3.6-flash` 审计成功。
> 茅台同步连续两次均为 `inserted=0/events_extracted=0` 且未新增 LLM 调用；回填连续两次均
> `pending=0/failed=0/skipped=30`；`/v1/events?symbol=600519&types=disclosure` 返回 30 条，
> 其中 4 条方向非零、30 条摘要非空、英文类型前缀为 0。API 经 launchd 重启后保持健康。

---

## P2.4-S3 个股 AI 解读（04 稿"AI解读/投资逻辑/催化剂/风险"） ✅ done 2026-07-22

**models.py**：`StockInsight(symbol pk, generated_at, core_view Text, drivers JSON, model_version, source)`
drivers 元素：`{"text": "...", "tag": "利多|利空|中性", "source_ref": "event:12|score:tech|sector:SH.BK0031"}`。

**services/insight.py::get_or_build(session, symbol, force=False)**：
- 缓存 24h 内直接返回；事件总线该股有新事件则视为过期；
- 输入打包：五维分(StockScore) + 最近 5 事件 + 板块 forecast 行 + 最新预测 horizons + 巨潮档案；
- LLM 输出 schema：`{core_view: str<=120, drivers: [{text<=40, tag, source_ref}]*3..6}`，**要求每条 driver 的 source_ref 必须来自输入清单中给出的 id 列表**（prompt 中明示，收到后校验非法 ref 即丢弃该条）；
- 降级（无 LLM）：规则模板拼 drivers（五维最高/最低维、最近事件、板块状态，各配 tag）——保证页面永远有内容，source 标 "rule"。
**API**：`GET /v1/stocks/{symbol}/insight?force=`。
**验证**：有/无 LLM 两种模式下均返回 3-6 条 drivers 且 tag 枚举合法；二次请求命中缓存（latency <50ms）。

> ⚠ DEVIATION: 原规格把最新预测与巨潮档案列为输入，但示例未定义这两类证据的
> `source_ref`。为避免模型引用不可追溯，白名单扩展为 `forecast:<snapshot_id>` 与
> `profile:<symbol>`；仍要求逐字命中本次输入清单，非法、重复或英文 driver 丢弃，不足 3 条时
> 整组切回规则模式，禁止混合来源冒充纯 LLM 结果。
>
> ⚠ DEVIATION: 真实 `sector_constituents.symbol` 沿用富途 `SH.600519`/`SZ.*` 码，而个股主键
> 为六位数字。S3 查询同时接受三种边界格式并复用规范化校验；板块预测若未与最新
> `SH.000001` 日线同日则不进入解读，避免展示陈旧板块状态。
>
> ⚠ DEVIATION: 真实 `StockScore` 会把缺失因子按中性值映射为 5 分，不能把该持久化分数误当成
> 完整证据。S3 因此同时读取 radar/missing_factors/input_coverage：完全无输入的维度不进入 LLM
> 引用白名单，规则文案明确标“因子输入暂缺”；完全无持久化证据的六位代码也不调用 LLM，
> 仅返回 3 条“暂无有效数据”的中性状态，不填假数字，仍满足页面始终有诚实内容。
>
> ⚠ DEVIATION: SQLite 下两个请求并发首建同一股票时，普通 ORM INSERT 会发生主键竞态。
> 实现增加进程内按 symbol 构建锁与 `INSERT ... ON CONFLICT DO UPDATE`，并用
> `generated_at` 水位阻止慢旧结果覆盖新结果；双 Session 并发回归证明双方成功且最终仅一行。
>
> 验证记录（2026-07-22）：Ruff、strict mypy、418 个 pytest 全绿；S3 的 15 个离线测试覆盖
> 严格 Schema、动态引用白名单、中文校验、稀疏规则降级、24h/模型版本缓存、事件
> `ingested_at` 失效、force、缓存契约自愈、评分输入覆盖、板块前缀、预测并列断点与并发首建；
> 无任何证据的代码返回 3 条中性数据状态且不调用 LLM。隔离无 key 实测返回
> `source=rule`、3 条合法 driver，审计 `not_configured`，二次缓存 1.06ms 且不新增调用；本机
> Qwen 实测返回 `source=llm`、3-6 条合法 driver，审计模型 `qwen3.6-flash`/`ok=1`，连续缓存
> 请求 3.2-12.1ms 且不新增调用。预提交验证产生的 1 行缓存表已重建以应用最终 source 约束并
> 重新生成；受管 API 与 OpenD 均健康，trade proposal/order 仍各 1 条，未触发任何交易调用。

---

## P2.4-S4 盘中 AI 监测 feed（07 稿） ✅ done 2026-07-22

**services/market_monitor.py::build_feed(session, limit=20) -> list[dict]**：
纯规则从真实数据生成条目（**不依赖 LLM**）：
- market_snapshot_agg 相邻两行：量能环比 ±5% / 宽度反转 / 涨停数变化 ≥5 / 炸板率 >30%；
- sector 强度榜首更替；sentiment label 换档；自选 capital_anomaly 事件。
每条 `{ts, text(中文事实句), level: info|warn}`；若 LLM 可用且 `settings.llm_polish_feed=true`（新配置默认 false）批量润色一次。
**API**：`GET /v1/market/monitor-feed`。
**验证**：盘中返回 ≥3 条且全部可从聚合表复算；收盘后返回当日存量。

> ⚠ DEVIATION: `market_snapshot_agg.total_amount` 的真实口径是当日累计成交额，不是单分钟
> 成交量；规则仍按相邻快照的 ±5% 阈值计算，但用户文案明确写“累计成交额”，避免误导。
>
> ⚠ DEVIATION: 若把“炸板率 >30%”逐分钟都写成 feed，2026-07-22 的 315 行真实快照会有
> 306 行处于阈值之上并淹没其他信号。因此阈值本身保持严格 `>30%`，只在炸板率从 `<=30%`
> 跨入 `>30%` 时生成预警；连续高位不重复刷屏，当日再次回落后重新上穿仍会再次提醒。
>
> ⚠ DEVIATION: `SectorSnapshot` 当前由板块强度接口按需采样，不与 60 秒全市场轮询同步；
> 本步只比较真实存在的当日采样点，不插值、不伪造缺失板块快照。市场快照与情绪记录也不是
> 严格一一对应（验证时历史总量分别为 319/316），因此各数据源只在自身时间序列内相邻比较。
>
> 验证记录（2026-07-22）：Ruff、strict mypy、全量 449 个 pytest 全绿；新增 31 个离线测试
> 覆盖阈值边界、上海日界、未来行过滤、全局排序、API 参数和 LLM 整批降级。LaunchAgent API
> 重启后 `/v1/market/monitor-feed?limit=20` 返回 20 条当日事实。独立从 315 行市场快照和 5 个
> 板块采样点复算得到 26 个候选（量能 15、宽度反转 4、涨停变化 3、炸板率上穿 2、板块榜首
> 更替 2；当日情绪换档和自选资金异动均为 0）。默认 `llm_polish_feed=false` 不调用模型；显式
> 开启后 Qwen `qwen3.6-flash` 单批调用一次、审计成功，仅改中文文案并保持时间与 level 不变。

---

## P2.4-S5 复盘增强（09 稿 AI 改进建议 + 事件时间线） ✅ done 2026-07-22

**reports.py 改造**：payload 增加
- `improvement_suggestions`: 输入 = alert_outcomes 分组统计（按 action/板块/波动三分位的命中率表）→ LLM 生成 ≤4 条建议（schema 校验）；降级 = 直接返回统计表格行（前端表格展示，标"统计"）；
- `event_timeline`: 当日 events（含市场级）按时间排序 top10，带 type 中文标签色。
**验证**：generate 后两字段非空（降级模式为统计行）；页面联动 S12。

> ⚠ DEVIATION: 2026-07-22 生产库尚无满五个交易日的 `alert_outcomes`，当日 `events`
> 也为 0。为遵守“UI 不摆假数据”，真实 POST→GET 返回带中文 `empty_reason` 的稳定 envelope，
> 不伪造零命中率、统计行或事件；有样本时的三维分组守恒、LLM/统计双模式、市场级事件及
> latest top10 由 14 项离线构造测试验证，真实 Qwen 聚合输入也实测了合格建议与整批降级。

> ⚠ DEVIATION: `sector_constituents` 只有每周刷新的当前快照，没有历史生效区间，无法声称历史
> point-in-time 板块归属。报告显式返回 `sector_membership_basis=current_snapshot`；缺失或冲突
> 成员统一进入“板块未知”，每个 outcome 在 action/板块/波动三分位三个维度均只计一次。

> ⚠ DEVIATION: S1 的报告调用约定原本复用当前 DB session；S5 报告会连续调用复盘建议与市场
> 总结，为避免首个 LLM 审计持有 SQLite writer lock 跨越第二次网络等待，`review_advice` 改用
> `session=None` 的独立短事务立即落审计。真实 client 回归确认一次逻辑请求只写一条脱敏
> `llm_calls`，且 prompt/response 不入库。

> 验证（2026-07-22）：Ruff 0 error；strict mypy 111 files 0 error；全量 pytest 退出码 0；
> LaunchAgent API 重启为 PID 92092，POST generate 与 GET 均返回一致 S5 字段，生产报告仅 1 行；
> `/health` 显示 DB/OpenD healthy、`live_trading_enabled=false`、`unlock_trade_endpoint_exposed=false`。

---

## P2.4-S6 通知中心（后端）

**models.py**：`Notification(id, kind(alert|event|job|system), ref_id, title, body, level, read_at nullable, created_at)`。
生产：AlertRecord 创建时、DomainEvent(strength≥0.6) 时、job failed 时各写一条（在对应 service 内，不用信号机制，直接调用 `services/notifications.push(...)`）。
**API**：`GET /v1/notifications?unread_only=&limit=`、`POST /v1/notifications/read`（body: ids 或 all=true）、`GET /v1/notifications/unread-count`。
**验证**：重算信号后 unread-count 增长；read 后归零。

---

## 前端二期（S7-S14，每步一页；组件放 `apps/web/src/components/`）

### P2.4-S7 全局：通知铃铛 + 新组件底座
- `App.vue` topbar 加 `NotificationBell.vue`：lucide `Bell` + 未读 badge（红点数字，30s 轮询 unread-count）；点击开右侧抽屉（fixed 面板，z-index 用 var(--z-overlay)），列表按 kind 过滤 chips，条目点击跳对应页并标已读。
- 新通用组件：
  - `RadarChart.vue`（props: indicators[{name,max}], values[]；ECharts radar，线/填充用 areaGradient(cyan)）
  - `StackedAreaChart.vue`（props: dates[], series[{name,color,values[]}]；stack:'total', areaStyle 渐变，legend 底部）
  - `LifecycleWheel.vue`（props: stages[{key,label,sectors[]}]；实现为环形 5 段 ECharts pie(半径环) + 中心当前主导阶段文本；hover 显示该段板块 chips）
  - `MiniBarRow.vue`（props: items[{label,value,color}]，横向小条组——04 稿五维小柱）
**验证**：build 过；铃铛计数与接口一致。

### P2.4-S8 总览页升级（01 稿收口）
- 指数走势面板加第三个 tab「风格概率」→ `StackedAreaChart` 吃 `/v1/style/daily?days=60`（成长=cyan、价值=green、防御=purple、均衡=slate，顺序固定）；
- 热力图数据源换 `/v1/sectors/strength`（P2.1-S6 后 ~90 行业），tile 网格自动铺满，顶部加着色模式 tab-pills（强度/涨跌/资金流→ heatColor 输入切换，资金流用 `sector_flow_daily` 联出的 net_inflow 归一）；
- 统计卡行换全市场口径：机会数 = 最新 screening diff 的 new+stayed 中 score≥70 计数（`/v1/screens/diff` + latest）、高置信 = `/v1/alerts` 今日 conf≥0.6 去重（沿用现逻辑）、样本宽度卡换 `/v1/market/breadth-full`（真全市场，含涨停 x 跌停 y 炸板 z 小字）、环比小箭头（lucide TrendingUp/Down + 较昨日 pp）；
- 近期活动数据源换 `/v1/jobs/runs`（真实任务审计）+ 保留信号批次。
**验证**：三 tab 图表可切换；宽度卡显示全市场数字。

### P2.4-S9 AI选股页升级（03 稿收口）
- 筛选条：市场(固定A股disabled)/风格/风险等级/持有周期(映射 sort horizon 5|20)/行业(下拉，选项来自 securities distinct industry_csrc 新接口 `GET /v1/meta/industries`——顺手加)/排序方式 → 组装 ScreenFilter POST；
- 表格列补 胜率(win_rate_20d)/板块(industry)/AI结论(badge: score≥80 强势买入 green、≥65 买入、≥50 观察 blue、<50 减持 red——规则写在组件内常量并与后端 score 口径注释对齐)/状态(new→"新入选" green、stayed→"持有中" blue、来自 diff)；分页(前端 slice, 每页10)；导出 CSV（前端 Blob 生成，文件名 `screen_YYYYMMDD.csv`）；
- 右栏：因子权重条改吃 `/v1/factors/weights`（真实 yaml 权重，负权重显示"(反向)"）；组合风格暴露 donut 吃 `/v1/screens/style-exposure`；AI信心 gauge = 候选平均 confidence（GaugeArc 复用）。
**验证**：行业筛选后结果行业全部匹配；导出文件可打开；权重条与 yaml 数值一致。

### P2.4-S10 个股页升级（04 稿收口）
- 行情头补字段（overview.quote 已含 open/high/low/turnover_rate/pe_ttm/market_cap——若 S4 快照回填后 overview 未透传则在 stocks.py overview 中补），两行 kv 排布同稿；
- K线周期 tabs：日K/周K/月K（`/v1/stocks/{s}/bars?freq=d|w|m`——后端 stock_bars 加 freq 参数，BaoStock frequency 直传，futu K_WEEK/K_MON 映射）+ 分时 tab（`/v1/market/intraday?symbols=` 个股版：允许个股码，RT 订阅同池）；
- **B/S 信号标注**：`GET /v1/stocks/{symbol}/signals`（新，后端从 alerts 表取该股历史，买类→B 绿、减类→S 红）→ ECharts markPoint（pin，coord=[date, close]，click 弹 tooltip 显示理由）；
- 五维评分区：五张 mini 卡（分值 + MiniBarRow）+ `RadarChart`（综合分居中大数字 glow-cyan）；AI评级 = composite → 头部 `x.x/10` 替换现 ConfRing 旁边；
- AI解读卡（右栏首位）：吃 `/v1/stocks/{s}/insight`，drivers 按 tag 着色（利多 green badge/利空 red/中性 gray），底部"依据: source_ref"小字；
- 事件日历区改 tabs：全部/公告/业绩/解禁/分红 → 公告走现有 disclosures，其余走 `/v1/stocks/{s}/calendar` 按 type 过滤，时间线样式不变；
- 底部操作条：买入/持有/减仓 三按钮（primary/ghost/danger 风格，danger=红边 btn 变体加进 style.css）→ 买入/减仓打开确认浮层（展示目标区间 target_low/high、建议金额 suggested_notional、风控预检 `/v1/trades/evaluate` 结果）→ 确认则 POST proposals（mode=confirm_to_trade）并 toast "提案已入库，去交易提醒页批准执行"。
**验证**：周期切换 K 线变化；B/S 标记与该股 alerts 数量一致；三按钮流程走通到提案列表。

### P2.4-S11 自选页升级（05 稿收口）
- 顶部三摘要卡（`/v1/watchlist/summary`）：逻辑强化 n（↑x 7日）/不变/转弱，各配 Sparkline（transitions_7d 按日计数）与 glow 色；
- 表格行内"最新事件"列：该股最近 events 图标组（公告=FileText、日历=CalendarDays、资金=Zap，lucide 14px，title 悬浮预览，点击进个股页事件区）——数据在 `/v1/watchlist/track` 响应扩展 `recent_events`（后端 tracked_overview 联 events 表 top3/股）；
- 分组管理：新建分组（输入框+确认，upsert group_name 任意值）、行多选 checkbox → 批量移组/删除/重算（循环调既有接口即可）；
- 持仓配置 donut 改真实市值：有模拟持仓(`/v1/portfolio/overview`)则按其行业聚合，否则回退 cost×qty 口径，卡片标注口径来源。
**验证**：手动改一条 forecast 触发漂移后摘要卡计数变化；批量操作生效。

### P2.4-S12 板块页升级（06 稿收口）
- 顶部预测周期 tabs 5日/10日/20日 → `/v1/sectors/forecast?horizon=`；排行榜列：排名/板块/热度(net_inflow 归一条)/资金流5日(fmtAmount)/强度(现 score)/未来h日胜率/预期超额/龙头股；
- `LifecycleWheel` 面板（`/v1/sectors/lifecycle`）；
- 三窄卡：最强看多(score top3)/超买预警(`/v1/sectors/overbought`: 板块+RSI+热度+信号badge)/反转潜力(`/v1/sectors/reversal`)；
- 资金流向分布 donut：净流入/净流出 tab（sector flows 正负分组）；
- 龙头扩散链简版：选中排行榜行 → 右栏"联动个股"表（`GET /v1/sectors/{plate_code}/leaders`——后端新增：板块内与龙头 20 日收益相关性 top5，pandas corr 即可）。
**验证**：三 horizon 数字不同且胜率列非空；轮盘 hover 出板块。

### P2.4-S13 大盘页升级（07 稿收口）
- 市场状态区重排：左 情绪 GaugeArc(score/100 大号) + label；右三 kv：赚钱效应/资金面/风险提示（`/v1/market/sentiment`）；
- 分时图 tab（分时/5日/日K）：分时吃 `/v1/market/intraday`（价格线 cyan glow + 均价线 amber 虚线），5日=最近5日分时拼接（后端支持 days=5 参数，v1 可先只做当日+日K）；
- 宽度区换 `/v1/market/breadth-full`：上涨/下跌大数字 + 涨停/跌停/炸板 chips + 今日成交额与环比（07 稿"9842亿 较昨日+612亿"版式）；
- AI实时监测 feed 面板（右栏顶部，`/v1/market/monitor-feed`，level=warn 条目左侧 amber 圆点）；
- 跨市场信号条（页面底部四小卡，`/v1/market/cross`，null 项显示 note 灰字）。
**验证**：盘中分时有曲线；宽度与富途 App 大盘数量级一致；feed 条目可与聚合表对上。

### P2.4-S14 提醒页 + 复盘页收口（08/09 稿）
提醒页：
- 详情卡加 目标价位区间（target_low~high，num）与 建议金额（suggested_notional，¥ fmtNum）；
- "生成交易提案"后新增"批准并执行"按钮（proposal pending → approve → execute 两连调），执行中 spinner，结果 badge（已执行含 avg_fill_price）；执行 tab 表格增加 已执行 状态筛选与成交价列（/v1/orders 联出）；
- 批量操作：全部已读（alerts read 循环）、批量拒绝提案。
复盘页：
- 四统计卡接真实归因：超额收益/最大回撤对比（`/v1/portfolio/attribution`）、命中率环比（reports payload）、板块判断（当日 sector forecast top3 实际超额均值——后端在 report payload 加 `sector_call_excess`）；
- 预测vs实际图替换为 组合净值 vs 沪深300 双线（attribution 序列，glowLine cyan/slate）；
- 机会/错误复盘表（signal_attribution top_hits/top_misses，含贡献收益列）；AI改进建议卡（improvement_suggestions，LLM/统计双形态）；重要事件时间线（event_timeline，type 彩色 badge）。
**验证**：两页所有卡片有数或有降级说明；执行流水与富途 App 对得上。

---

## P2.4-S15 里程碑与二期总验收

- [ ] `npm run build` + 后端质量门全绿；一期全部测试仍绿
- [ ] 无 LLM 配置：9 张稿对应页面全部可用（规则/统计降级，页面标注生成方式）
- [ ] 无 Futu（关 OpenD）：各页降级提示正确，无白屏无假数
- [ ] 逐页对照 `docs/AlphaPilot-AI-UI-16x9/` 走查并截图存 `docs/phase2/screenshots/`（9 张）
- [ ] `docs/PHASE2_DESIGN.md` §9 验收清单逐项勾选
- [ ] 更新 CHANGELOG.md（0.3.0）与 README API 表

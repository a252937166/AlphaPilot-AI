# P2.1 数据底座 — 实施规格

> 前置：通读 `00_EXECUTION_GUIDE.md`。本里程碑不改前端（S5 除外可顺手验证），全部为后端。
> 目标：全市场证券主档与日线仓库、盘中全市场快照聚合、事件日历、事件总线、任务调度与审计。

---

## P2.1-S1 迁移机制 + 调度器骨架 + job_runs ✅ done 2026-07-21

**目标**：后续所有步骤的基建——给已存在的 SQLite 表安全加列的迁移器、APScheduler 进程内调度、任务审计表。

**新增文件**
- `src/alphapilot/db/migrate.py`
- `src/alphapilot/jobs/__init__.py`
- `src/alphapilot/jobs/registry.py`
- `src/alphapilot/jobs/scheduler.py`

**依赖**：`pyproject.toml` dependencies 增加 `"APScheduler>=3.10,<4"`，然后 `.venv/bin/pip install -e ".[dev]"`。

**models.py 增加**：

```python
class JobRun(Base):
    __tablename__ = "job_runs"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    job_name: Mapped[str] = mapped_column(String(64), index=True)
    started_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow)
    finished_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    status: Mapped[str] = mapped_column(String(16), default="running")  # running/ok/failed
    stats: Mapped[dict[str, Any]] = mapped_column(JSON, default=dict)
    error: Mapped[str | None] = mapped_column(Text)
```

**migrate.py 契约**（幂等，SQLite/PG 双兼容）：

```python
def ensure_column(engine: Engine, table: str, column: str, ddl_type: str) -> bool:
    """列不存在则 ALTER TABLE ADD COLUMN，返回是否新增。SQLite 用 PRAGMA table_info 检查。"""

def run_migrations(engine: Engine) -> list[str]:
    """集中登记所有列级迁移（后续步骤往 MIGRATIONS 列表里加三元组），返回执行过的项。"""
```
`db/engine.init_db()` 中 `create_all` 之后调用 `run_migrations(engine)`。

**jobs/registry.py 契约**：

```python
@dataclass
class JobSpec:
    name: str
    func: Callable[[], dict[str, Any]]   # 返回统计信息，写入 job_runs.stats
    trigger: CronTrigger | IntervalTrigger
    enabled_key: str | None = None       # settings 布尔开关名，None=跟随总开关

def run_job(name: str) -> JobRun:
    """手动执行一个已注册 job：开 session、写 running 行、执行、回填 ok/failed。所有 job 必须经此入口。"""
JOBS: dict[str, JobSpec] = {}
def register(spec: JobSpec) -> None: ...
```

**scheduler.py**：`start_scheduler()` / `shutdown_scheduler()`，在 `main.py` lifespan 中调用；仅当 `settings.scheduler_enabled` 为真才启动。BackgroundScheduler，`max_instances=1`、`coalesce=True`（错过的任务合并，不追赶）。

**config.py 增加**：`scheduler_enabled: bool = False`（`.env` 里本机设 true；测试环境永远 false——conftest 已设 provider=mock，无需额外处理，但 scheduler 默认 false 保证测试不受影响）。

**API**：`GET /v1/jobs/runs?limit=50`（新文件 `api/routes/jobs.py`，返回最近任务审计，时间走 iso_utc）；`POST /v1/jobs/{name}/run` 手动触发（同步执行并返回该次 JobRun 摘要——供每一步验证用）。注册进 `main.py`。

> ⚠ DEVIATION: 本 Session 开工时，Git `main` 仍停在 v0.1，而规格所述的 v0.2
> 代码、测试与二期文档已存在于未提交工作树。为避免后续步骤的提交依赖不可复现的
> 工作树状态，P2.1-S1 提交会一并固化其运行所必需的现有 v0.2 项目基线；不会纳入
> `data/`、OpenD 安装包、密钥或其他运行时文件，也未提前实现任何后续 P2 步骤。

**验证**
```bash
.venv/bin/ruff check src tests && .venv/bin/mypy src/alphapilot && .venv/bin/python -m pytest -q
# 重启后端后：
curl -s localhost:8000/v1/jobs/runs | head -c 300        # {"runs":[]}
```
**完成标准**：质量门过；`ensure_column` 有单测（临时 SQLite 里对已建表加列两次，第二次返回 False）。

---

## P2.1-S2 证券主档扩展与全市场清单 ✅ done 2026-07-21

**目标**：`securities` 表覆盖全 A（≈5300 行），含行业、ST、市值、PE 等，供筛选与涨跌停判定。

> ⚠ DEVIATION: 2026-07-21 实测 AKShare 1.18.64 返回 5529 只，其中 328 只为
> `920xxx` 北交所新代码；原规格只把 `4/8` 开头识别为北交所，会漏掉这批证券。
> 实现已把 `920xxx` 同样归为北交所。首次 BaoStock 登录还返回“网络接收错误”，
> 因此为共享惰性登录增加 3 次有界重试。最终真实重跑恢复正常，取回 5200 条
> 行业映射，600519 为 `C15酒、饮料和精制茶制造业`，且连续两次执行均为幂等更新。

**迁移**（走 S1 的 MIGRATIONS）：securities 加列
`industry_csrc TEXT / industry_futu TEXT / is_st BOOLEAN DEFAULT 0 / list_status TEXT / market_cap FLOAT / float_cap FLOAT / pe_ttm FLOAT / pb FLOAT / turnover_rate FLOAT / snapshot_at DATETIME`。

**新增** `src/alphapilot/jobs/universe.py`：

```python
def sync_universe() -> dict[str, Any]:
    """1) AKShare stock_info_a_code_name 拉全A(code,name)（已验证 5529 行）；
       失败则备源 BaoStock query_all_stock(day=最近交易日)（tradestatus 过滤）。
    2) BaoStock query_stock_industry() 回填 industry_csrc（已验证 5534 行, code 形如 sh.600000 → 去前缀）。
    3) is_st = 名称含 'ST'；board 按代码段：688→科创,300/301→创业,8/4开头→北证,其余主板。
    4) upsert securities（存在则更新 name/industry/is_st，不覆盖巨潮已填的 profile 字段）。
    返回 {"total": n, "inserted": x, "updated": y, "st_count": z}"""
```
注册 JobSpec：cron 每日 08:30。市值/PE 等快照字段由 S4 轮询顺带回填（本步不做）。

**最近交易日工具**：`services/market_data.py` 增加 `latest_trade_date(session) -> date`（取 daily_bars 中 SH.000001 最大 trade_date；无数据则今天往前找非周末）。

**验证**
```bash
curl -s -X POST localhost:8000/v1/jobs/sync_universe/run | python3 -m json.tool
# stats.total ≥ 5200
.venv/bin/python -c "
import sqlite3; c=sqlite3.connect('data/alphapilot.db')
print(c.execute('select count(*), sum(is_st) from securities').fetchone())
print(c.execute(\"select symbol,name,industry_csrc,board from securities where symbol='600519'\").fetchone())"
```
**完成标准**：全A行数≥5200；600519 行业/board 正确；重复执行幂等（updated 而非 inserted）。

---

## P2.1-S3 全市场日线仓库（夜间增量，可断点续传）

**目标**：`daily_bars` 覆盖全 A 近 300 交易日，且每日增量 10 分钟内完成。

**新增** `src/alphapilot/jobs/daily_bars.py`：

```python
def sync_daily_bars(lookback_days: int = 450, batch_size: int = 200) -> dict[str, Any]:
    """遍历 securities 全部 symbol：
    - 查该 symbol 在 daily_bars 的 max(trade_date)，start = max+1（无记录则 today-lookback）
    - start > 最近交易日 → skip（已最新）
    - BaoStockMarketDataProvider().get_daily_bars(symbol, start, end) → save_bars(...)（复用现有函数）
    - 单只失败记入 failures 列表继续，连续失败 20 只则中止并 failed
    - 每处理 batch_size 只 commit 一次；stats 含 {done, skipped, failed[:20], rows_inserted}
    速率预期：BaoStock ~5 req/s（一期实测），全量冷启动 ~20 分钟，增量 <5 分钟。"""
```
注册：cron 交易日 17:30（周一至周五；节假日空跑无害——增量为 0）。
**注意**：全程只用 BaoStock（守则坑#3：富途历史有 7 日额度；东财不可靠）。BaoStock 调用天然被全局锁串行化，不要开线程池。

**验证**
```bash
curl -s -X POST 'localhost:8000/v1/jobs/sync_daily_bars/run' | python3 -m json.tool   # 首跑较久，可先临时把 securities 截取 50 只验证逻辑，再全量
.venv/bin/python -c "
import sqlite3; c=sqlite3.connect('data/alphapilot.db')
print('symbols covered:', c.execute('select count(distinct symbol) from daily_bars').fetchone())
print('rows:', c.execute('select count(*) from daily_bars').fetchone())"
# 二次执行：stats.rows_inserted 应接近 0（幂等）
```
**完成标准**：覆盖 symbol 数 ≥ securities 的 97%；二跑幂等；job_runs 有完整统计。

---

## P2.1-S4 全市场快照轮询与大盘聚合

**目标**：盘中每 60s 一份全市场统计（宽度/涨跌停/炸板/成交额），并回填主档市值 PE。

**models.py 增加**：

```python
class MarketSnapshotAgg(Base):
    __tablename__ = "market_snapshot_agg"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    ts: Mapped[datetime] = mapped_column(DateTime(timezone=True), index=True)
    advancers: Mapped[int]; decliners: Mapped[int]; unchanged: Mapped[int]
    limit_up: Mapped[int]; limit_down: Mapped[int]; broken_boards: Mapped[int]
    up_gt4: Mapped[int]; down_gt4: Mapped[int]
    total_amount: Mapped[float]                      # 全市场成交额(元)
    avg_change_pct: Mapped[float]; median_change_pct: Mapped[float]
    source: Mapped[str] = mapped_column(String(16), default="futu")
```
（Mapped[int]/[float] 简写按现有 models.py 风格补全 mapped_column。）

**新增** `src/alphapilot/jobs/market_poll.py`：

```python
def poll_market_snapshot() -> dict[str, Any]:
    """1) codes = securities 全部 symbol → 富途码；分批 400 只调 quote_call_raw('get_market_snapshot')
       （≈14 请求；富途配额 60/30s，安全。批间 sleep 0.2s）
    2) change_pct = last_price/prev_close_price-1（守则坑#1）
    3) 涨跌停：按 board/is_st 得 limit 幅度（坑#10），|chg| ≥ limit-0.2% 判涨/跌停
    4) 炸板与涨停确认交叉源：AKShare stock_zt_pool_em(date=今天)（已验证可用），
       东财失败时仅用富途阈值法并 stats 标 'zt_source':'futu-threshold'
    5) 写 market_snapshot_agg 一行；同时把 total_market_val/pe_ttm/turnover_rate 回填 securities（每轮回填，代价可忽略）
    6) 非交易时段（09:25-15:05 之外，按本地 Asia/Shanghai 判断）直接 skip 返回 {'skipped':'closed'}"""
```
注册：interval 60s，`enabled_key="market_poll_enabled"`（config 新增，默认 False；本机 .env 设 true）。

**API**（`api/routes/market.py` 增加）：
- `GET /v1/market/breadth-full` → 最新一行 agg + 昨日同时刻 `total_amount` 环比（`amount_delta_pct`），字段名与 07 稿对应；无数据时 404 带说明。
- 保留现有 `/v1/market/breadth`（抽样版）作为降级。

**验证**（盘中执行；收盘后手动 run 会返回 skipped，可临时传 `?force=true` 查询参数绕过时段判断——实现该参数）
```bash
curl -s -X POST 'localhost:8000/v1/jobs/poll_market_snapshot/run?force=true' | python3 -m json.tool
curl -s localhost:8000/v1/market/breadth-full | python3 -m json.tool
# advancers+decliners+unchanged ≈ securities 总数(±停牌); limit_up 与东财涨停池同量级
```
**完成标准**：一轮 <15s；agg 行完整；PE/市值回填 600519 非空；非交易时段 skip。

---

## P2.1-S5 指数分时（RT）

**目标**：07 稿"大盘分时走势"。

- `futu/client.py` 无需改（`get_rt_data` 已在白名单，需先 `subscribe` RT_DATA）。
- `services/market_data.py` 增加：
  ```python
  def index_intraday(client: FutuClient, symbols: list[str]) -> dict[str, list[dict[str, Any]]]:
      """subscribe([富途码], ['RT_DATA'], is_first_push=False) 后逐只 get_rt_data；
      返回 {symbol: [{time, price, avg_price, volume}]}；订阅一次后不退订（复用；5 指数占 5 个订阅额度）。"""
  ```
- API：`GET /v1/market/intraday?symbols=SH.000001,SZ.399001`。Futu 不可用 → 503 中文说明。
**验证**：盘中 curl 返回 ≥200 个点/指数；收盘后返回当日全天分时。
**完成标准**：质量门 + 盘中冒烟通过；订阅计数（`query_subscription`）确认 ≤ 配额。

---

## P2.1-S6 板块成份全量缓存与板块资金流

**目标**：行业板块从"10 个抽样"升级为全量（~90 个），成份落库；板块级资金流日更。

**models.py 增加**：

```python
class SectorConstituent(Base):
    __tablename__ = "sector_constituents"
    __table_args__ = (UniqueConstraint("plate_code", "symbol", name="uq_sector_member"),)
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    plate_code: Mapped[str] = mapped_column(String(24), index=True)
    plate_name: Mapped[str] = mapped_column(String(64))
    symbol: Mapped[str] = mapped_column(String(24), index=True)
    name: Mapped[str | None] = mapped_column(String(64))
    refreshed_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow)

class SectorFlowDaily(Base):
    __tablename__ = "sector_flow_daily"
    __table_args__ = (UniqueConstraint("plate_code", "trade_date", name="uq_sector_flow"),)
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    plate_code: Mapped[str] = mapped_column(String(24), index=True)
    trade_date: Mapped[date] = mapped_column(Date)
    net_inflow: Mapped[float | None] = mapped_column(Float)   # 元；正=流入
    main_inflow: Mapped[float | None] = mapped_column(Float)
    source: Mapped[str] = mapped_column(String(16))
```

**新增** `src/alphapilot/jobs/sectors_sync.py`：
- `sync_sector_constituents()`：`get_plate_list('SH','INDUSTRY')` 全部板块 → 逐板块 `get_plate_stock`。**限频：每请求后 `time.sleep(3.2)`**（10 次/30s 配额，坑#3），~90 板块一次 5 分钟；cron 每周日 09:00 + 首次手动。完成后**改造 `services/sectors.py`**：`_load_plate_constituents` 优先读 DB（`refreshed_at` 7 天内），无数据才走一期的在线小抽样（保留降级）。
- `sync_sector_flows()`：主源=对每板块成份快照的 `net_inflow` 字段求和——**先探测**：取一次快照打印列名，若含资金字段（`net_inflow`/`capital_flow`…）直接聚合；若无 → 对每板块市值前 5 成份调 `get_capital_flow`（限频 sleep）聚合为近似值，`source='futu-top5'`；东财 `stock_sector_fund_flow_rank` 仅 try 一次成功才用 `source='em'`。cron 交易日 15:20。
  > ⚠ 快照字段以实测为准，把探测到的可用字段名写进本文件 DEVIATION 记录。

**板块页升级**：`get_sector_strength` 的 MAX_PLATES 提到全量（成份来自 DB，抽样上限每板块 30 只按成交额排序取前 30——需要快照后排序），单轮快照仍 ≤400 只×N 批（90 板块×30=2700 只 → 7 批，配额内）。

**验证**
```bash
curl -s -X POST localhost:8000/v1/jobs/sync_sector_constituents/run | python3 -m json.tool   # plates≈90, members>2500
curl -s 'localhost:8000/v1/sectors/strength?refresh=true' | python3 -c "import json,sys; d=json.load(sys.stdin); print(len(d['sectors']))"   # ≈90
curl -s -X POST localhost:8000/v1/jobs/sync_sector_flows/run | python3 -m json.tool
```
**完成标准**：板块数≈90；强度接口耗时 <20s；flow 表当日有行且 source 标注真实来源。

---

## P2.1-S7 事件日历

**models.py 增加**：

```python
class CalendarEvent(Base):
    __tablename__ = "calendar_events"
    __table_args__ = (UniqueConstraint("symbol", "event_type", "event_date", "title", name="uq_calendar"),)
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    symbol: Mapped[str] = mapped_column(String(24), index=True)
    event_type: Mapped[str] = mapped_column(String(24))   # dividend/unlock/earnings_report/earnings_preview
    event_date: Mapped[date] = mapped_column(Date, index=True)
    title: Mapped[str] = mapped_column(Text)
    payload: Mapped[dict[str, Any]] = mapped_column(JSON, default=dict)
    source: Mapped[str] = mapped_column(String(24))
    available_time: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow)
```

**新增** `jobs/calendar_sync.py` → `sync_calendar(symbols: list[str] | None = None)`：默认只同步**自选股**（全市场日历量大且非必需）：
- 分红：BaoStock `query_dividend_data(code, year)`（近 2 年）→ dividend；
- 解禁：AKShare `stock_restricted_release_queue_em`（**先 `help()` 确认当前版本参数签名**，按 symbol 过滤）→ unlock；失败跳过记 warning；
- 披露日期：BaoStock `query_forecast_report` / cninfo 公告标题含"业绩预告|年度报告|季度报告" → earnings_preview/earnings_report。
cron 每日 07:30。

**API**：`GET /v1/stocks/{symbol}/calendar?days=90` → 按 event_date 升序，含 event_type 供前端 tabs 过滤。

**验证**：`curl -s -X POST .../sync_calendar/run`；`curl -s localhost:8000/v1/stocks/600519/calendar | python3 -m json.tool`（茅台应有分红记录）。
**完成标准**：自选 5 只均有 ≥1 类事件；唯一约束防重复（二跑 inserted=0）。

---

## P2.1-S8 事件总线

**models.py 增加**：

```python
class DomainEvent(Base):
    __tablename__ = "events"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    symbol: Mapped[str | None] = mapped_column(String(24), index=True)   # None=市场级
    event_type: Mapped[str] = mapped_column(String(32), index=True)
    # disclosure/calendar/thesis_shift/capital_anomaly/market_regime_change
    direction: Mapped[float] = mapped_column(Float, default=0.0)          # -1..1
    strength: Mapped[float] = mapped_column(Float, default=0.5)           # 0..1
    title: Mapped[str] = mapped_column(Text)
    summary: Mapped[str | None] = mapped_column(Text)
    source_ref: Mapped[str | None] = mapped_column(String(128))           # 如 disclosure:123
    occurred_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), index=True)
    ingested_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow)
```

**新增** `services/events.py`：`emit(session, *, symbol, event_type, title, ...) -> DomainEvent`（内部去重：同 source_ref 已存在则跳过）。
**接线（本步只接现成生产者）**：
1. `services/disclosures.sync_disclosures` 每插入一条公告 → emit(disclosure, direction=0)；
2. S4 轮询检测自选股 `|chg|≥7%` 或 换手率异常 → emit(capital_anomaly)；
3. regime 变化：dashboard 聚合时对比上次 regime（存 `daily_reports` 或简单表）→ emit(market_regime_change)。

**API**：`GET /v1/events?symbol=&types=&limit=50`。
**验证**：同步一次茅台公告后 `curl /v1/events?symbol=600519` 出现 disclosure 事件；重复同步不重复 emit。

---

## P2.1-S9 跨市场信号

**新增** `services/cross_market.py` + `GET /v1/market/cross`：

```json
{"fx_usdcny": {"value": 7.18, "change_pct": -0.04, "as_of": "...", "source": "akshare-boc"},
 "us_futures": {"name": "标普500期指", "last": 5293.0, "change_pct": 0.24, "source": "futu"},
 "commodities": {"name": "CRB/南华指数", "last": null, "note": "东财源不可用", "source": null},
 "northbound": {"note": "盘中数据官方已停发，仅日度余额", "daily_balance": null}}
```
- 美期：富途快照（探测可用合约码如 `US.ESmain`，写 DEVIATION 记录实际码）；
- 汇率：AKShare 央行中间价接口（非东财域，先验证），失败→BaoStock 宏观；
- 每字段独立 try/except，不可用返回 note（守则 §4-4：不摆假数）。

**验证**：curl 返回结构完整、至少 fx 或 us_futures 一项有真值。

---

## P2.1-S10 里程碑验收

依次执行并全部通过：
```bash
.venv/bin/ruff check src tests && .venv/bin/mypy src/alphapilot && .venv/bin/python -m pytest -q
curl -s localhost:8000/v1/jobs/runs | python3 -c "import json,sys; rs=json.load(sys.stdin)['runs']; print(sorted({r['job_name'] for r in rs}))"
# 期望包含: sync_universe, sync_daily_bars, poll_market_snapshot, sync_sector_constituents, sync_sector_flows, sync_calendar
```
- [ ] securities ≥5200 行含行业/ST/市值
- [ ] daily_bars 覆盖 ≥97% 证券
- [ ] breadth-full 盘中每分钟一行，含涨跌停/成交额环比
- [ ] 板块强度覆盖 ~90 行业、来源 DB 缓存
- [ ] 自选股日历+事件总线有数据
- [ ] 调度器随 API 启停，job_runs 完整审计
- [ ] 全程未触碰安全不变量（grep 确认无 unlock_trade / REAL 相关改动）

# P3-M3 历史因子数据回填与完整模型重检验 — 实施规格（实现者必读）

> **起点（为什么做这一步）**：P3-M1/M2 判定 `composite` 无效，但架构师复核（`phase3/02` 末尾，
> 2026-07-24）发现那是**伪结论**——实测 13 因子里只有 4 个价量因子有历史截面（n_periods 12-13），
> 其余 9 个基本面/估值/板块因子 `n_periods=0`、历史数据几乎为空（`financial_indicators` 仅 1 只股
> 35 行、`sector_flow_daily` 仅 1 天、估值只有当前快照）。**M1/M2 实际只回测了一个残缺的 4 价量
> 因子模型,而价量因子在 A 股散户市本就偏反转,弱负 IC 属正常。真正的基本面 alpha 因子从未被检验。**
> 同时 M2 抱怨的 |t|<2 不显著,根因是只有 ~1 年样本、统计效力弱。
>
> **M3 的任务**：把历史因子输入底表回填到**多年**深度(数据源已全部实测可行,详见 §0.1),让**完整
> 因子模型第一次在有统计效力的样本上被检验**——回答那个一直没真正回答的问题:**这套因子,尤其
> 是基本面因子,到底有没有可交易的 alpha?**
>
> **承接**：`phase2/00_EXECUTION_GUIDE.md` 全部通用守则 + `phase3/01` 全部回测铁律(未来函数、复权、
> 成交错开、涨跌停、成本、基准、可复现)+ `phase3/02` 全部防过拟合铁律(样本外、不 p-hack、样本量
> 诚实、符号翻转要有经济学理由、去冗余、可以结论"仍无效")**统统仍然适用**。本文件只增加**回填特有
> 的铁律**与 S1…S9 实现级步骤。按编号顺序做,每步跑完"验证"才进下一步,做完标 `✅ done`、独立
> commit、偏差写 `⚠ DEVIATION`。

---

## 0.1 数据源可行性(已实测,不是假设)

M3 动手前,架构师已亲手验证三源都能回填,记录在此供 Codex 信任:

| 腿 | 数据源 | 实测结果 |
|---|---|---|
| 财务(5 因子) | BaoStock `query_profit/growth/cash_flow/balance_data` | 茅台 2015Q1 ROE 可拉,**带 `pubDate` 披露日**(2015-04-21)=正确 available_time |
| 估值(2 因子) | BaoStock `query_history_k_data_plus` peTTM/pbMRQ/psTTM | 逐日历史,**实测回溯到 2016-01**(茅台 2016-01-05 peTTM=16.6) |
| 板块资金流(1 因子) | 富途 `get_capital_flow(period_type=DAY)` | 一年 242 天日频,主力/超大/大/中/小单齐全,**硬上限约一年**(2025-07 起) |

**范围决策(用户已拍板:打法 B · 多年回测)**：财务/估值回填到多年,对长历史因子跑多年回测
(第一次有统计效力检验基本面 alpha);板块资金流因子受富途一年深度限制,单独在一年窗口测。

**两处诚实修正（2026-07-25 架构裁定）**：
- `sector_strength` 读的是 `SectorSnapshot.payload` 的实时衍生板块强度，历史补不了，列为
  **live-only**。
- `net_inflow_5d` 的一年资金流使用当前 top5 固定篮子，缺少历史板块成分 PIT。不得回拨
  `refreshed_at`，也不得用当前成分伪造历史映射，因此列为
  **`history_excluded_pit_gap` / live-forward**，退出 S7 历史 IC 与 S9 v3 权重候选。

真正进入 M3 历史回测的是 **11 个因子**：4 价量 + 5 财务 + 2 估值。板块成分从裁定日起按日
固化 PIT 快照，`net_inflow_5d` 只在未来自然形成的诚实窗口中恢复可测。

> **⚠ B 的成败点(审查已核实源码,写在最前面):`weights_rebuild.py` 的研究/切分窗口写死"只取最近
> **301 个交易日**"(`_RESEARCH_SESSION_COUNT=301`,`train_test_split` 在 `len!=301` 时直接抛错)。**
> **若 S7/S9 沿用 `train_test_split` 切 train/test,回填再多年也只看最近 ~1.2 年 → 复现 M2 弱样本、B 白做。**
> 好在 `all_factors_ic`/`rebuild_weights` 本身认显式区间(`_calendar(start,end)`,已核实),所以**关键动作是:
> S7/S9 自己按多年区间算 train/test 边界、全程传显式日期,绝不调 `train_test_split`**。详见 S7/S9 的 ⚠ 段。

---

## 0.2 M3 回填铁律(违反 = 回测结果作废或悄悄偷看未来,逐条对照)

回填最大的风险不是灌不进数据,是**灌进了带未来函数/来源不被审计的数据,让回测虚高**。

1. **available_time 决定一切(未来函数零容忍,承接 M1 铁律#1)。** 回填的每一行都必须带**决策时点
   之前真实可见**的 `available_time`:
   - 财务:`available_time = pubDate + 1 天`(上海零点→UTC),缺 pubDate 才退 `statDate + 45 天`。
     **这套 `financials.py::_availability`(:178-193)已实现,直接复用,不许改口径。**
   - 估值:peTTM/pbMRQ 是**当日盘后可得**,`available_time = trade_date 当日 15:00 上海 → UTC`
     (与日线同口径)。绝不允许把 trade_date T 的估值标成 T 之前可见。
   - 消费端(`engines/factors.py::_load_financial_factors` :343)只取 `available_time ≤ decision_cutoff`
     的最新期——回填口径错了,回测就偷看未来,比灌不进更危险。

2. **来源必须在审计白名单内,否则回测直接忽略(load-bearing)。** 回测的 `_prices_for_date` /
   `_trading_calendar` / `eligible_universe` 全部 `WHERE DailyBar.source IN AUDITED_DAILY_BAR_SOURCES`
   (`provenance.py:3` = `{akshare, baostock, futu, futu-close, sina}`)。
   - 回填 daily_bars 的 `source` 必须 ∈ 这 5 个(BaoStock 走 `"baostock"`)。
   - 板块资金流新增 source(建议 `"futu-daily"`)**必须同步加进** `AUDITED_SECTOR_FLOW_SOURCES`
     (`provenance.py:6`),否则审计层拒绝。
   - 估值新表若有审计集合,同样登记。

3. **复权因子必须与日线同区间同步(承接 M1 铁律#3)。** 回测 engine 用 `DailyBar` **inner join**
   `AdjFactor`(`engine.py:_prices_for_date` :193),**缺复权因子的历史日线会被整行丢弃**。所以 S1
   扩 `daily_bars` 后**必须**用 `sync_adj_factors` 把 `adj_factors` 补到同一起始日,否则多年日线白扩。

4. **幂等 + 断点续传,回填可随时中断重跑。** registry 的 `_job_lock` 只防同名并发,**不防重复回填同
   一区间**。每个回填函数自己保证幂等:先查库已存 → 只补缺 → upsert。照 `sync_financials`
   (按 `(symbol,report_period,metric)` 跳过)/ `sync_daily_bars`(按 per-symbol max trade_date 增量)。

5. **写对库(CWD 坑)。** DB 是相对路径 `sqlite:///data/alphapilot.db`(`config.py:26`),按**进程 CWD**
   解析。回填程序**必须以仓库根为 CWD**、复用 `from alphapilot.db.engine import get_session`
   (共享同一 Settings/引擎),否则会在错目录建空库(根目录那个 0 字节 `alphapilot.db` 就是这么来的)。
   SQLite 写一律走"独立短事务 + `_save_bars_with_lock_retry` 式退避",压短写锁。

6. **覆盖率与缺口如实标注,不得宣称完整。** 回填必然有缺口:停牌/退市/次新股/北交所/数据源缺项。
   每张表回填完,job 的 `stats` 必须报**覆盖率、缺失清单、available_time 口径占比**。幸存者偏差
   (当前 `securities` 不含退市股)**仍然存在,继续显著标注**(承接 M1 铁律#7)。`sector_strength`
   历史补不了,报告里写明 live-only；`net_inflow_5d` 标为 `history_excluded_pit_gap`，不进入
   factor_ic_stats 或 v3 权重候选。

7. **回填与重跑解耦,权重不动直到样本外验证。** 严格分两段:先补数据 + 重跑 **M1 单因子 IC**
   (多年窗,看每个因子逐个有没有 alpha)→ 再谈 **M2 重构**(train 窗定权重、test 窗验证)。
   任何"补一点数据 → 看结果不好 → 回头调回填口径/因子定义/权重直到好看"都是 p-hacking(违反
   M2 铁律#2/#6)。**M3 期间绝不开 paper_auto、不动 live/unlock 三闸。**

8. **回测不读 `factor_values` 表(澄清,防走错路)。** M1 回测与 M1 单因子 IC 都走 `backtest/pit.py`
   对每个历史决策日**现场重算**因子(`compute_factors_for_date` + PIT 过滤),**不读 `factor_values`
   缓存表**。所以 M3 关键路径**回填的是输入底表**(daily_bars/adj_factors/financial_indicators/估值/
   sector_flow),**不是 factor_values**。回填 factor_values 只为 live 展示(S8,次要、可选)。

---

## 1. 目标与范围

**M3 交付**：历史因子输入回填 + 因子引擎估值改造 + 运行时入口补全 + 完整模型多年重检验 + 诚实结论。

具体:
- **S1** daily_bars + adj_factors 向前扩到多年(B 的样本长度前提)。
- **S2** financial_indicators 全市场、多年深度回填(复用现成 job,加固规模)。
- **S3** 新建历史估值表 `valuation_daily` + BaoStock 多年回填 + 盘后增量 job。
- **S4** 因子引擎改造:`pe_percentile/pb_percentile` 改读历史估值表(PIT),不再读当前快照。
- **S5** 板块资金流一年回填(富途 DAY 口径)。
- **S6** 回填后一次性数据体检(覆盖率/PIT/缺口报告)。
- **S7** M1 重跑:多年单因子 IC(新增运行时入口),逐因子定性有没有 alpha。
- **S8**(可选/live)历史 factor_values 重算入口。
- **S9** M2 重构:多年 train 窗 IC_IR 加权 → `composite-v3`,test 窗验证,诚实判断转正与否 + 安全复核。

**M3 不做**(留 M4+):ML / 滚动 walk-forward 训练(且只有 M3 证明因子确有 alpha 才值得做)、
`sector_strength` 历史(补不了)、退市股历史(幸存者偏差仍在、标注)、组合优化、实盘/自动模拟盘。
**M3 只回答:补齐历史、在多年样本上检验 PIT 有效的 11 因子模型,基本面因子到底有没有 alpha。**

---

## 2. 代码放置

```text
src/alphapilot/db/models.py                   新增 class ValuationDaily(Base)（S3）
src/alphapilot/db/migrate.py                  如需给 financial_indicators 加索引/列（S2 加固）
src/alphapilot/data/baostock_provider.py      新增 get_valuation_history()（S3，拉 peTTM/pbMRQ/psTTM）
src/alphapilot/jobs/valuation_sync.py         新增 backfill_valuation() + sync_valuation_daily()（S3）
src/alphapilot/jobs/sector_flow_backfill.py   新增 backfill_sector_flows()（S5，富途 DAY 口径）
src/alphapilot/jobs/factor_research_job.py    新增 run_factor_research()（S7，串 IC+corr+可选 rebuild）
src/alphapilot/engines/factors.py             改造 pe_percentile/pb_percentile 读 valuation_daily（S4）
src/alphapilot/jobs/factors.py                （S8 可选）compute_factors 加历史 trade_date 入口
src/alphapilot/jobs/__init__.py               注册所有新 job（register_builtin_jobs）
config/factor_weights_v3.yaml                 S9 M2 重构产出（多年 train 窗，version v3.0.0）
docs/phase3/03_P3.3_historical_factor_backfill.md   本文件
tests/test_phase3_valuation_backfill.py       S3/S4 测试
tests/test_phase3_sector_flow_backfill.py     S5 测试
tests/test_phase3_m3_rerun.py                 S7/S9 重跑测试
```

**框架约定(照抄,别自创)**:
- 本项目**无 CLI 框架**(无 Typer/argparse),`cli.py` 只有 `run_api()`。所有回填一律写成 **Job**:
  `def backfill_xxx(...) -> dict[str, Any]`(返回 stats)+ `register(JobSpec(name=..., func=..., trigger=None))`
  + 在 `jobs/__init__.py::register_builtin_jobs` 注册。**范式照抄 `jobs/event_backfill.py`**
  (`backfill_events` :41 + `register_event_backfill_job` :111,`trigger=None`=只手动不排程)。
- **带参数触发**:`POST /v1/jobs/{name}/run` 只对 `poll_market_snapshot` 透传 `force`,**不接受
  业务 kwargs**。所以带 `quarters/start_date/symbols` 的回填,要么在 job 函数内写死默认+读 env,
  要么在 `tests/` 里直接调函数(现有 `sync_financials`/`sync_daily_bars` 测试即如此)。**推荐**:
  回填 job 函数签名带默认参数,冷启动用一次性测试/脚本直接调(`from alphapilot.jobs.xxx import
  backfill_xxx; backfill_xxx(start_date=..., ...)`),日常增量走无参 job + scheduler。
- 审计自动接入:走 `run_job` 就有 `JobRun`(`job_runs` 表)running→ok/failed + `stats` + 同名串行锁;
  中途失败用 `raise JobExecutionError(msg, stats=partial)` 保留部分进度。
- 限频:无全局限流器,靠 `time.sleep(pause_seconds)`。BaoStock 靠 provider 全局锁串行(无需额外
  sleep);富途沿用 `CAPITAL_FLOW_PAUSE_SECONDS = 1.05` 量级。
- 新表:在 `models.py` 加 `class + __tablename__`,建表由 `init_db`(`engine.py:64`)的 `create_all`
  完成;**加列/加索引**走 `migrate.py` 的 `MIGRATIONS`/`INDEX_MIGRATIONS` 清单(`create_all` 不改
  已存在表)。

---

## P3.3-S1 daily_bars + adj_factors 向前扩展到多年 ✅ done 2026-07-24

**目标**：把回测可用区间从当前 ~2024-03 起扩到**多年起点**,给足统计效力。目标起始日设 `2019-01-01`
(约 7.5 年、~1800 交易日,远超 M2 的 301 日);若 Tushare 复权因子额度扛不住,允许收窄到 `2021-01-01`
(仍 ~5×M2 样本)并在报告标注。**这是 B 的前提:没有多年日线+复权,财务/估值因子的多年 IC 无从算起。**

**做法**:
1. **daily_bars 扩历史**：`jobs/daily_bars.py::sync_daily_bars` 当前冷启动只回溯 `lookback_days=450`
   (`:237`)。给它加一个可选 `start_date: date | None = None`(或直接调大 `lookback_days`),使
   `start = start_date`(冷启动)覆盖到 2019-01;全市场跑。写入 `source="baostock"`(北交所走 Sina)。
   复用 `_save_bars_with_lock_retry`(:119)短事务退避、连续 20 失败熔断。
2. **adj_factors 同区间补齐(铁律#3,必须)**：`jobs/backtest_jobs.py::sync_adj_factors_job`(:55)
   → `backtest/adjust.py::sync_adj_factors`(:373),Tushare `adj_factor` 接口(低积分账号该接口**有权限**,
   凭据 `.env` 的 `ALPHAPILOT_TUSHARE_TOKEN`)。扩到与 daily_bars 相同起始日。**这是 S1 最重的一步**
   (5200 股 × 7 年,Tushare 限频),**先跑、留足时间**;跑完前不要进 S7。

**验证**(架构师会独立抽查):
- `daily_bars` 覆盖:`SELECT MIN(trade_date), COUNT(DISTINCT trade_date), COUNT(DISTINCT symbol) FROM daily_bars WHERE source IN (...)` → 最早日 ≤ 目标起始、审计源占比 100%。
- `adj_factors` 同区间齐全:随机抽 20 只股,`adj_factors` 的日期集合覆盖其 `daily_bars` 日期集合
  (否则该股历史日线会被 inner join 丢弃)。
- 覆盖率报告:每年有效股票数、缺口(次新/停牌)清单。
- 幂等:重跑 job 应 `rows_inserted≈0`、大量 `skipped`。

**实现与验收记录（2026-07-24）**：

- `sync_daily_bars(start_date=...)` 已支持向前补历史；成功或确认上市前无数据后，把
  `daily_bars_backfill_start` 水位合并写入 `Security.profile`，5530 只均记录到
  `2019-01-01`。水位只用于断点续传，不替代真实 K 线、不填补休市/上市前日期。
- 全量日线回填处理 5530 只，新增 `6,820,873` 行；目标证券的审计日线现有
  `8,458,363` 行、1833 个交易日，范围 `2019-01-02..2026-07-23`。2019～2026 各年有效证券数
  依次为 `3604/4001/4494/4921/5234/5334/5450/5530`；历史新增区间的不可审计来源行数为 0。
- 全量复权回填新增同样的 `6,820,873` 行，失败 0；目标 5530 只的日线逐行 left join
  `adj_factors` 缺口为 **0 / 8,458,363**。来源为 `baostock-hfq` 5201 只、
  `sina-hfq` 329 只；600519 在新旧数据边界的复权单日收益为 `+0.035%`，没有标尺跳变。
- 固定随机种子 `20260724` 抽 20 只（含北交所、科创板、次新），20/20 的日线与复权日期集合
  完全相等，`missing_adj=0` 且 `extra_adj=0`。次新/上市前无历史均按真实首个交易日保留，
  没有补零；`920685` 自 2026-07-15 后无新 K 线，实时快照价格/成交量为 0，作为停牌态缺口保留。
- 幂等空跑：日线 `10.18s`、`rows_inserted=0`、`skipped=5529`（仅 `920685` 的停牌后增量
  仍诚实报空）；复权 `11.48s`、`rows_inserted=0`、`rows_updated=0`、`skipped=5530`、
  `failed_count=0`、`coverage=1.0`。
- 写锁路径已补离线回归：日线与复权均在原 provider 结果上用新事务退避 3 次，不重新拉行情，
  SQLite lock 不计入数据源熔断。全量质量门通过：ruff 0、strict mypy 130 个源文件 0、
  pytest 612 项全绿。
- 安全复核：`trading_mode=research`、`paper_auto=false`、`live=false`，
  `unlock_trade` 仍在永久屏蔽集合；回填前后 `trade_proposals=1`、`broker_orders=1`，
  无新增提案/委托/交易。API 与 OpenD 行情/交易登录均健康。

> ⚠ DEVIATION: P3.1 已实测并记录本机低积分 Tushare `adj_factor` 为每小时 1 次，且当前
> 5530 只既有复权历史已全部固定为 `baostock-hfq` / `sina-hfq`。为遵守“同一证券不得跨源
> 拼接绝对标尺”，本次多年回填沿用各证券既有 provider 来源，而不是再切回 Tushare；最终做到
> 2019 起逐行 100% 覆盖，因此无需启用规格允许的 2021 退路。执行 shell 的环境代理会使 Sina
> 出现 `SSL WRONG_VERSION_NUMBER`，真实回填进程显式移除 `HTTP(S)_PROXY/ALL_PROXY` 后直连，
> 未改白名单、未伪装来源。

> ⚠ DEVIATION: 完整质量门跨日暴露既有自动模拟盘测试的审计时间未贯穿执行器：
> `execute_proposal(now=...)` 内两次来源提醒复核仍读取真实墙钟。现统一传递显式 `now`，
> 只消除时间注入泄漏，未放宽提醒有效期、SIMULATE 隔离或任何交易门禁；专项 7 项及全量测试均绿。

---

## P3.3-S2 财务全市场、多年深度回填 ✅ done 2026-07-31

**目标**：`financial_indicators` 从"1 只股 35 行"→ **全市场、多年**。5 个财务因子(roe/net_profit_yoy/
ocf_to_profit/debt_ratio/revenue_yoy)的历史 PIT 值就位。

**做法**：**复用现成 job,不重写**。`jobs/financials.py::sync_financials(quarters=8, *, symbols=None,
batch_size=25)`(:319)已实现:BaoStock 四表取数、5 metric 映射(:21-29)、`_availability` 披露日 PIT
(:178-193)、按 `(symbol,report_period,metric)` 断点续传、连续 20 失败熔断。
- 冷启动:`sync_financials(quarters=40)`(=回溯 ~10 年),`symbols=None`(全市场)。
- **规模加固(交给 Codex 判断是否需要)**:5200 只股跨 10 年,注意
  (a) 连续失败熔断阈值 20 是否会被"整段停牌/退市/次新无财报"的股触发误熔断——若是,改成"仅当
  *网络/接口* 连续失败才熔断,数据缺失记 skip 不计入熔断";
  (b) 北交所已按 `_supported_symbol`(:315)跳过,确认无误;
  (c) 单只 upsert 后立即 commit+expunge(:458-493)保持,别攒大事务。
- **不改 metric 口径**:比率是小数(roeAvg=0.1057 即 10.57%,不再除 100),这是 `phase2/02` 已定的
  DEVIATION,S2 不许动。

**验证**:
- 覆盖:`SELECT COUNT(DISTINCT symbol) FROM financial_indicators` → 接近全市场(扣除次新/北交所);
  `SELECT metric, COUNT(*) FROM financial_indicators GROUP BY metric` → 5 个 metric 各有大量行。
- PIT 口径:`payload.available_time_basis='provider_pub_date_end_of_day'` 的占比应**压倒性高**
  (statDate+45 兜底只应占极少);随机抽 5 只股对拍 BaoStock `query_profit_data` 的 pubDate。
- 幂等:重跑大量 `symbols_skipped`。
- `job_runs` 里 `sync_financials` 有一条大规模 `status=ok`、stats `symbols_with_data` ≈ 全市场。

> ⚠ DEVIATION: 2026-07-24 冷启动实测确认 BaoStock 财务接口没有按季度批量拉取全市场的
> 入口，只能按“股票 × 季度 × 四张财务表”逐项查询；单连接抽样外推全市场 40 季度约需
> 14.7 小时。曾用 16 个互斥股票分片做规模探测，但数据源连续返回 `10002007`/broken pipe，
> 随后登录明确返回 `10001011 黑名单用户`。该次父 `JobRun id=10294` 已如实标 `failed`
> （`BrokenProcessPool`），成功写入的断点保留；截至阻断时为 125 只、23,780 行。为遵守数据源
> 访问约束，已撤掉多连接方案，冷启动恢复后改为单连接长任务；同时补上 transport error
> 一次重登、黑名单立即失败（不重复撞源）、单股短写事务及 SQLite 锁退避。当前 S2 **未完成、
> 未提交，S3 继续冻结**；待数据源解除限制后全量续跑并补齐本节全部验证。
> BaoStock 官网黑名单统计对本机出口 `222.211.229.98` 显示值 `7`；该列标题是“日访问总次数/
> 并发连接数”，结合 16 分片实况，`7` 表示服务端检测到的并发连接数而非“仅请求 7 次即超
> 50,000 次日限额”，因此根因定性为违反禁止并发规则。
>
> 2026-07-24 15:30 CST 冷却前最后一次单股审计探测使用 `600519`（40 季度已有 39 个完整
> 季度，仅缺 `2026Q2`），未启动并发或后台全量任务。`JobRun id=10435` 在首次登录后即
> `failed`，`error=DataProviderError: BaoStock login failed (10001011): 黑名单用户，请与管理员联系`；
> 证明根因已直接保留而非再包装成 `BrokenProcessPool`/20 连败熔断。按访问保护纪律停止探测，
> 未创建 S2 launchd 长任务。
>
> ⚠ DEVIATION: BaoStock 官网当前访问统计规则明确为“每日 API 请求不超过 5 万次、禁止并发
> 连接”，所以此前“单连接连续约 14.7 小时跑完”的方案仍不足以证明合规。实现新增
> `max_provider_requests` 日分片闸门：每处理一个季度前按最坏 5 次 provider 查询预留额度，
> 达到预算前干净停止并返回 `is_complete=false`、`resume_symbol`，下一次继续依靠既有
> `(symbol, report_period, metric)` 幂等键跳过；独立冷启动入口默认每日最多 `40,000` 次，
> 给登录、异常重连及估算误差保留至少 20% 余量。经用户授权，在隔离的阿里云 ECS 上仅做一次
> 匿名登录探测，返回 `error_code=0`；这证明本机 `10001011` 是出口 IP 维度而非个人账号维度，
> 但不改变限额纪律。远端只部署 S2 runner 与精简续跑库（5530 只证券、125 只已有财务数据、
> 23,780 行），运行态固定为 `research`、Kill Switch 开启、Futu/自动模拟盘/实盘/账户写入全部
> 关闭；systemd 只允许一个实例，runner 外层另加 `flock --nonblock` 文件锁防止重复启动，
> 拟每日 00:30（Asia/Shanghai）续跑一个 40,000 次分片。
> S2 仍 **未完成、未提交**，S3 继续冻结。
>
> 远端首次启动的 `JobRun id=1` 在连接 BaoStock **之前**因该机 SQLite 变量上限较低而失败：
> 全市场 5530 个 symbol 被一次性放进 `IN (...)`，触发 `too many SQL variables`，未消耗
> provider 请求额度。断点预载随即改为按单只证券查询，避免参数上限且将长期内存占用限制为
> 单只股票规模；离线回归主动把 SQLite 参数上限降至 999，并以 1005 只证券验证通过。
> 2026-07-24 15:50:13 CST 启动修复后的 `JobRun id=2`：systemd 仅 1 个 runner 进程，
> 系统仅 1 条到 `public-api.baostock.com:10030` 的 ESTABLISHED 连接；首只证券完成后远端库由
> 125 只/23,780 行推进到 126 只/23,975 行，证明断点实际落库而非仅进程存活。下一定时触发为
> 2026-07-25 00:31 CST；当前分片仍在运行，最终覆盖率与 PIT 验证待全量完成。
>
> ⚠ DEVIATION: 阿里云仅是避开本机出口黑名单的采集跳板，远端 staging DB 增长不算本项目
> 数据交付。已补齐回传闭环：远端对运行中的 SQLite 使用 online backup 生成原子一致性快照，
> manifest 携带 SHA-256/行数/五 metric/PIT 口径/最新 JobRun；本机 SCP 到同目录临时文件，
> 哈希一致后原子替换快照，再以 `busy_timeout=15000`、`BEGIN IMMEDIATE` 单事务只插入本地
> `financial_indicators` 缺失复合键。同键内容不一致时拒绝覆盖并报告样本；导入前后强制核对
> `trade_proposals` / `broker_orders`，绝不整库替换。
>
> 2026-07-24 首轮回传已真实完成：本机从 125 只/23,780 行增至 **175 只/33,530 行**，
> 五个 metric 各 6,706 行，`provider_pub_date_end_of_day` 为 33,530/33,530，冲突 0，
> `PRAGMA quick_check=ok`；提案/委托始终为 1/1。LaunchAgent
> `com.alphapilot.s2-financial-pull` 已安装，RunAtLoad 全链路 `exit=0`，此后每日 04:10
> 自动执行“远端在线快照 → 校验回传 → 本地幂等合并”；该回传不调用 BaoStock，不影响远端
> 每日 40,000 次/单连接额度。S2 仍须等待接近全市场覆盖和最终 PIT/幂等验证后才能标 done。
>
> ETA 复核时发现冷回填对完全空的季度（当前尚未披露的 `2026Q2`、上市前无数据期）原本不落
> 任何断点，后续每日会重复查询，无法满足幂等验收，后期甚至可能耗尽当日额度而不再前进。
> 已增加仅供 cold runner 使用的 `financial_no_data_periods` 负向断点：真实查询为空后写入
> `Security.profile`，后续冷分片跳过；常规周度 `sync_financials` 默认不使用该断点，财报后来
> 发布时仍会正常补数，不制造 `financial_indicators` 占位行。离线测试验证第一次查询并记断点、
> 第二次 provider 调用为 0，profile 既有字段保留；ruff、strict mypy、财务/BaoStock/回传专项
> 均绿。按 5201 只支持证券、每只约 164 次、每日 40,000 次估算需 21–22 个额度日：无额外封禁/
> 停机时预计 **2026-08-14** 完成采集，预留数据源失败重试后保守窗口为 **2026-08-15..18**；
> 随后执行最终本地回传、覆盖率/PIT/幂等质量门并交架构师签字，签字通过才标 S2 done。
>
> ⚠ DEVIATION: 2026-07-24 用户确认管理员允许两个独立公网 IP 同时采集后，曾按硬边界准备
> “阿里云 `<300000`、本机 `>=300000`”双出口方案。实现增加 `symbol_min` /
> `symbol_max_exclusive` 参数及 JobRun stats，离线测试证明 `300000` 边界无重叠；BaoStock
> provider 增加跨进程 `flock`，第二个本机进程会在 `login()` 前失败，所有 JobSpec 结束后
> 主动 logout/释放锁。登录失败、黑名单和本机锁冲突均提升为全局致命错误，不再乘以 20 只
> 连败阈值。远端 2026-07-24 首个额度分片 `JobRun id=2` 已自然完成：
> `status=ok`、`provider_requests_estimated=39996`、失败 0、远端库达到 71,350 行/366 只；
> 未追加请求，也未重启当日任务。
>
> 本机双出口实测未通过，不能把“两个 IP”当成已投产。Mac 当前网络由 Trojan-Qt5 PAC
> 提供 SOCKS5，原始 TCP 不读取系统 PAC；首次直连只在登录前出现 TCP 超时，`JobRun
> id=10597` 被停止后如实补记 failed（同时把 API 重启遗留的 stale `id=10564` 标为
> interrupted failed，stats 保持空，不伪造进度）。随后实现仅允许 loopback PAC、启动时
> 动态发现 SOCKS5 端口的显式隧道；代理公网出口 `204.77.128.32` 与阿里云
> `47.93.234.51` 独立，但真实 `JobRun id=10646` 在第一只股票、三次封顶登录内仍返回
> `DataProviderError: BaoStock login failed after 3 attempts (10002007): 网络接收错误。`，
> 未进入任何季度查询，本地仍为 33,530 行/175 只，提案/委托仍为 1/1。
>
> 为保护访问权，本机 `com.alphapilot.s2-financial-local` 已 bootout，不会在次日自动重试；
> 阿里云配置已恢复为全市场断点续跑（不再限制 `<300000`），仍保持单进程/单连接/每日
> 40,000 次，下一次为 2026-07-25 00:33 CST。只有本机出口先通过一次真实登录后，才允许
> 同一变更窗口把远端切回 `<300000` 并启用本机 `>=300000`，避免重叠采集。当前 ETA 因此
> 仍按单出口 **2026-08-14**、保守 **2026-08-15..18**；S2 未 done、未提交，S3 继续冻结。
> 本轮代码门：全仓 Ruff、strict mypy（131 files）、BaoStock/财务/JobRun/回传/本机 runner
> 专项 67 tests 全绿；未把专项测试表述为全量 pytest。

> **▶ S2 加速前置（架构师蒸馏，2026-07-24；未来想用第二出口把 S2 从 ~3 周提速时逐条对照）:**
> 上方 DEVIATION 已证:① 管理员已确认两 IP、② 跨进程 `flock` 已锁本机自并发。但浮现**第三道墙——
> 本机 Mac 不是可用的 BaoStock 出口**（本机网络经 Trojan-Qt5 PAC/SOCKS5，BaoStock 走 raw-TCP 不读
> 系统 PAC；即便显式隧道 `204.77.128.32` 仍 `10002007 网络接收错误`）。故**加速需同时满足三条，缺一
> 即维持阿里云单出口（ETA 2026-08-14，保守 08-15..18）**:
> 1. 管理员确认两个公网 IP 各 1 连接、各 ≤4 万次/日（IP 变了重新确认）；
> 2. 第二出口能**直连 raw-TCP 到 BaoStock 且 `login` 成功**——本机当前做不到，需修好本机网络路径，
>    或换一台能直连的云 VM 作第二出口；
> 3. 硬隔离:证券区间不重叠、每出口单进程/单连接/≤4 万次、黑名单错误立即致命不自动重试、跨进程
>    `flock` 常驻。

> **▶▶ S2 三出口加速方案（架构师 2026-07-25 亲手实测后签发；覆盖上方"第三道墙"的悲观结论）**
>
> **一、实测事实（推翻两个旧结论，供 Codex 信任）**
> 1. **本机 Mac 其实可用,旧结论作废。** 实测:出口 IP `222.209.151.130`（成都电信，**注意已非当初被封的
>    `222.211.229.98`，家宽动态 IP 已换**）；`scutil --proxy` 显示 HTTP/SOCKS 均 disabled（仅 PAC）、
>    默认路由 `en0→192.168.0.1` **未被 utun 劫持** → **raw-TCP 直连不经代理**；`bs.login()` **0.61s 成功**。
>    之前判"本机不可用"的根因是**测试脚本 stdout 缓冲**导致误判为卡死（需 `python -u`），**不是网络、
>    不是封禁、不需要隧道**。上方第三道墙那条**不再成立**。
> 2. **狗云 `206.237.18.80`（美东）可用。** DNS→114.94.20.42、TCP 10030 通、`bs.login()` **0.33–0.51s
>    成功**；`baostock` 已装（`pip3 install --user`，py3.6）。**美东 IP 也能登 BaoStock。**
> 3. **`108.181.252.227` 暂不列入**:SSH 需经本机 trojan(51837)，实测 `Connection closed`，够不着。
> 4. **⚠ 新发现:BaoStock 有夜间维护窗口（约 02:00 CST 起）。** 实测 02:00 后**三台机器（阿里云/本机/
>    狗云）全部 login 秒过、但所有查询接口 25s+ 超时**（`query_profit_data`/`query_history_k_data_plus`/
>    `query_all_stock` 无一例外）；而阿里云 `JobRun 3` 在 **00:33–01:53 正常跑满 40k 额度**（604 股 /
>    105,285 行）。→ **是服务端窗口，非 IP/代理/并发问题。定时任务须避开该窗口，且必须有"query 探测
>    失败就退出、不空耗额度"的保护。**
>
> **二、三出口硬分片（按 symbol 数值，互斥不重叠；阿里云现按 symbol 升序推进至 ~002077）**
> | 出口 | 分片 | 该片总数 | 剩余（估） |
> |---|---|---|---|
> | 阿里云 47.93.234.51（北京，已在跑） | `symbol < 300387` | 1843 | ~1240 |
> | 狗云 206.237.18.80（美东，新增） | `300387 <= symbol < 601121` | 1843 | 1843 |
> | 本机（成都，新增） | `symbol >= 601121` | 1845 | 1845 |
>
> 复用既有 `symbol_min` / `symbol_max_exclusive` 参数（S2 已实现、离线测过 300000 边界无重叠）。
> 三出口 × 40k/日 ≈ 732 股/日 → **剩余 ~4,927 股约 7 天，ETA 从 08-14 提前到 ~08-01~04。**
>
> **三、每出口铁律（缺一不可）**
> - **1 进程 / 1 BaoStock TCP 连接 / ≤40,000 请求/日**；跨进程 `flock` 常驻（已实现，保留）。
> - **启动前 query 探测门**:先单只真实查询，`query` 成功才继续；**探测失败（如撞上夜间窗口）立即退出、
>   不消耗额度、不重试**。黑名单 `10001011` 立即致命、绝不自动重试。
> - **本机专属**:① **不走任何代理**（直连即可，已验证；`env -u ALL_PROXY/http_proxy/...`，禁止再造隧道）；
>   ② 跑采集期间**必须关闭本机 scheduler 的 BaoStock cron**（`ALPHAPILOT_SCHEDULER_ENABLED=false`，
>   或确保 `flock` 同时罩住 in-process scheduler）——否则本机自开第二连接触发违规；③ 日志/脚本用
>   `python -u`，避免缓冲导致"看起来卡死"的误判重演。
> - **狗云专属**:py3.6 + `--user` 装的 baostock；用 `nohup setsid` 分离 + 日志落盘；采集结果需新增回传
>   链路（照阿里云 `financial_pull` 那套:远端快照 → SHA-256 校验 → 幂等只补缺合并进 `data/alphapilot.db`）。
> - **前置**:**三个 IP 同时采集已超出此前"两个 IP"的确认范围，须先向 BaoStock 管理员补确认**
>   （"三个公网 IP 各 1 连接、各 ≤4 万次/日 是否允许"）。未确认前只跑已确认的出口。
>
> **四、纪律不变**:S2 仍不标 done，直到全市场覆盖 + 覆盖率/PIT/幂等验证 + 架构师签字；三闸
> （live/paper_auto/unlock）全程 false。
>
> **五、⚠ 本机 flock 争用（架构师 2026-07-25 实测发现，必须处置）**：本机同时承担 ① S2 分片 C
> 采集（长任务，占 BaoStock 单连接 + 跨进程 `flock`）与 ② 日常 live 同步 `sync_daily_bars`(18:40)/
> `sync_valuation_daily`(18:50)。**两者抢同一把 flock，长任务会饿死日常同步 → 选股链持续停更。**
> 处置二选一:(a) **把本机 S2 分片挪给狗云/阿里云，本机专职跑 live 产品链**（推荐——本机是唯一有
> 完整 API+前端的机器）；(b) 把本机 S2 运行窗口排到 19:00 之后（日常同步完成之后）。

---

> **三出口投产记录（Codex，2026-07-25）**
>
> - 前置许可已由用户代表 BaoStock 管理员确认“符合”：三个公网 IP 各 1 个进程/1 条连接、
>   各不超过 40,000 次/日。投产窗口再次实测出口为阿里云 `47.93.234.51`、狗云
>   `206.237.18.80`、本机 `222.209.151.130`，与获准范围一致。
> - provider 现按**实际财务查询调用数**计数并在第 40,000 次后硬阻断，不再只依赖作业层最坏值估算；
>   三个 runner 均在额度内先执行一次不重试的真实 `query_profit_data` 探测，探测失败直接令 JobRun
>   failed，黑名单仍立即致命。探测本身计入 40,000 次上限。离线测试覆盖探测单次调用、探测失败根因/
>   stats、探测占用额度与 provider 硬上限。
> - 阿里云 runner 已固定为 `symbol < 300387`，systemd timer active，下一次为
>   `2026-07-26 00:06:49 CST`；投产时无遗留 BaoStock 连接。上一额度日 `JobRun id=3`
>   `status=ok`，39,999 次估算请求、失败 0、远端 105,285 行/604 股，作为三分片共同种子。
> - 狗云 runner 已固定为 `300387 <= symbol < 601121`，`2026-07-25 18:19:37 CST`
>   启动 `JobRun id=4`。启动后仅 1 个容器、1 条到 `114.94.20.42:10030` 的连接；至本记录时
>   该分片已真实新增到 3,705 行/19 股，JobRun 仍 running，未出现 query/黑名单错误。
> - 本机 runner 已固定为 `symbol >= 601121`，显式清除全部 HTTP(S)/SOCKS/ALL_PROXY 环境、
>   `python -u`，并令进程内 scheduler/财务 cron 为 false；host `flock` 继续与 API 共用。
>   LaunchAgent `com.alphapilot.s2-financial-local` 于 `2026-07-25 18:19:01 CST` 启动
>   `JobRun id=14844`，仅 1 条直连 BaoStock 的连接。至本记录时本地该分片为 5,135 行/29 股
>   （含种子既有 1 股），且继续增长。
> - 狗云回传闭环已投产：远端 online-backup → SHA-256 → SCP 压缩传输 → 本地校验后幂等补缺；
>   首次手动回传 SHA 一致并插入 1,560 行，随后 LaunchAgent
>   `com.alphapilot.s2-financial-pull-dog` RunAtLoad 再插入 390 行、冲突 0、`exit=0`，
>   `trade_proposals` / `broker_orders` 始终 1/1，此后每日 04:25 自动回传。首次裸 SCP 在
>   5.1 MiB 处停止增长，已安全终止临时传输并启用 SSH 压缩；重跑 30 余秒完成，远端/本地
>   SHA-256 完全一致，不把卡住的半文件当成功。
> - 当前本地合并库三个互斥区间分别为：阿里云段 105,090 行/603 股、狗云段 2,145 行/11 股、
>   本机段 5,135 行/29 股；复合键重复组为 0，PIT 口径为
>   `provider_pub_date_end_of_day=112,358`、`stat_date_plus_45_days=12`。上述仅是运行中证据，
>   **不是覆盖率验收，S2 仍不标 done、不提交**。
> - 投产后质量门：全仓 Ruff、strict mypy（133 source files）、财务/provider/本机 runner/回传
>   专项 47 tests、shell/plist 语法及 `git diff --check` 全绿。专项测试已改用测试独占的临时
>   BaoStock 锁路径，因而能在真实 runner 正确持有生产锁时执行；没有为跑测试释放生产锁或创建
>   第二条数据源连接，且未把专项结果表述为全量 pytest。
>
> ⚠ DEVIATION: 狗云宿主机只有 Python 3.6，而当前 AlphaPilot 项目要求 Python >=3.11，不能直接
> 用宿主 `python3.6 --user` 执行完整 runner。为保持同一份已测代码和安全门，改用隔离的
> Python 3.11 非 root Docker 容器（只挂载 staging DB、只读根文件系统、cap-drop/no-new-privileges、
> CPU/内存/PID 上限），BaoStock 出口、单连接、40,000 次上限、分片和 `flock` 契约均未改变。
> 快照导出容器最初把 WAL 数据目录只读挂载，SQLite 无法创建读协调所需的 `-shm/-wal` 而失败；
> 改为仅允许 SQLite 目录协调写、仍只执行 online backup 后导出成功，源业务表不作逻辑修改。
>
> ⚠ DEVIATION（2026-07-25 18:59）：按“五、本机 flock 争用”选择方案 (a)，本机不再承担 S2
> 分片 C，专职运行 live 产品链。`com.alphapilot.s2-financial-local` 已 bootout，plist recoverably
> 移至 Application Support 的 disabled 文件，安装器在本机 scheduler 开启时会拒绝重新启用；
> `JobRun 14844` 如实标为 interrupted failed、`stats={}`。停止前 C 段已取得 28,820 行/159 股，
> 通过 online snapshot + SHA-256 校验幂等合入狗云 staging，冲突 0、重复键 0；合入后远端快照
> 为 160,175 行/897 股。狗云当日仍只有原 B 段任务的 1 条 BaoStock 连接，导入过程不访问
> BaoStock；未来 runner 边界调整为 `symbol >= 300387`，接管 B+C。本机确认无 S2 LaunchAgent、
> 无 TCP 10030 连接，`sync_daily_bars`/`sync_valuation_daily` 不再被长任务饿死。
>
> ⚠ DEVIATION（2026-07-26 09:26 CST，Task 1 第三出口正式投产）：本机 C 分片已按架构裁定
> 迁至新 LAX 出口，本机继续专职 live 产品链。三段最终边界为阿里云 `symbol < 300387`、
> 狗云 `300387 <= symbol < 603182`、LAX `symbol >= 603182`；证券全集分别为
> 1,843 / 2,172 / 1,516 只，边界互斥且并集覆盖 5,531 只。变更窗口先手工回传阿里云与狗云，
> 本地确认 268,750 行/1,504 股、重复键 0、财务 PIT 异常 0、提案/委托 1/1；随后狗云 runner
> 由无上界原子收紧到 `<603182`，旧文件留有可恢复备份且当时无在跑进程或 BaoStock 连接。
>
> - LAX 只执行了一次独立外部探测：`2026-07-26 09:09:56 CST`，一次 login 与一次
>   `query_profit_data(sh.600519, 2024Q4)` 均返回 `error_code=0`、1 行；没有重试。
>   为使“外部探测 1 次 + runner 内部探测/采集”在首日仍严格不超过 40,000 次，LAX runner
>   的持久上限设为 **39,999**，不是临时手工豁免。
> - 新鲜 staging seed 由两路回传后的本地库在线导出，SHA-256 为
>   `d58c7d9c69a4ef72e173a6ce9623d0f7f3da689e1e01a3b6f5381a3001a975d3`：
>   268,750 行/1,504 股、5 个 metric 各 53,750 行，5,531 份 `Security.profile`
>   （含 1,499 股/6,363 季负向断点），重复键 0、PIT 异常 0、`quick_check=ok`，且 seed
>   不带 JobRun/提案/委托业务行。远端落库哈希与本地完全一致，只传主 DB 文件。
> - CentOS 7 无系统 Python 3。按规格采用官方 glibc 2.17 兼容的
>   python-build-standalone 3.11.15；首次无约束安装尝试因最新版 `greenlet` 无 CentOS 7
>   二进制轮子、且主机无 C++ 编译器而失败，未启动 runner、未连接 BaoStock。随后固定使用
>   `numpy=1.26.4`、`pandas=2.2.3`、`greenlet=3.1.1` 的 manylinux_2_17 轮子，项目与
>   BaoStock/SQLAlchemy/Pydantic/APScheduler 均成功导入。该兼容性处置不改变数据或请求语义。
> - LAX 运行态固定 `research`、scheduler/行情轮询/Futu/账户写入/实盘/自动模拟盘全关、
>   `trading_halted=true`，代理环境全部清空；systemd 219 用 `MemoryLimit=300M`、
>   `CPUQuota=50%`、`Nice=10`、专用非 root 用户和双层 `flock`。oneshot service 保持
>   `static`（不能 enable），只有 `00:10 Asia/Shanghai` timer 为 enabled/active，下一次
>   `2026-07-27 00:10:22 CST`。
> - 手工首跑于 `2026-07-26 09:22:31 CST` 启动：仅一个 Python runner、仅一条到
>   BaoStock `:10030` 的连接，`JobRun id=1 status=running`，无 error；09:24 时 C 段已
>   新增 795 行/5 股，远端总量 269,545 行/1,509 股，重复键仍为 0，cgroup 实占约 87 MB。
> - 本机 `com.alphapilot.s2-financial-pull-lax` 已安装为 RunAtLoad + 每日 04:40；首轮
>   online-backup/SHA-256/SCP/幂等合并 `exit=0`、仅 1 次传输，快照
>   SHA-256 `59887a56abc853e3148482af014cc5db71a1db0bf2b7faa1cb6b5e061097f3cd`，
>   插入 275 行、冲突 0，本地达到 269,025 行/1,506 股，重复键与 PIT 异常均为 0，
>   `trade_proposals` / `broker_orders` 合并前后始终 1/1。
> - LAX 上原生产 `sing-box` 仍 active/enabled 且 PID 2745、启动时间和 unit 哈希未变；
>   证书续期 timer 仍 active/enabled 且 unit 哈希未变。防火墙逐条语义复核仍只放行
>   80/443/49291 后丢弃其他新入站；`iptables-save` 的包计数会随流量变化，因此不把原始文本
>   哈希当配置一致性证据。本机 API 健康且未重启，交易态仍
>   `research / live=false / paper_auto=false`，历史唯一委托仍为 `SIMULATE`。
> - 投产后质量门：全仓 `ruff check .`、strict `mypy src/alphapilot`（138 source files）、
>   全量 pytest、财务/LAX/上海时区门控专项 74 tests、shell/zsh/plist 语法与 `git diff --check`
>   均以退出码 0 通过；没有停止真实 runner 或建立第二条 BaoStock 连接来跑测试。
> - ⚠ OPS OBSERVATION：LAX 未安装 active 的 ntpd/chronyd/systemd-timesyncd，仅
>   qemu-guest-agent active；投产复核时远端 epoch 落在本机 SSH 往返的 3 秒窗口内，当前时钟准确，
>   00:10 距 02:00 维护窗也有充分余量。未擅改该生产 VPN 机的时间服务，后续持续观察 timer 漂移。
> - ⚠ DEVIATION（本机 launchd 时区修复）：投产后独立复核发现 Mac 系统时区为
>   `America/Los_Angeles`，原 `StartCalendarInterval Hour=4` 会按本机 04:xx 触发，而非文档约定的
>   Asia/Shanghai 04:xx。三路回传已统一改为 DST 安全的上海日门控：
>   launchd 在各自分钟（10/25/40）每小时唤醒，`run_shanghai_daily.py` 仅在上海时间达到
>   04:10/04:25/04:40 且当天尚未成功时执行；成功日期以 0600 权限原子落盘，睡眠错过目标时刻会在
>   下一小时补跑。迁移当天阿里云/狗云手工回传和 LAX 首轮回传均已有 exit 0 实证，因此按真实成功
>   日期初始化状态；重新 bootstrap 后三任务均 RunAtLoad `exit=0` 且没有重复传输。该修复只影响
>   本机 snapshot 回传调度，不访问 BaoStock、不改变三台采集 timer。
>
> 以上只证明第三出口安全投产和回传闭环，不是 S2 全市场覆盖验收。按当前 1,504 股基线及
> 三出口各自剩余量，ETA 维持 **2026-07-31..2026-08-02**（瓶颈为狗云 B 段）；S2
> **不标 done、不提交**，完成后仍须做全量覆盖率/PIT/幂等终验并交架构师签字。
>
> ⚠ DEVIATION（边界变更后 JobRun 证据待自然形成）：狗云最新 `JobRun id=5` 是收紧边界前
> 已跑完的额度任务，stats 诚实保留 `symbol_min=300387, symbol_max_exclusive=null`；
> 当日已使用 39,998 次，不能为制造一条新 stats 再连 BaoStock。收紧后的当前证据为 runner
> SHA-256、参数、修改时间以及无在跑连接；`symbol_max_exclusive=603182` 的新 JobRun stats
> 须等 2026-07-27 00:05 自然额度分片产生后补核，不伪造、不越额。
>
> ⚠ DEVIATION（2026-07-26 23:58 CST，旧 LAX 故障后四出口重平衡）：用户代表 BaoStock
> 管理员明确确认新的活跃出口集合为 `47.93.234.51`、`206.237.18.80`、
> `38.150.35.34`、`192.220.61.208`，各 1 进程/1 连接/每日不超过 40,000 次；
> `192.220.61.180` 从许可集合退出。旧 LAX 在切换窗口已 `connection refused`，无法远程
> disable timer；其最后可用库已先用 SQLite online backup 救回本机，快照 SHA-256
> `8ca22134f83c4210a699d7d02ec7db84b8ae18923e3705caa62962c038f6fb85`，
> 305,850 行/1,751 股，`JobRun id=1 status=ok`、断点 `resume_symbol=603607`。
> 旧实例不得再次恢复运行；若云厂商侧重新开机，须先确认其 S2 timer 已禁用。
>
> - 按本机真实“5 metric 已齐或负向断点”状态重算未完成工作，四段原子调整为：
>   阿里云 `symbol < 300387`；狗云 `300387 <= symbol < 600235`；
>   US38 `600235 <= symbol < 603730`；US192 `603730 <= symbol < 1000000`。
>   四段无交集、无缺口，分别剩约 39,742 / 29,264 / 34,544 / 34,520 个季度查询单元。
>   狗云旧 cron 在无进程、无 `:10030` 连接时先可恢复移出 `/etc/cron.d`，runner 原子收紧并
>   通过 SHA-256/参数复核后才恢复 cron；没有产生旧、新边界并发。
> - 两个新 IP 各只执行一次 `bs.login()` + 一次
>   `query_profit_data(sh.600519, 2024Q4)`：均 `error_code=0`、1 行、logout 成功；
>   无重试、无黑名单。当天独立探测计 1 次，因此两个 runner 持久上限均为 39,999。
> - 统一 seed SHA-256
>   `5d98a9cfb80db92c2f0b24f9d9fc622c63f7d1ff87041d81350d247503baed7b`：
>   305,850 行/1,751 股、五指标各 61,170 行、5,531 份 Security.profile、
>   1,746 股/8,800 季负向断点，PIT 缺失 0、重复键 0、`quick_check=ok`，且
>   JobRun/提案/委托均为 0。两台落库和首次导出哈希完全一致。
> - 两台均为 CentOS Stream 9 原生 Python 3.11，未使用 Docker；专用非 root 用户，
>   `CPUQuota=40%`、`MemoryMax=256M`、`MemorySwapMax=384M`、`Nice=10`、
>   `ProtectSystem=strict`、`NoNewPrivileges=true`。research、scheduler、Futu、live、
>   paper、paper_auto 全关，`trading_halted=true`，代理变量与 BaoStock 专用代理变量全部清空。
>   `38.150.35.34` 于 09:05、`192.220.61.208` 于 09:15 Asia/Shanghai 启动，避开约 02:00
>   维护窗；投产时只有 timer active/enabled，oneshot service inactive、BaoStock 连接数 0。
> - 两台原 VPN 服务均未改：sing-box active/enabled 且 unit SHA-256 与投产前一致；
>   US38 证书 timer active，US192 WARP 与 certbot timer active，防火墙入站规则未新增。
>   US38 原配置显式关闭公钥认证，采用独立 SSH drop-in 开启并经 `sshd -t` + reload 验证，
>   密码认证回退、端口和防火墙未改；随后全程只用已部署密钥。
> - 本机新增 `com.alphapilot.s2-financial-pull-us38`（12:05 上海）与
>   `com.alphapilot.s2-financial-pull-us192`（12:20 上海）。首轮 online backup →
>   SHA-256 → SCP → 幂等合并均 exit 0；incoming 305,850、inserted 0、conflict 0，
>   本机提案/委托前后仍 1/1。旧 `com.alphapilot.s2-financial-pull-lax` 已 bootout，
>   plist 可恢复移入 Application Support 的 retired 目录，避免对故障旧机持续失败回传。
> - 本轮质量门：全仓 `ruff check .`、strict mypy（139 files）、
>   `.venv/bin/python -m pytest` 全量、四出口专项 32 tests、shell/zsh/plist 语法和
>   `git diff --check` 均通过。直接执行 `.venv/bin/pytest` 会因其 console-script
>   `sys.path` 不含仓库根而使两个现有跨包测试 collection 失败；使用 Python 模块入口后
>   768 项全部通过，这是测试入口差异，不是产品失败。
>
> 以上只证明四出口部署与回传闭环，S2 仍不标 done、不提交；
>   预计剩余约 3.5–4 个额度日，最终仍须全市场覆盖/PIT/幂等/外部对拍和架构师签字。
>
> ⚠ DEVIATION（2026-07-27 00:08 CST，四出口首轮自然运行补证）：阿里云自然产生
> `JobRun id=5 status=running`，实际进程参数保持 `symbol < 300387`，只存在 1 个
> Python worker/1 条 `:10030` 连接；日志已到
> `processed=850/1843, inserted=105, failed=0`。狗云自然产生
> `JobRun id=6 status=running`，实际进程参数已收紧为
> `300387 <= symbol < 600235`，同样只有 1 个 Python worker/1 条 `:10030` 连接；
> 日志已到 `processed=500/1225, inserted=870, failed=0`。两路均未见
> `10001011`/网络错误，四段边界切换已获得首个自然任务实证。运行中 `stats={}` 是
> runner 结束时才一次性落 stats 的既有语义；请求实数、断点与 `is_complete` 留待自然完成后
> 只读复核，不为取证追加请求。US38/US192 的首次自然运行仍分别待 09:05/09:15 CST 验证，
> 因此 S2 状态和提交纪律不变。
>
> ⚠ DEVIATION（2026-07-27，US38/US192 首跑权限 hotfix）：09:05/09:15 两个 timer
> 都在执行 runner 之前以 systemd `200/CHDIR` 失败。根因是部署器将 worker 根目录设为
> `root:root 0750`，同时把 `mktemp -d` 的 `0700` 模式原样移动成 `app`；
> `alphapilot-s2` 服务用户无法进入 `WorkingDirectory`，且 `.env root:root 0640`
> 会成为下一阻塞。失败发生在 BaoStock login/query 之前，因此两台都没有 JobRun、连接或配额消耗。
> 用户明确授权“修复并补跑”后，两台统一采用 root owner +
> `alphapilot-s2` 只读执行组，data/logs/exports 仍仅服务用户可写；
> app/.venv 普通文件和目录无组/其他用户写权限。两台都以服务用户通过
> cd/read/exec/write、核心依赖导入、seed SHA、`quick_check=ok`、
> 305,850 行/1,751 股、JobRun/提案/委托 0/0/0、分片和 39,999 上限离线门。
> 部署器移入 `scripts/deploy_financial_remote_worker.sh`，并将 timer 激活改成
> 仅 enable、禁止 `--now`，避免 `Persistent=true` 未经人工门自动补跑。
> 10:48 CST 两台各只显式启动一次，没有第二次 probe/重试；均产生
> `JobRun id=1 status=running`，各 1 worker/1 connection，边界分别为
> `600235 <= symbol < 603730`、`603730 <= symbol < 1000000`。
> 10:53 CST 数据已分别增长到 307,800 行/1,761 股、306,390 行/1,754 股，
> 未见 `10001011`/`10002007`。最终 stats 与本机回传证据仍待自然完成，
> S2 不标 done、不提交。
>
> ⚠ DEVIATION（2026-07-27，US192 物理服务器一换一替换）：
> 用户确认 `192.220.61.208:62981` 实例已消失，无法再登录旧机执行最终
> disable timer 或补取“最终”快照；不伪造该证据。旧机最后一份可用证据来自
> 12:20 Asia/Shanghai 的成功回传：远端 `JobRun id=1 status=running`，
> 322,910 行/1,845 股，SHA-256
> `249429455a3c741cec13b8724c513ded69c3f78e923ea79d0546071049753e0a`；
> 当次本机幂等合并新增 17,060 行和 348 个负向断点、冲突 0，本地达到
> 427,655 行/2,478 股，提案/委托前后均为 1/1。旧实例不得恢复其 S2 timer。
>
> 用户随后明确指定 `69.5.53.137:46531`（US-SEA-BGP-01）一换一接管逻辑
> US192，而非新增第五出口；分片继续严格保持
> `603730 <= symbol < 1000000`。新机初检为 CentOS 9、1 核、约 960 MiB、
> 20 GB 根分区，没有既有 AlphaPilot/BaoStock/VPN 负载；公钥登录经独立
> sshd drop-in 开启并完成实测。云镜像自身的 `dnf -y upgrade --refresh`
> 占用包管理锁 9 分钟且最终 `Return-Code: Success`、RPM 数据库校验通过；
> 没有强杀升级或并发写 RPM 数据库。
>
> 为避免旧 seed 倒退，先以已合并生产库生成新的隔离 seed：
> SHA-256 `09c9d39737ebc02b9c145926ce32c7d5400dee60339bf35d4651bd98d1562d60`，
> 427,655 行/2,478 股、五指标各 85,531 行、5,531 份 Security.profile、
> 2,472 股/13,523 季负向断点，重复键/PIT 缺失均 0、`quick_check=ok`，
> JobRun/提案/委托为 0/0/0。部署器已把 app/seed 路径与 SHA 从旧时间戳
> 硬编码改为显式必填参数，并以 10 项专项测试、bash 语法和 diff check 验证。
>
> 新机保持专用非 root 用户、`CPUQuota=40%`、`MemoryMax=256M`、
> `MemorySwapMax=384M`、`Nice=10`、`NoNewPrivileges=true`、
> `ProtectSystem=strict`；research、scheduler/Futu/live/paper/paper_auto
> 全关且 `trading_halted=true`。2026-07-27 13:18 后仅显式启动一次服务；
> runner 自带的单次真实探测成功，`JobRun id=1 status=running`、error 为空，
> 仅 1 worker/1 条 `:10030` 连接，无 `10001011`/`10002007`。
> 首轮写入后远端达到 427,850 行/2,479 股；online backup + SHA-256 +
> rsync 回传幂等新增 195 行和 1 个负向断点、冲突 0，本机同步达到
> 427,850 行/2,479 股，重复键/PIT 缺失 0，提案/委托仍为 1/1。
> 本机同名 `com.alphapilot.s2-financial-pull-us192` 已原子重渲染到新 IP/端口，
> 上海日门状态保留，bootstrap `exit=0` 未重复自动回传；远端 timer 已 active，
> 下一次为 2026-07-28 09:15 Asia/Shanghai。S2 仍不标 done、不提交。
>
> 命名更新（2026-07-28）：应用户要求，逻辑出口名 `us192`（沿用自已回收的
> `192.220.61.208`）更名为 `ussea`（US-SEA，`69.5.53.137` 实际位于西雅图）。
> 更名在远端空闲窗口执行（无进程、`:10030` 连接 0）：远端根目录改为
> `/opt/alphapilot-s2-ussea`，unit 更名为 `alphapilot-s2-financial-ussea.service/.timer`，
> Persistent stamp 以 `cp -p` 迁移保留上次触发记录、启动后验证未发生补跑，
> 下一次仍为 09:15 Asia/Shanghai；本机回传更名为
> `com.alphapilot.s2-financial-pull-ussea`（日门状态与快照目录
> `data/phase3-s2-ussea` 一并迁移）；`SHARD_CONTRACTS`、配置模板、runner
> 脚本与专项测试同步更名。分片边界、39,999 预算、时刻表与全部安全约束
> 逐项未变。本文此前各处的 US192 均为该出口更名前的历史名称，证据不回改。
>
> ⚠ DEVIATION（2026-07-29 16:19 CST，狗云临时救援阿里云尾段，配置切换完成、
> 待 2026-07-30 自然运行）：
> 用户批准把已完成原分片的狗云用于剩余工作重平衡。只读闭环确认阿里云尚有
> 254 股/10,128 季未解析，按最坏 5 请求/季计算后将临时边界定为阿里云
> `symbol < 300166`（55 股/2,168 季，最坏 10,841 请求）与狗云
> `300166 <= symbol < 300387`（199 股/7,960 季，最坏 39,801 请求）；
> US38/US-SEA 边界不变。两路当日额度已分别使用 39,999/38,772 次，故本变更
> 只准备次日自然任务，禁止当日探测或补跑。切换前两路均无 worker、Docker
> runner 或 `:10030` 连接；狗云 cron 已暂时移出，阿里云与狗云当前 runner/
> timer/cron 及狗云在线数据库快照均已按 SHA-256 冻结到各自
> `ops-backups/20260729-dog-rescue/`。
>
> 最新阿里云 seed（291,270 行/1,591 股，SHA-256
> `957bce4c1778cccd840bf1a0c839a61eb0f5bcca09a15be2a77709debf515f4b`）
> 经无网络 Docker 导入狗云时，宿主现有 `20260725` 镜像内的旧 importer 已安全
> 补入 185,985 行、同键冲突 0、交易表保持 0/0，但返回结果没有
> `security_profile_merge`，证明该镜像版本尚未合并
> `financial_no_data_periods`。该状态不冒充 seed 完成：cron 继续禁用，下一步仅
> 以当前已测 importer 做无网络幂等断点并集合并，并要求非断点
> `Security.profile` 哈希不变、第二次导入新增 0、JobRun 仍为 8 条后，才允许
> 进入边界切换。
>
> 上述补救已兑现：覆盖当前已测 importer 后第一次无网络导入只补
> 985 个 profile/2,291 个负向季度断点，正向 `inserted=0`、冲突 0、
> `missing_target_symbols=0`，非断点 profile SHA-256 前后均为
> `d0d94fa6102e14b3b5ece29e4d25ad2b71528687fd7c11543008491bb562a044`；
> 第二次同命令空跑为 profile/断点/正向行全部新增 0。seed SHA-256 前后均为
> `957bce4c1778cccd840bf1a0c839a61eb0f5bcca09a15be2a77709debf515f4b`，
> seed 到目标的缺键/内容冲突均为 0，目标为 495,870 行/2,975 股、重复键 0、
> PIT 必填字段异常 0、`quick_check=ok`；JobRun 仍为 8 条且最新 id=8，
> `trade_proposals/broker_orders=0/0`。
>
> 随后按“先收缩阿里云形成安全空档，再启用狗云补位”的顺序原子切换。阿里云
> runner SHA-256 从
> `d035045572044ab8da1f3cd1f12c7b21c7efe4aacc8ad39a131b1482ac622d3c`
> 变为
> `04b2282cf6420096f5e4598bd4183d64c8a9d457b060f5f28d48545e5d7dfff9`
> （`symbol < 300166`）；狗云从
> `96ff4ca2d6d377e53634fac118910f377ebd0b942202d95c4663e92c4872a6fa`
> 变为
> `13b548e36b42398c24ad7d405a6e6cf465ad2c6510693b4357a4984a1ea973a6`
> （`300166 <= symbol < 300387`）。狗云 cron 已按原 SHA-256
> `fb9acc7cbb13ebeaf5eb04cf65e2eb1d48c06bf1f592b0392510a594d8fd9a9a`
> 恢复；阿里云 timer 下一次为 2026-07-30 00:05:22 CST，狗云 cron 为
> 00:05 CST。切换后两路仍为 0 worker/0 `:10030` 连接，未手动探测、启动或
> 消耗当日额度。该临时 JobRun 仅作救援历史证据；完成后仍须回灌阿里云、
> 恢复最终四分片合同并在原合同上取得严格幂等空跑，S2 此时仍不标 done。
>
> ⚠ DEVIATION（2026-07-31，阿里云回灌事务顺序兼容性）：最终回灌预备时，
> 阿里云 Python 链接的 SQLite `3.26.0` 在“先 `ATTACH` `mode=ro` 来源快照、
> 再 `BEGIN IMMEDIATE`”的顺序下返回
> `attempt to write a readonly database`；错误发生在事务开始前，目标库未改变，
> 且已有在线备份。独立临时库复现确认旧版本会尝试把只读 attachment 纳入
> 写事务升级。导入器改为先对目标库 `BEGIN IMMEDIATE`、再挂载只读来源；
> 来源仍为 `mode=ro + query_only`，目标锁与冲突/交易安全检查不放宽。
> 专项 Ruff、strict mypy 与 transfer tests 通过后才继续真实回灌。随后元数据
> 同步门又发现旧 staging 经增量迁移后的 `securities` 物理列序与新生产库
> 不同、但 21 个字段集合完全一致；未发生写入。missing-only 导入器因此改为
> 仍要求字段集合严格相等、但按字段名重排后全字段复制，并新增错列序回归，
> 防止位置映射造成静默错列。
>
> **Codex 最终机器验收回执（2026-07-31，架构师已签认）**：
> 四出口已恢复最终互斥合同并分别取得严格幂等空跑：阿里云 JobRun `id=10`
>（`symbol<300387`）、狗云 `id=13`
>（`300387<=symbol<600235`）、US38 `id=5`
>（`600235<=symbol<603730`）、US-SEA `id=7`
>（`603730<=symbol<1000000`）。四条最新记录均为
> `status=ok/error=null/is_complete=true`、`failures=[]`、`symbols_done=0`、
> `metrics_inserted=metrics_updated=0`、`provider_probe_requests=1`、
> `provider_requests_estimated=1`、`resume_symbol=null`，并通过最终验收检查器的
> §2 全字段合同。
>
> 收尾期间 universe 发生两次真实漂移，均未冻结旧分母冒充完成：US-SEA 对新证券
> `688825` 拉得 25 行（5 个季度 × 5 指标），另 35 个季度写诚实负向断点；
> 随后今早 `sync_universe JobRun 38843` 新增 `301677`，missing-only 同步至狗云后
> 拉得 20 行（4 个季度 × 5 指标），另 36 个季度写诚实负向断点。两只股票均做到
> 40 季度正负闭环、重叠 0；每次补采后都再取得同合同严格空跑，未把接口错误当作无数据。
>
> 四路最终快照回传均 `conflicting_rows=0`，生产库由 `5201/5202` 经两次实时分母漂移
> 最终闭环为 **5203/5203**，`financial_indicators=847095`；五个 metric 各
> `169419` 行/`5203` 股。目标季度对为 `208120/208120`，`unresolved=0`、
> `partial=0`、重复键 0、正负重叠 0、PIT 异常 0、`quick_check=ok`；
> `provider_pub_date_end_of_day=846871/847095`（99.9736%）。冻结快照 SHA-256：
> 阿里云 `f6e3778d53973f4513a6200b491d0b5078288dda9aa909097135571d281e326e`，
> 狗云 `a91cbefd40a0a18cd2cd46ece5ff206abb0f8ad2b324afda5dedf594d9b9e990`，
> US38 `e13e24f3e7d9dda75b1f5e351607d93c3076c448a5c4e0644b4866c2f8a46d84`，
> US-SEA `dfe2e2fe41027b8cacf8c8dd8fa41dcf936789edfebc9897b7f71cbfddceba50`。
>
> 固定样本 `002144/688733/300739/301188/688458` 已在获准的阿里云出口仅执行
> 一次真实 `pubDate` 对拍：单次 login、5 次 query、0 重试、不换样本、5/5 的
> `statDate/pubDate/available_time` 全一致并正常 logout；执行后仍为 0 worker/
> 0 `:10030` 连接，远端 staging 财务/JobRun/交易计数未变。机器验收 JSON 为
> `docs/phase3/reports/P3.3-S2-final-acceptance-20260731.json`
>（SHA-256 `29ead3fb55f5c382f4b0c5539dc70e42db0e3d59048551f6b5834ac07188b16a`），
> 对拍证据为
> `docs/phase3/reports/P3.3-S2-pubdate-evidence-20260731.json`
>（SHA-256 `ff2dcaf445c26b93ce4aaad427f331bf8979d2020e2e666d61865c0153b80a8b`）。
> 全程 `research/live=false/paper_auto=false`、`unlock_trade` 永久屏蔽，
> 本机提案/委托保持 1/1、远端 0/0。**机器门与真实外部对拍门已齐，并于
> 2026-07-31 获架构师签认；S2 据此标 done，按既定三组提交切分收官。**

---

## P3.3-S3 历史估值表 + 多年回填 + 盘后增量 ✅ done 2026-07-24

> **⚠ S3 数据源修订(2026-07-24,架构师，实测驱动，覆盖下方 BaoStock 版契约)**
>
> **动机**：S2 把 BaoStock 拖成单点 + 3 周慢跑。为让 S3 **并行、且完全不碰 BaoStock**，换源。实测:
> - Tushare `daily_basic`:❌ 低积分账号 **1 次/小时**死限（拉 1833 天要 76 天，不可行）。
> - **东财 `stock_value_em`(via akshare):✅ 采纳为 S3 主源**。走东财 datacenter 端点（非被封的
>   push2his/push2），实测 5 股 5/5 通、每股 ~2076 行（≈2016 至今，覆盖 2019-2026 窗）。中文列
>   `PE(TTM)`→pe_ttm、`市净率`→pb_mrq、`市销率`→ps_ttm——**S4 要的 pe/pb/ps 齐全**；输入 6 位
>   `symbol`（如 `600519`），与本库 symbol 格式**完全一致，零转换**。
> - 下方 **BaoStock `get_valuation_history` 契约降级为"备用源"**:仅东财中途不稳时对失败股补齐；
>   注意它走 BaoStock，**不得与 S2 的 BaoStock 连接并发**。
>
> **改动要点（照此实现，覆盖下方 BaoStock 版取数与 backfill 契约）**:
> 1. **取数** `jobs/valuation_sync.py::fetch_valuation_em(symbol) -> DataFrame`:内部
>    `import akshare as ak; ak.stock_value_em(symbol=<6位>)`;列改名映射
>    {数据日期→trade_date, `PE(TTM)`→pe_ttm, `市净率`→pb_mrq, `市销率`→ps_ttm}；空/非数值→NaN；
>    只留 `start_date(默认 2019-01-01) ≤ trade_date ≤ S1 末日` 的行。
> 2. **source 字段**:主源行 `source="em"`；备用补齐行 `source="baostock"`。valuation_daily 不进
>    daily_bars/sector 的审计白名单，**无需动 provenance.py**。`ValuationDaily.source` 默认改 `"em"`。
> 3. **backfill_valuation(...)** 改成对全市场每股调 `fetch_valuation_em`;**东财防封韧性**:每股间
>    `time.sleep(0.3~0.5s)`；瞬时失败退避重试；幂等按 (symbol,trade_date) 跳过、断点续传按 per-symbol
>    `max(trade_date)`；**区分"东财限流/封"与"个股无数据"**——前者连续失败即暂停并如实记 `em_throttled`，
>    **不伪造成功**；独立短事务 + 锁退避。
> 4. **available_time 不变**:trade_date 15:00 上海 → UTC（铁律#1）。
> 5. **可与 S2 并行**:东财 ≠ BaoStock，S3 冷启动**本机跑、与阿里云 S2 的 BaoStock 慢跑并行**，互不
>    干扰（本机绝不碰 BaoStock 那条连接；本机 scheduler 若开着 BaoStock cron 仍按 S2 的约束处理）。
> 6. **对拍**:抽 5 (股,日) 与 **BaoStock 单次** `query_history_k_data_plus` 的 peTTM、或 **Tushare 单次**
>    `daily_basic`（1 次/小时足够抽验）对拍——三者应一致（茅台 2025-01-02 peTTM≈22.61 已知基准）。
>
> **零下行风险**:东财是独立源，试它**完全不威胁 BaoStock 关系**；东财中途封则幂等设计保住已拿的、
> 余量留给备用 BaoStock 或改日重试，损失仅时间。`sync_valuation_daily` 盘后增量同样改走东财。

**目标**：新建历史每日估值表并回填多年,为 S4 的估值因子改造供 PIT 数据源。**这是新建,不是复用**
(现在全项目无任何 peTTM/pbMRQ 历史落库,`securities.pe_ttm/pb` 只是 Futu/Sina 当前快照)。

**models.py 新增**：
```python
class ValuationDaily(Base):
    __tablename__ = "valuation_daily"
    __table_args__ = (UniqueConstraint("symbol", "trade_date", name="uq_valuation_daily"),
                      Index("ix_valuation_symbol_date", "symbol", "trade_date"))
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    symbol: Mapped[str] = mapped_column(String(24), index=True)
    trade_date: Mapped[date] = mapped_column(Date)
    pe_ttm: Mapped[float | None] = mapped_column(Float, nullable=True)   # peTTM
    pb_mrq: Mapped[float | None] = mapped_column(Float, nullable=True)   # pbMRQ
    ps_ttm: Mapped[float | None] = mapped_column(Float, nullable=True)   # psTTM
    source: Mapped[str] = mapped_column(String(16), default="baostock")
    available_time: Mapped[datetime] = mapped_column(DateTime(timezone=True))  # 铁律#1:trade_date 当日 15:00 上海→UTC
    ingested_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=...)  # 用 core.timeutil
```
(注:`create_all` 会建此表;无需 migrate。若日后加列再走 migrate。)

**baostock_provider.py 新增契约**：
```python
def get_valuation_history(self, symbol, start, end) -> pd.DataFrame:
    """query_history_k_data_plus(code, "date,peTTM,pbMRQ,psTTM", start, end,
       frequency='d', adjustflag='3')。复用 _baostock_lock + _ensure_login;
       空值('')转 NaN;返回列 [trade_date, pe_ttm, pb_mrq, ps_ttm]。"""
```

**jobs/valuation_sync.py 新增**:
```python
def backfill_valuation(*, start_date, end_date=None, symbols=None, batch_size=100) -> dict:
    """一次性多年冷启动。对全市场每只股 get_valuation_history → 逐日 upsert valuation_daily;
       available_time = 该 trade_date 15:00 上海 → UTC(core.timeutil);
       幂等:按 (symbol, trade_date) 跳过已存;断点续传按 per-symbol max(trade_date);
       独立短事务 + 锁退避;连续 20 网络失败熔断(数据缺失记 skip)。返回 stats。"""

def sync_valuation_daily(lookback_days=10) -> dict:
    """日常增量(盘后),照 sync_daily_bars 结构:按每股已存 max(trade_date) 增量拉。
       register(JobSpec(name='sync_valuation_daily', func=..., trigger=CronTrigger(
       day_of_week='mon-fri', hour=18, minute=50, tz='Asia/Shanghai')))  # 在 daily_bars 18:40 之后"""
```
在 `jobs/__init__.py` 注册两者(backfill 用 `trigger=None`)。

**验证**:
- 覆盖:`valuation_daily` 最早 trade_date ≤ S1 目标起始;`COUNT(DISTINCT symbol)` 接近全市场;
  pe_ttm 非空率合理(亏损股 peTTM 可能负/空,记录但不当错误)。
- PIT:随机抽样 `available_time` 是否 = trade_date 当日盘后(不早于收盘)。
- 对拍:抽 5 只股某历史日,与 BaoStock 直接查的 peTTM 一致。
- 增量 job 幂等空跑。

**实现与运行记录（2026-07-24）**：

- 已新增 `valuation_daily`、东财 `stock_value_em` 标准化、每股 `0.3~0.5s` 节流、三次退避、
  连续上游失败暂停并记 `em_throttled`、按每股最大日期续传、独立短事务与 SQLite 写锁退避；
  主源固定为 `em`，本机代码路径不调用 BaoStock。
- 真实五股探针 `JobRun 10703` 为 `ok`：5/5 成功，写入 7,876 行，范围
  `2019-01-02..2026-07-23`；贵州茅台 `600519 / 2025-01-02` 的
  `pe_ttm=22.60643568`，命中规格约 22.61 的基准；`available_time=2025-01-02 07:00:00 UTC`
  等于上海 15:00。
- 使用低积分 Tushare 本小时唯一一次 `daily_basic(trade_date=20250102)` 做五股只读对拍：
  PE(TTM) 绝对差最大 `0.00004263`。PB 在两源间存在约 `0.014..0.188` 的口径差，本节只按
  既定 PE(TTM) 基准验收，不把不同 PB 定义伪报为一致。
- 收官 `JobRun 11097` 为 `ok`：8,450,142 行、5,530/5,530 股、范围
  `2019-01-02..2026-07-23`、失败 0；source 全 `em`、重复键 0、区间外 0、PIT 异常 0。
- 全量幂等空跑 `JobRun 11325` 为 `ok`：5,530/5,530 全 skip，`provider_calls=0`、
  `provider_attempts=0`、`rows_inserted=0`，前后表行数均为 8,450,142。
- 冷回填期间暂关的 `sync_valuation_daily` 已恢复：`ALPHAPILOT_SCHEDULER_ENABLED=true`、
  `ALPHAPILOT_VALUATION_SYNC_ENABLED=true`；API 重启后健康，任务启用且下次触发为
  `2026-07-27 18:50 Asia/Shanghai`。本机 `sync_financials` 仍独立保持关闭。
- 本机 `sync_financials` cron 已单独关闭，BaoStock 端口 10030 无连接；阿里云 S2 仍按原
  单 IP / 单连接 / 每日 40,000 次额度运行，未增加第二条 BaoStock 连接。

> ⚠ DEVIATION: 首次用 `nohup` 启动时，执行宿主在会话退出后回收子进程；按单股短事务已落的
> 3,664 行保持可续传，孤儿 `JobRun 10705` 已如实标 failed，没有改成成功。随后改用 launchd
> 从每股最大日期续跑。进一步发现 `launchctl submit` 会在正常退出后重启，故中途把 `JobRun 10714`
> 标 failed 并迁到 `RunAtLoad=true / KeepAlive=false` 的标准 LaunchAgent，当前续跑为 `10771`；
> 这是运行托管方式修正，不改变数据源、PIT 或因子设计。
>
> ⚠ DEVIATION: 2026-07-24 19:28 后本机断网，当前 AKShare `stock_value_em` 内部
> `make_request_with_retry_json` 未向 `requests.get` 传入超时，导致 `JobRun 10771` 保持 running、
> 进程长期阻塞在 SSL read。为消除无限挂起，`fetch_valuation_em` 改为直接调用 AKShare 该函数使用的
> 同一东财 `RPT_VALUEANALYSIS_DET` 端点与同一字段契约，并强制 connect=5s/read=20s；外层仍只退避
> 3 次。网络中断现在如实记 `network_unavailable`（不误记 `em_throttled`）后暂停，已落的每股短事务
> 保留并按最大日期幂等续跑；数据源、字段、PIT 与 `source="em"` 均未改变。

---

## P3.3-S4 因子引擎估值改造:pe/pb_percentile 读历史表 ✅ done 2026-07-24

> **⚠ S4 实现修订(2026-07-24，架构师核过当前代码，覆盖下方"取最新一行"写法)**
>
> 已核实:pe/pb 块在 `factors.py:508-520`（读 `Security.pe_ttm/pb` 快照）；PIT 范式参考
> `_load_financial_factors`（:326-367）。**但估值不能照抄财务那套"扫全历史取最近一期"**——财务季频
> 稀疏（33k 行）才那么取；**估值日频稠密（`valuation_daily` 已 160 万行、终态 ~千万行），"扫全历史
> 每股取最近值"会让回测每个决策日拉几百万行，慢到不可用。** 正确做法:按**决策日单日**查。
>
> **新增 `_load_valuation_factors(session, symbols, trade_date, cutoff)`（单日查，不扫全历史）:**
> ```python
> rows = session.execute(
>     select(ValuationDaily.symbol, ValuationDaily.pe_ttm, ValuationDaily.pb_mrq,
>            ValuationDaily.available_time)
>     .where(ValuationDaily.trade_date == trade_date,          # 决策日当天，单日 ~5000 行
>            ValuationDaily.available_time <= cutoff)           # 防未来函数
> ).all()
> latest = {}
> for symbol, pe, pb, at in rows:
>     if str(symbol) not in symbols: continue
>     if not isinstance(at, datetime) or _utc(at) > cutoff: continue   # 双检(镜像财务:360)
>     latest[str(symbol)] = {"pe_ttm": pe, "pb_mrq": pb}
> return latest, len(latest)
> ```
> 缺行的股当日无估值 → 不进 latest → NaN（可接受，与旧快照缺失同义）。（边界:若某回测 `trade_date`
> 恰非估值覆盖日，可选降级为"≤trade_date 的最近有估值日"，但须**按 symbol 限定窗口**（如
> `trade_date-10d ≤ td`）再取 max(td)，**绝不无界扫全历史**；日频数据下正常无需降级。）
>
> **替换 `factors.py:508-520` 的 pe/pb 块**（注意列名:valuation_daily 是 **`pb_mrq`** 不是 `pb`）:
> ```python
> valuations, _ = _load_valuation_factors(session, symbols, trade_date, decision_cutoff)
> for column, factor in (("pe_ttm","pe_percentile"), ("pb_mrq","pb_percentile")):
>     series = pd.to_numeric(pd.Series({s: v[column] for s, v in valuations.items()}),
>                            errors="coerce")
>     series = series.where(series > 0)                # 保留原 >0 过滤(负/零 PE 不进百分位)
>     frame.loc[series.index, factor] = series.rank(method="average", pct=True)
> ```
> - 截面 `rank(pct=True)` 逻辑不变，只是排名域从"快照全 securities"变成"当日有估值的股"，百分位含义
>   不变；方向不变（`pe_percentile` 越低越好仍由权重负号体现，S4 不碰权重）。
> - **`security_frame` 若因此不再被 pe/pb 用到 → 检查是否成死代码**:是则删其加载；但 `Security.pe_ttm/pb`
>   列**保留**（前端展示仍用）。
> - **live 兼容**:S3 的 `sync_valuation_daily` 盘后(18:50)落当日估值，live 决策(19:30)单日查即取今天 → 无回归。
> - **改测**:原先依赖 `Security` 快照 pe/pb 造因子的测试，改用 `valuation_daily` fixture。
>
> 下方"最新一行"描述以本修订为准。

**目标**：解决"估值因子历史必为 NaN"的根因。当前 `engines/factors.py::compute_factors_for_date`
(:508-520)的 `pe_percentile/pb_percentile` 读 `Security.pe_ttm/pb` 当前快照,且被
`snapshot_is_current = day_start ≤ snapshot_at ≤ decision_cutoff` 过滤 → **历史某日重算必全 NaN**。
改成读 S3 的 `valuation_daily`(PIT)。

**做法**:
- 在 `compute_factors_for_date(session, trade_date)` 里,把 pe/pb 的取数从 `Security` 快照改为:
  查 `valuation_daily` 中每只股 `available_time ≤ decision_cutoff` 的**最新一行**(通常即 ≤ trade_date
  的最近交易日估值),取 `pe_ttm`/`pb_mrq` → 再做原有的**截面百分位** `rank(pct=True)`(逻辑不变,
  只换数据源)。
- **保持 live 兼容**:S3 的 `sync_valuation_daily` 盘后增量保证"当日"也有估值行,live 决策(19:30
  cutoff)照样取到最新;因此可**统一走 valuation_daily**,不再依赖 `Security` 快照做因子(Security
  快照仍供前端展示,不动)。
- pe/pb 方向不变(`pe_percentile` 越低越好由权重负号体现,承接现有约定;S4 不碰权重)。

**验证**(关键,S4 是 estimated改动最容易错的一步):
- 单测:构造某历史 trade_date 的 valuation_daily 数据,调 `compute_factors_for_date`,断言
  `pe_percentile/pb_percentile` **不再全 NaN**、百分位在 [0,1]、方向正确。
- PIT 单测:某股估值 available_time 晚于决策 cutoff 时,该股当日 pe 不参与(不偷看未来)。
- 回归:live 路径(当日)pe/pb_percentile 与改造前数值一致(用当日 valuation_daily = 当日快照校准)。
- 全量质量门 ruff + mypy strict + pytest 绿。

**实现与验证记录（2026-07-24）**：

- 新增 `_load_valuation_factors`，SQL 仅查询 `trade_date == 决策日` 且
  `available_time <= 19:30 cutoff`，返回前再做 UTC 截止时间双检；不回退前一交易日，
  不扫描历史窗口。因子列使用 `pe_ttm` / `pb_mrq`，原有 `> 0` 过滤与截面百分位方向不变。
- `Security.pe_ttm/pb` 模型列完整保留供前端；仅从因子合格证券查询中移除这两个不再使用的快照字段，
  ST 状态及其 PIT 判定路径不变。
- 性能审计发现原 `(symbol, trade_date)` 索引无法支持单日截面，真实执行计划为全表 `SCAN`；
  已新增幂等迁移 `ix_valuation_trade_date_symbol(trade_date, symbol)`。845 万行生产库迁移后，
  执行计划变为索引 `SEARCH`，`2026-07-23` 单日 5,530 行读取约 0.05 秒。
- PIT/回归单测覆盖：仅精确决策日、截止时间后行排除、负/零 PE/PB 排除，以及
  当日 `valuation_daily` 与旧快照同值时百分位一致；迁移测试同时锁定查询计划必须使用日期索引。
- 真实 `2026-07-23` 因子计算：5,526 个合格标的均命中当日估值，PE/PB 有效覆盖
  3,985/5,484，百分位范围均在 `(0, 1]`，全流程约 5.1 秒。
- 全仓 `ruff check .`、strict mypy（133 source files）、全量 pytest 均通过。

---

## P3.3-S5 板块资金流一年回填(富途 DAY 口径) ✅ done 2026-07-25

**目标**：`sector_flow_daily` 从 1 天 → **一年**,让 `net_inflow_5d` 因子有历史。**白名单无需改**
(`get_capital_flow` 已在 `QUOTE_READ_METHODS`,`client.py:58`,现网 `sectors_sync.py:359` 已在调),
**改的是调用参数**:从默认 INTRADAY(只当天)改 `period_type=DAY` + `start/end`。

**jobs/sector_flow_backfill.py 新增**:
```python
def backfill_sector_flows(*, start_date, end_date=None, pause_seconds=1.05) -> dict:
    """一次性一年回填。
    1. 板块→成分:复用 sectors_sync._cached_plates()(:186);
    2. 每板块 top5:复用现有口径——成分股按 total_market_val 降序取前 5
       (sectors_sync.py:344-350;市值来自快照,回填用当前快照的 top5 作固定篮子,见诚实标注);
    3. 去重后对每只选中股:client.quote_call_raw('get_capital_flow',
         kwargs={'stock_code': code, 'period_type': {'__futu_constant__':'PeriodType.DAY'},
                 'start': start_date_str, 'end': end_date_str})
       —— SDK normalize 会把窗口卡到 ≤365 天;每只之间 time.sleep(pause_seconds);
    4. 遍历返回的**每一行**(capital_flow_item_time = 每个交易日;不是只取最后一行!):
       net = in_flow;main = main_in_flow 或回退 super_in_flow+big_in_flow(复用 _latest_capital_flow
       的字段解析逻辑,但改成逐行);
    5. 按 (plate_code, trade_date) 聚合成员:net_inflow=Σnet、main_inflow=Σmain;
    6. upsert(照 _persist_flow_rows :396),source='futu-daily';
    7. 幂等:按 (plate_code, trade_date) 跳过已存;富途 client 用 get_futu_client() 单例,
       走 job(API lifespan 兜底 close)或脚本 finally: client.close()(防 pytest 退出挂起坑)。
    返回 stats:板块数/交易日数/成员调用数/覆盖率/失败清单。"""
```
**必须同步**:`provenance.py:6` 的 `AUDITED_SECTOR_FLOW_SOURCES` 加 `"futu-daily"`,否则审计层拒。
在 `jobs/__init__.py` 注册(`trigger=None`)。

**诚实标注(写进 job stats 与 S6/S9 报告)**:
- **深度硬上限约一年**(富途 2025-07 起),`net_inflow_5d` 因子回测窗因此**最多一年**,比价量/财务/估值
  短——补不动,不得掩盖。
- **top5 固定篮子的轻微前视偏差**:用"今天的 top5"当历史篮子聚合,严格说有 look-ahead;对资金流
  因子影响很小,S5 先接受并标注,值得再上"全成分聚合"口径(留 M3+ 增补)。
- **2026-07-25 裁定覆盖前述“最多一年回测”预期**：历史成分 PIT 缺口使该一年窗口不能进入
  S7/S9；回填表仍保留给 live 产品和前向窗口，旧 IC 仅作审计，不作为研究结论。

**验证**:
- 覆盖:`sector_flow_daily` 覆盖 131 板块 × ~242 交易日、source='futu-daily'、非空。
- `net_inflow_5d` 在固定篮子口径下机械可算：对某历史日调 `_sector_flow_values`
  (factors.py:390)不再空(需 5 连续交易日)；但 2026-07-25 裁定其成分 PIT 不合格，结果仅作
  数据链审计，不进入 S7/S9。
- 幂等空跑。

**实现与运行证据（2026-07-24，架构师已独立验收）**：

- `JobRun 10715` 成功：当前市值 top5 固定篮子去重为 640 只，640/640 富途
  `PeriodType.DAY` 请求成功，失败 0。
- `sector_flow_daily` 为 `31,702 = 131 × 242` 行，区间
  `2025-07-25..2026-07-24`，`source='futu-daily'`，net/main 空值均为 0。
- 真实五日链路在 `2026-07-20..2026-07-24` 得到 131 个板块，
  `observed_days=5`，唯一来源 `futu-daily`。
- `JobRun 10764` 幂等空跑 0.18 秒：`idempotent_skip=true`、成员请求 0、
  inserted/updated 0、`skipped_existing=31,702`。
- Job stats 固定保留 `Futu PeriodType.DAY <= 365 calendar days` 与
  `current top5 fixed for history` 前视偏差；任务只手动触发，成员请求有任一失败则不落部分结果。

> ⚠ DEVIATION: 首次 `nohup` 任务被执行宿主回收，孤儿 `JobRun 10706` 已标 failed 且未写历史行；
> 改用 launchd 后完成 10715。`launchctl submit` 成功退出后又拉起一次，重复
> `JobRun 10758` 已标 failed 且未持久化；之后以交易日历覆盖短路完成幂等复核。

### ▶ net_inflow_5d 历史 PIT 裁定与前向修复（2026-07-25）

- 历史状态固定为 `history_excluded_pit_gap`：退出 S7 历史 IC 主表与 S9 v3 权重候选，不写
  偏差版 `factor_ic_stats`。
- M3 历史检验范围从 12 修订为 11 因子（4 价量 + 5 财务 + 2 估值）。
- 新建 `sector_constituent_snapshots`，唯一键为
  `(plate_code, symbol, as_of_date)`，并记录真实 `available_time`。
- `snapshot_sector_constituents` 在工作日 15:10 固化当日可见成分；幂等复跑保留已有行的首次
  可见时间，仅为同日后来真实出现的新成分追加行。
- 因子引擎只按**精确决策日**读取快照，并同时要求 `available_time <= 19:30` 决策截止时间；
  不回退旧日、不扫描或借用当前成分。当前 `sector_constituents` 仍只服务实时
  `sector_strength`。
- S6 把该项改为非阻断记录，同时检查前向快照重复键；未来积累出足够窗口后另行验收恢复研究资格。

> ⚠ DEVIATION: 当前内存调度器无持久 jobstore/catch-up。若 API 在 15:10 停机，当日快照会诚实
> 缺失、`net_inflow_5d` 保持空值，不补写或借用其它日期；后续应补 19:30 前启动补采或缺口告警。
> Mon–Fri cron 也不会排除交易所节假日，节假日快照因 exact-date 交易日读取而不会污染因子，仅形成
> 无用记录。

---

## P3.3-S6 回填后数据体检(一次性,进重跑前的闸门)

**目标**：在跑 M1/M2 重检验**之前**,出一份诚实的"回填后数据现状"报告,确认输入底表达标,否则重跑
又是垃圾进垃圾出。

**做法**:写一个只读体检脚本/函数(可放 `scripts/` 或 job),输出:
- 四张底表覆盖:daily_bars/adj_factors(多年)、financial_indicators(全市场多年)、valuation_daily
  (多年)、sector_flow_daily(一年)的日期范围、股票数、每因子非空率。
- **逐因子历史可测性预检**(照架构师 07-24 手法):对若干历史决策日调 `pit.py::factor_zscores`,
  确认 11 个历史候选因子 `n≥` 阈值、非全 NaN；`sector_strength` 明确标 live-only，
  `net_inflow_5d` 明确标 `history_excluded_pit_gap` / live-forward。
- PIT 抽样:随机若干 (股, 历史日) 对拍数据源。

**验证**:体检报告显示 11 个历史候选因子在各自可用区间都有充足截面；任何仍 `n_periods=0`
的候选因子必须给出原因(不是"没数据"就是"因子/取数有 bug"——后者要修)。资金流排除项不再
阻断 S7，但必须保留裁定日期与原因。

---

## P3.3-S7 M1 重跑:多年单因子 IC(补运行时入口)

**目标**：在补齐数据后,**第一次**对完整 11 个历史候选因子跑多年单因子 IC,逐个定性有没有 alpha。这是 M3 的
核心产出。**注意:M1 IC 走 `pit.py` 现场重算,不读 factor_values,所以 S1-S5 一到位就能测。**

**缺口**:`backtest/factor_research.py::all_factors_ic`(:229)/ `persist_factor_correlation`(:327)
**目前无任何运行时入口**(只测试调用)。S7 补一个 job 把它们串起来。

**jobs/factor_research_job.py 新增**:
```python
def run_factor_research(*, start_date, end_date, do_rebuild=False,
                        train_ratio=0.7, output_path=None) -> dict:
    """1. all_factors_ic(session, start, end, sample_tag='full') → 写 factor_ic_stats;
    2. 按 train_ratio 时间切分,再各跑 sample_tag='train'/'test'(承接 M2 铁律#1 样本外);
    3. persist_factor_correlation(session, train_start, train_end) → 写 factor_correlation_stats;
    4. do_rebuild=True 时(留给 S9,S7 传 False)才 rebuild_weights;
    返回逐因子 ic_mean/ic_ir/t_stat/n_periods/long_short 汇总。"""
```
- **⚠ 多年窗口必须绕开固定 301 日窗口(打法 B 成败的唯一关键,已核实源码,别漏)**:
  - `all_factors_ic → _research → _calendar(session, start, end)`(factor_research.py:96)**认显式区间**,
    所以 S7 只要把多年 `start_date/end_date`(如 2019-01 起)传进去,IC 就在完整多年样本上算——**无需改
    `factor_research.py`**。
  - **但 `weights_rebuild.py` 的 `train_test_split`/`_research_calendar` 写死"只取最近 301 个交易日"**
    (`_RESEARCH_SESSION_COUNT=301`:22、`.limit(301)`:41,且 `len!=301` 直接抛错:59)。**S7/S9 绝不能调
    `train_test_split` 来切 train/test**——否则回填了 7 年、窗口仍只看最近 ~1.2 年,**完美复现 M2 弱样本、
    B 白做**。
  - 正确做法:`run_factor_research` **自己**按 `start_date/end_date/train_ratio` **算 train/test 边界日期**
    (取 [start,end] 内审计交易日、按比例切),把**显式日期**传给 `all_factors_ic`(sample_tag='full'/'train'/'test'
    各跑)。`train_test_split` 那个 301 helper 要么彻底不用、要么另行泛化(去掉 `!=301` 断言)。
- 在 `jobs/__init__.py` 注册(`trigger=None`);冷启动用测试/脚本直接调传 `start_date/end_date`。

**验证 + 架构师独立复核**:
- `factor_ic_stats` 里 **11 个历史候选因子** `n_periods` 充足(多年→数百期,基本面因子
  统计效力这次够了);每因子 `t_stat` 有值。
- `net_inflow_5d` 不写 S7 IC，诊断状态固定为 `history_excluded_pit_gap`；`sector_strength`
  仍为 live-only。
- **逐因子结论**:哪些因子 |t|≥2 且方向合理(可能有 alpha)、哪些不显著、哪些反向(区分反转市 vs
  bug,承接 M2 铁律#4)。**这次才是对"基本面因子有没有 alpha"的第一次有效回答。**
- 架构师会**独立重算**若干因子的 IC(抽一个因子、一段区间,手算 Rank IC 对拍),不轻信 job 输出。
- `GET /v1/backtest/factors/ic` 刷新到多年窗口结果。

---

## P3.3-S8（可选/live）历史 factor_values 重算入口

**目标**（非关键路径,仅为前端 live 展示补历史因子值）:给 `jobs/factors.py::compute_factors(trade_date)`
(:278,函数本已支持传历史日、按日 delete+insert 幂等)补一个能传 `trade_date` 的入口,循环历史交易日回填
`factor_values`。**回测/IC 不依赖它,可最后做或跳过。**

**注意**:`compute_factors` job 有 3 道闸门会拦历史日(`MIN_INPUT_COVERAGE=0.90` 用**当前** universe
做分母 :312、composite 覆盖率下限 :350、daily_bars/adj running 检查 :289)。历史重算需放宽/绕过这些
针对历史日的语义(如覆盖率用"当日在册股"做分母,而非当前 universe)。**这段改动风险高、收益低
(只影响展示),排最后,谨慎。**

**验证**:若实现,`factor_values` 覆盖多年、model_version 标注清楚(由 `factor_weights_file` 的
`version` 决定,当前 v1.1.0→`factor-v1.1.0`);不影响 S7/S9 结论。

---

## P3.3-S9 M2 重构:多年 train 窗 → composite-v3 + 诚实结论 + 安全复核

**目标**：在多年样本上,用防过拟合方法重构权重,样本外验证完整因子模型能不能转正。**这是 M3 的
收官判断。**

**做法**(严格承接 `phase3/02` 全部防过拟合铁律):
- **算 train/test 边界(承接 S7,不走 301)**:`run_factor_research(do_rebuild=True)` 自己从多年
  [start,end] 按 `train_ratio` 切出 `train_start/train_end`(前 70%)与 `test_start/test_end`(后 30%),
  **不调 `train_test_split`**。
- **重构权重**:`rebuild_weights(session, train_start, train_end, output_path=...)` 已核实**认显式
  train 区间、内部只调 `all_factors_ic(train_start,train_end)`,不走 301**(weights_rebuild.py:114-131)。
  在多年 train 窗用 signed IC_IR 加权 + L1 归一 + `classify_factors` 去冗余,一次成型,**不挑选、不回头调**。
  财务/估值因子这次有多年数据 → IC_IR 算得出 → **权重不再像 v2 那样只有 4 价量非零**。
  - **⚠ 已核实:`rebuild_weights` 硬编码 `version:"v2.0.0"` / `signal_id:"composite-v2"` / 默认输出
    `factor_weights_v2.yaml`(weights_rebuild.py:136-140)。** 要产出 composite-v3,必须给它**加
    `version`/`signal_id`/`output_path` 参数**(传 `v3.0.0`/`composite-v3`/v3 路径),否则写出来的仍是 v2 内容。
  - **⚠ 引擎信号路由也要加 v3**:`engine.py` 现只认 `composite-v1`/`composite-v2`(+ `_V2_WEIGHTS_FILE`:36)。
    需在 signal_id 解析处加 `composite-v3 → config/factor_weights_v3.yaml` 映射,并放开 `BacktestConfig`/
    `BacktestRunRequest` 对 signal_id 的取值校验到 v3,否则 `run_backtest` 不认 v3。
    (保留 v2 那份失败权重不覆盖,好处是 `GET /v1/backtest/compare?v1=&v2=` 能直接对比"残缺 4 因子 v2"
    与"完整因子 v3"两个时代。)
- **test 窗回测(必须传显式日期,别用 window='test')**:`run_backtest` 用
  `BacktestConfig(signal_id='composite-v3', start_date=test_start, end_date=test_end,
  rebalance_freq='20d', cost_model=...)`,vs 沪深300 + 等权双基准,扣成本。**⚠ 已核实:API
  `POST /v1/backtest/run` 的 `window='test'` 会调 `train_test_split(session)`(backtest.py:137)→ 固定
  301 日窗 → test 只有 ~91 天,直接吃掉多年样本。且 `window` 与显式 `start_date/end_date` 互斥
  (backtest.py:127)。所以务必 `window=None` + 传 S7 算出的多年 `test_start/test_end`。**
- **诚实判定**:test 窗净值、Rank IC、IC_IR、t 值。转正就是转正,仍失败就如实报"仍失败"
  (承接 M2 铁律#6,**绝不调参到好看**)。诚实的三次失败 > 过拟合的虚假成功。

**验证 + 架构师收官复核**:
- 质量门全绿;`config/factor_weights_v3.yaml` 权重分布(基本面/估值因子这次是否拿到非零权重)。
- test 窗结果**独立重算**校验(净值、基准超额)。
- **安全不变量**:live/unlock/paper_auto 三闸仍 false、无新增交易、`unlock_trade` 仍 PERMANENTLY_BLOCKED。
- **结论表述**:M3 第一次在多年样本上检验了 PIT 有效的完整 11 因子模型 → 基本面因子到底有没有 alpha 的
  **有效结论**(区别于 M1/M2 的伪结论);sector_strength live-only、`net_inflow_5d`
  `history_excluded_pit_gap`、幸存者偏差三处限制显著标注。
- **红线**:即使 composite-v3 转正,也只是"样本外一次通过",**不足以开 paper_auto**——需多轮
  滚动验证(M4)。M3 结束不动实盘、不动自动模拟盘。

---

## 附:关键代码锚点速查(交给 Codex,均为 2026-07-24 只读核实)

| 关注点 | 位置 |
|---|---|
| 富途通用调用 + 白名单 | `futu/client.py` `quote_call_raw`:601、`QUOTE_READ_METHODS`:50(`get_capital_flow`:58)、`get_futu_client`:913 |
| 富途 SDK 资金流签名 | `get_capital_flow(stock_code, period_type=PeriodType.INTRADAY, start, end)`;`PeriodType.DAY`;窗口 normalize≤365 |
| 板块资金流现状 | `jobs/sectors_sync.py` `_cached_plates`:186、top5 按 total_market_val:344、`_latest_capital_flow`:306、`_persist_flow_rows`:396、`CAPITAL_FLOW_PAUSE_SECONDS`=1.05 |
| 审计源白名单 | `data/provenance.py` daily:3、sector_flow:6 |
| BaoStock provider | `data/baostock_provider.py` `_baostock_lock`、`get_adjusted_closes`:71、`get_bars`:116、`query_history_k_data_plus`:129、`_FINANCIAL_QUERIES`:16 |
| 财务同步 job | `jobs/financials.py` `sync_financials`:319、`FINANCIAL_METRICS`:21、`_normalize_quarter`:239、`_availability`:178 |
| 因子定义/计算 | `engines/factors.py` `FACTOR_SET`(13):33、`FINANCIAL_FACTORS`:48、`_load_bar_factors`:230、`_load_financial_factors`:326、pe/pb_percentile:508、`compute_factors_for_date`:475、`composite`:590 |
| factor_values 写库 | `jobs/factors.py` `compute_factors(trade_date=None)`:278、`MIN_INPUT_COVERAGE`:45、register:417 |
| 单因子 IC / 相关 / 重构 | `backtest/factor_research.py` `all_factors_ic`:229、`persist_factor_correlation`:327;`backtest/weights_rebuild.py` `rebuild_weights`:114、`train_test_split`(301):47、`_DEFAULT_OUTPUT`:23 |
| 回测引擎 | `backtest/engine.py` `run_backtest`:551、`BacktestConfig`:41、`_prices_for_date`(inner join AdjFactor):193;`backtest/pit.py` `eligible_universe`:38、`factor_zscores`:164、`signal_scores`:203、`forward_return`:226 |
| 复权因子 job | `jobs/backtest_jobs.py` `sync_adj_factors_job`:55;`backtest/adjust.py` `sync_adj_factors`:373(Tushare) |
| 日线 job(冷启动范式) | `jobs/daily_bars.py` `sync_daily_bars`:180、`_save_bars_with_lock_retry`:119、`latest_by_symbol`:200 |
| 一次性回填范式 | `jobs/event_backfill.py` `backfill_events`:41、`register`(trigger=None):111 |
| Job 框架/审计 | `jobs/registry.py` `JobSpec`、`run_job`:85、`JobExecutionError`;`jobs/__init__.py::register_builtin_jobs`:6 |
| 迁移 | `db/migrate.py` `MIGRATIONS`:8、`run_migrations`:134、`ensure_column`:55 |
| 模型 | `db/models.py` `FinancialIndicator`:549、`FactorValue`:572、`DailyBar`:59、`SectorFlowDaily`:455 |
| DB 路径(CWD 坑) | `core/config.py` `database_url`=`sqlite:///data/alphapilot.db`:26;`db/engine.py` `_build_engine`:18(相对 CWD) |

---

## 架构师复核记录（P3-M3 交付后逐 S 独立验收，随交付追加）

（待 Codex 交付后填写：每个 S 独立跑质量门、验证 DB 数据、抽样对拍数据源、独立重算关键 IC/净值、
核安全不变量。重点盯 3 处：① S4 估值改造的 PIT 正确性与 live 回归；② S7 基本面因子多年 IC 是否
真的转出统计显著性（架构师手算对拍）；③ S9 结论是否诚实、是否有 test 窗回头调参的痕迹。）

### S2 ✅ 最终独立验收通过（2026-07-31）

- 四路最终合同严格空跑全部通过：阿里云/狗云/US38/US-SEA 最新 JobRun 分别为
  `10/13/5/7`，均满足全字段 §2 合同。
- 生产库支持证券 `5203/5203` 全覆盖，`financial_indicators=847095`；五个 metric 各
  `169419` 行/`5203` 股；目标季度对 `208120/208120` 闭环，PIT 异常、重复、partial、
  unresolved、正负重叠均为 0。
- 固定五股真实 `pubDate` 对拍仅执行一次：单次 login、5 次 query、0 重试、不换样本，
  `statDate/pubDate/available_time` 5/5 一致并正常 logout。
- 安全复核：全程三闸未动，本机 `trade_proposals/broker_orders=1/1`、远端 0/0；
  架构师据最终 JSON、冻结 SHA 与真实外部对拍证据签认通过，**准予 S2 标 done 并按验收
  预案拆为三组独立提交。**

### S5 ✅ 独立验收通过（2026-07-24）

- 31,702 行精确等于 131 板块 × 242 交易日，区间与富途一年上限一致，来源 100%
  `futu-daily`，审计白名单已登记。
- 640/640 请求成功，幂等空跑无成员请求；365 天上限与固定 top5 前视偏差均被测试和 Job stats
  锁定。
- S5 数据链与 live 用途验收通过。2026-07-25 的后续裁定只撤销
  `net_inflow_5d` 的历史研究资格，不撤销本步数据验收。

### S3 ✅ 数据独立验收通过（2026-07-24；标 done 前尚差 2 项 ops）

架构师**全量**查库核实（8.45M 行逐项，非抽样）：
- **完成度**：**8,450,142 行 / 5,530 股 = 全市场 100% 覆盖**（`securities` 无一缺估值）；区间
  2019-01-02~2026-07-23；source 100% `em`（零 BaoStock）。
- **零缺陷**：重复键 0 组、区间外行 0、**PIT 异常 0（全 8.45M 行 `available_time` 恒 = trade_date
  07:00:00 UTC = 15:00 上海，铁律#1 满分）**；pe/pb 空值 0.00%、ps 0.02%。
- **对拍**：茅台 2025-01-02 `pe_ttm=22.60643568`（与 Tushare/BaoStock 基准 8 位一致）。
- 收官 `JobRun 11097` status=ok；中途 10714/10771 failed 系东财间歇限流，**幂等续传自愈、最终态
  100% 完整零污染**——失败如实记录。安全:提案/订单仍 1/1，三闸未动。
- **数据准予验收。但标 done 前还差 2 项 ops（Codex 执行并回报，架构师确认后即 done）**：
  ① **幂等空跑**——重跑 `backfill_valuation` 应 ~0 新增行、全 skip；
  ② **恢复暂关的 `sync_valuation_daily` 盘后 cron**——否则 live 估值不再更新、S4 的 live 路径会 stale。
  两项确认后:标 S3 done + 独立提交 + 解锁 S4（S4 实现按本文 S4 标题下 ⚠ 修订块，注意单日查/`pb_mrq`/`>0`）。

**Codex ops 回执（2026-07-24 22:38 CST）**：上述两项均已完成。幂等空跑为
`JobRun 11325 ok`，5,530 全 skip、零 provider 调用、零新增；估值 cron 已恢复并随 API 重启生效，

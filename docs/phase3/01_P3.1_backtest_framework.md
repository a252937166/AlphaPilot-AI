# P3-M1 回测框架 — 实施规格（实现者必读）

> 目标：给现有预测引擎（多因子 `composite`、板块预测、个股五维分）建**严格回测**，
> 回答唯一的核心问题——**这些信号到底有没有可交易 alpha？** 在此之前，二期所有"预测"
> 都是未经检验的基线（架构师二期收官签字已写明）。回测通过与否，决定这个平台是"能跑的
> 工程"还是"可信的投研工具"。
>
> 承接：`docs/phase2/00_EXECUTION_GUIDE.md` 的**全部通用守则仍然适用**（venv 路径、分层、
> `iso_utc`、质量门 ruff/mypy strict/pytest、安全不变量、中文文案、一期坑清单）。本文件只
> 增加**回测特有的铁律**，并给出 P3.1-S1…S8 的实现级步骤。按编号顺序做，每步跑完"验证"
> 才进下一步，做完在标题标 `✅ done`、独立 commit、偏差写 `⚠ DEVIATION`。

---

## 0. 回测铁律（违反 = 回测结果全部作废，比功能 bug 严重得多）

回测最大的风险不是崩溃，是**跑出一个看起来很好、实则错误的结果**，然后据此上实盘亏钱。
以下每一条都是量化界血泪教训，实现时逐条对照：

1. **未来函数（look-ahead bias）零容忍。** 决策日 T 的信号，只能用 `available_time ≤ T` 的
   数据。财务数据必须按 `financial_indicators.available_time` 过滤（不是 `report_period`！报告期
   2025Q1 的数据可能 4 月底才披露，5 月才可用）。价格因子只能用 ≤T 的 K 线。**任何"当时还
   不知道的信息"进入决策 = 致命 bug，回测会虚高。**

2. **决策与成交必须错开。** 用 T 日**收盘后**的数据打分 → 在 **T+1** 成交（按 T+1 开盘价或
   VWAP）。绝不允许"用 T 日收盘价决策、又用 T 日收盘价成交"——那是偷看未来。

3. **复权正确性。** `daily_bars` 存的是**不复权价**（BaoStock adjustflag=3）。除权除息日不复权
   价会假跳空，直接用它算收益是错的。**收益必须用复权价**：`日收益 = close_t·adj_t /
   (close_{t-1}·adj_{t-1}) − 1`。买卖用同一复权口径。这是 S1 先解决的前置。

4. **成交可行性过滤（A 股特有）。** 次日**涨停开盘买不进、跌停开盘卖不出、停牌无法成交**。
   选中的票若 T+1 一字涨停，该笔跳过（不能假装买到）。涨跌停幅度按 `securities.board/is_st`
   判（主板±10/创业科创±20/北证±30/ST±5）。

5. **T+1 制度。** 买入当日不可卖出，最早 T+1（隔日）卖。回测持仓逻辑必须遵守。

6. **交易成本必扣，裸收益不作数。** 佣金（双边，约万 2.5，最低 5 元）+ 印花税（**卖出单边**
   千 1）+ 过户费 + 滑点。任何"未扣成本的收益"不得作为结论；换手越高，成本吃得越狠——这正是
   区分真假 alpha 的关键。

7. **幸存者偏差如实标注。** 当前 `securities` 是**当前存续**的全 A，不含已退市股。历史某日的
   真实股票池应含当日存续+可交易的票（包括后来退市的）。M1 **不具备退市股历史**（P0 数据缺口），
   因此回测**带幸存者偏差，必须在报告里显著标注**，不得宣称无偏。

8. **无拟合泄漏。** M1 的信号是**静态权重**（`config/factor_weights.yaml`），不训练，所以是
   "历史检验"而非"训练-测试"——天然无拟合泄漏。但**为 M2 的 ML walk-forward 预留接口**：
   将来引入训练时，训练窗与测试窗必须严格时间隔离，绝不用全样本拟合再报告。

9. **基准对比是必须项。** 任何策略收益必须同时给出 **vs 沪深300** 和 **vs 等权全市场** 两个
   基准。单独的正收益毫无意义——跑赢基准才是 alpha。

10. **可复现。** 回测入参（信号、区间、成本模型、股票池过滤）落库 `backtest_runs.params`；
    同参数重跑结果一致（无随机，或随机种子入库）。

---

## 1. 目标与范围

**M1 交付：Point-in-Time 固定信号回测器**，对现有 `composite` 多因子信号做首次严格历史检验。

- **信号来源**：复用 `engines/factors.py` 的打分逻辑，对历史每个 rebalance 日用 PIT 数据重算
  （不是读只有 2 天的 `composite_scores` 表）。
- **回测区间**：受日线覆盖限制，约 2025-05 ~ 2026-07（~300 交易日）。财务因子受 `available_time`
  质量限制，报告里标注。
- **输出**：Rank IC 序列、分层净值、多空组合、换手、夏普/回撤/年化、概率校准；vs 双基准；结论
  "现有因子是否有统计显著的 alpha"。

**M1 不做**（留 M2+）：ML 模型训练与 walk-forward、日内/分钟回测、退市股历史、组合优化器、
实盘对接。M1 只回答"现有信号有没有 alpha"。

---

## 2. 代码放置

```text
src/alphapilot/backtest/          新增
├── __init__.py
├── adjust.py       复权因子接入与复权价重建（S1）
├── pit.py          Point-in-Time 数据访问 + 信号回放（S2）
├── costs.py        A股交易成本与成交约束（S3）
├── engine.py       回测主引擎 walk-forward 日循环（S4）
├── metrics.py      评估指标 Rank IC/分层/校准等（S5）
└── report.py       结果汇总与结论（S6）
src/alphapilot/jobs/backtest_jobs.py   Tushare 复权因子同步 job（S1）
src/alphapilot/api/routes/backtest.py  回测 API（S7）
```

Tushare token 从 `.env` 读（**新增 `ALPHAPILOT_TUSHARE_TOKEN`，绝不进 git**；本机值在
Obsidian「Tushare API 凭证」，低积分账号，`adj_factor`/`trade_cal`/`daily` 可调，
`fina_indicator`/`index_weight` **无权限**）。

---

## P3.1-S1 复权数据接入 + 复权价重建 ✅ done 2026-07-23

**目标**：解决铁律#3。给全市场补复权因子，提供复权价/复权收益工具。

**config.py 增加**：`tushare_token: str | None = None`，`.env.example` 同步（空值）。

**models.py 增加**：
```python
class AdjFactor(Base):
    __tablename__ = "adj_factors"
    __table_args__ = (UniqueConstraint("symbol", "trade_date", name="uq_adj"),
                      Index("ix_adj_symbol_date", "symbol", "trade_date"))
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    symbol: Mapped[str] = mapped_column(String(24), index=True)
    trade_date: Mapped[date] = mapped_column(Date)
    adj_factor: Mapped[float] = mapped_column(Float)
    source: Mapped[str] = mapped_column(String(16), default="tushare")
```

**backtest/adjust.py 契约**：
```python
def tushare_call(token, api_name, params, fields="") -> pd.DataFrame:
    """标准库 urllib POST http://api.tushare.pro；code!=0 抛错。复用 Obsidian 记录的用法。"""

def sync_adj_factors(session, symbols=None) -> dict:
    """对每 symbol 拉 tushare adj_factor（ts_code 形如 600519.SH，需市场后缀转换）→ upsert。
    低积分有频率限制，串行 + 适度 sleep；断点续传（查 max(trade_date) 增量）。"""

def adjusted_close_frame(session, symbol, start, end) -> pd.DataFrame:
    """join daily_bars + adj_factors，返回列 date/close/adj_factor/adj_close(=close*adj_factor)。
    缺 adj_factor 的日期：warning 并用 adj_factor=1.0 兜底（标记 degraded），不静默。"""

def daily_returns(session, symbol, start, end) -> pd.Series:
    """基于复权价的日收益序列（防除权假跳空）。"""
```

**job** `sync_adj_factors`（cron 交易日 18:50，在日线之后）。

**验证**：
```bash
curl -s -X POST localhost:8000/v1/jobs/sync_adj_factors/run | python3 -m json.tool  # 或直接脚本调
# 抽查除权股:茅台某年分红除权日,不复权价有跳空、复权收益连续
.venv/bin/python -c "from alphapilot.db.engine import get_session; from alphapilot.backtest.adjust import daily_returns; ..."
```
**完成标准**：adj_factors 覆盖 ≥95% 有日线的证券；复权日收益在除权日无假跳空（抽查 1-2 只分红股，
复权收益 |日收益|<11% 无异常尖峰）；质量门通过 + adjust 单测（构造除权场景验证复权收益正确）。

⚠ DEVIATION: 2026-07-23 实测本机 Tushare 低积分 token 的 `adj_factor` 配额为每小时 1 次
（业务错误 `code=40203`），无法按原规格逐股完成全市场同步。实现仍先走标准 Tushare HTTP
接口；确认频控后诚实降级为 BaoStock 后复权收盘价（沪深）和 Sina/AKShare 后复权收盘价
（北交所），按 `复权价 / 本地不复权 close` 重建因子，并逐行记录 `baostock-hfq` /
`sina-hfq` 来源，不伪装成 Tushare。凭证请求使用禁用环境代理的标准库 opener，避免本机代理
导致的 HTTP 400，也避免令牌经过代理。

⚠ DEVIATION: 二期审计库保留了 5 只证券共 95 行 `source=mock` 的日线，其中含休市日和与真实
价格量级不一致的数据。为避免污染严格回测，未删除审计历史，而是在同步窗口、因子重建和收益
读取三个边界统一只接受 `AUDITED_DAILY_BAR_SOURCES`，并补回归测试证明 mock 行不会进入回测。

⚠ DEVIATION: 2026-07-23 运行观察发现 18:40 的 `sync_daily_bars`（JobRun `#6750`）超过
10 分钟，18:50 的 `sync_adj_factors`（JobRun `#6781`）已启动，固定时刻先后并不等于上游
已完成。若此时提前固定同步窗口，可能漏掉日线任务后半段刚补齐的当日证券。hotfix 改为在读取
目标窗口前等待最近两小时内的日线任务完成（最多 30 分钟）；若确实等待过上游，则重取最新
交易日并 upsert，以修正同日不复权价被日线任务更新后的因子；超时则带结构化 stats 失败且不做
部分同步。已补等待、超时及最新日刷新单测，cron 仍保持规格要求的 18:50。

**实现与验收记录（2026-07-23）**：

- 全量同步：目标 5,529 只，处理 5,529，只读断点续传跳过 2，只同步 5,527，失败 0；
  新增 1,631,411 行。连同预验收的 2 只样本，库内共 1,631,962 行、5,529 只证券。
- 覆盖核对：有审计日线的证券覆盖 5,529 / 5,529（100%）；逐日 join 覆盖
  1,631,962 / 1,631,962（100%）。来源为 `baostock-hfq` 5,201 只、
  `sina-hfq` 328 只。
- API 断点续传复跑：`POST /v1/jobs/sync_adj_factors/run` 生成 JobRun `#6731`，
  `status=ok`，5,529 只全部 skipped，0 失败，覆盖率 1.0。
- 除权日抽查：600519 在 2025-12-19 的不复权/复权收益为 -1.47% / +0.21%，
  2026-06-26 为 -3.59% / -1.30%；000333 在 2025-11-18 为 -1.30% / -0.68%，
  2026-06-29 为 -1.63% / +3.26%。复权收益均小于 11%，无缺因子降级。
- 验证通过：`ruff check src tests`、`mypy src/alphapilot`、完整 `pytest -q`，
  以及 Tushare 协议/错误、频控降级、upsert/断点续传、mock 隔离、除权收益与 18:50 cron
  的专项单测。

---

## P3.1-S2 PIT 数据访问层 + 信号回放（审计未来函数）

**目标**：解决铁律#1。提供"某历史日只能看到当时数据"的严格访问，并把现有因子引擎改造成可回放。

**先审计**：通读 `engines/factors.py` 的 `_load_financial_factors`，确认它是否用 `available_time`
过滤。**若发现它用了 report_period 或未来数据 → 记 `⚠ DEVIATION` 并修复**（这可能意味着二期
的 composite_scores 本身有轻微未来函数，回测前必须纠正）。

**backtest/pit.py 契约**：
```python
def eligible_universe(session, as_of: date) -> pd.DataFrame:
    """as_of 日可交易股票池:上市≥60日、非停牌(当日有 daily_bars)、按 is_st 标记。
    ⚠ 幸存者偏差:当前 securities 无退市股,标注 has_survivorship_bias=True。"""

def signal_scores(session, as_of: date, weights) -> pd.Series:
    """对 as_of 日回放多因子打分:只用 <=as_of 的日线 + available_time<=as_of 的财务。
    复用 engines/factors 的因子计算(若其非 PIT 安全,在此传入 as_of 上界强制截断)。
    返回 symbol->score(0-100)。这是回测的信号源(而非读 composite_scores 表)。"""

def forward_return(session, symbols, entry_date, exit_date) -> pd.Series:
    """entry->exit 的复权收益(用 adjust.daily_returns 累积)。成交价口径由 engine 决定。"""
```

**验证**：对 2026-07-21 调 `signal_scores`，与库里 `composite_scores` 的 07-21 截面对比（应高度
一致，证明回放正确）；构造一个"未来财务"测试——插一条 available_time 在未来的财务，断言
signal_scores 不使用它。
**完成标准**：回放分数与在线 composite 一致（rank 相关 >0.99）；未来函数单测通过（PIT 截断生效）。

---

## P3.1-S3 交易成本 + A股成交约束

**目标**：铁律#4/#5/#6。

**backtest/costs.py 契约**：
```python
@dataclass
class CostModel:
    commission_bps: float = 2.5      # 双边佣金 万2.5
    commission_min: float = 5.0      # 单笔最低5元
    stamp_duty_bps: float = 10.0     # 印花税 千1,仅卖出
    transfer_bps: float = 0.2        # 过户费
    slippage_bps: float = 5.0        # 滑点(单边)

def trade_cost(notional: float, side: str, model: CostModel) -> float:
    """按方向算成本;买入无印花税,卖出有。"""

def tradable_at_open(session, symbol, d: date, side: str) -> bool:
    """T+1 成交可行性:涨停(open 达涨停价)时 side=buy 不可成交;跌停时 sell 不可;
    停牌(当日无 bar)不可。涨跌停幅度按 board/is_st。"""
```

**验证**：单测覆盖——买入涨停不可成交、卖出跌停不可成交、停牌不可成交、印花税只卖出扣、
最低佣金生效。
**完成标准**：全部约束单测通过。

---

## P3.1-S4 回测引擎核心（walk-forward 日循环）

**models.py 增加**：
```python
class BacktestRun(Base):
    __tablename__ = "backtest_runs"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    name: Mapped[str] = mapped_column(String(64))
    signal_id: Mapped[str] = mapped_column(String(64), default="composite-v1")
    start_date: Mapped[date]; end_date: Mapped[date]
    rebalance_freq: Mapped[str] = mapped_column(String(8), default="5d")  # 5d/10d/20d
    top_pct: Mapped[float] = mapped_column(Float, default=0.1)            # 多头取分数前10%
    params: Mapped[dict[str, Any]] = mapped_column(JSON, default=dict)    # cost_model/universe/weights快照
    status: Mapped[str] = mapped_column(String(16), default="running")
    error: Mapped[str | None] = mapped_column(Text)
    summary: Mapped[dict[str, Any]] = mapped_column(JSON, default=dict)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow)

class BacktestDaily(Base):
    __tablename__ = "backtest_daily"
    __table_args__ = (Index("ix_bt_daily", "run_id", "trade_date"),)
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    run_id: Mapped[int] = mapped_column(Integer, ForeignKey("backtest_runs.id"))
    trade_date: Mapped[date]
    rank_ic: Mapped[float | None] = mapped_column(Float)
    long_ret: Mapped[float | None] = mapped_column(Float)     # 扣成本后
    ls_ret: Mapped[float | None] = mapped_column(Float)       # 多空
    turnover: Mapped[float | None] = mapped_column(Float)
    nav: Mapped[float]; benchmark_nav: Mapped[float]
    n_eligible: Mapped[int]
    group_returns: Mapped[list[Any]] = mapped_column(JSON, default=list)  # 10分层各组收益
```

**backtest/engine.py 契约**：
```python
def run_backtest(session, cfg: BacktestConfig) -> int:
    """核心 walk-forward 日循环,返回 run_id。流程(严守铁律):
    1. 交易日历(trade_cal 或 daily_bars 推断)从 start 到 end
    2. 每逢 rebalance 日 T:
       a. signal_scores(session, T, weights)  # 只用<=T数据(铁律1)
       b. eligible_universe(T) 过滤 + 剔除 T+1 不可成交的票(铁律4)
       c. 目标持仓 = 分数 top_pct 等权;记 rank_ic(分数 vs 前向收益)
    3. 持仓在 rebalance 间按复权日收益累积;换手在调仓日按|Δw|算成本扣减(铁律6)
    4. 成交价用 T+1 开盘(铁律2);T+1 制度用持仓锁定(铁律5)
    5. benchmark: SH.000300 复权净值 + 等权全市场净值
    6. 每日落 backtest_daily;结束写 summary
    异常:单日失败记 error 但尽量续跑;严重(如无数据)则 status=failed。"""
```

**验证**：跑一个短区间（如最近 60 日）小回测，确认 nav 序列连续、benchmark 对齐、无未来函数
（rebalance 日成交价晚于决策日）。
**完成标准**：短回测跑通、落库完整；引擎单测（构造玩具数据验证成交时序 T→T+1、成本扣减、
T+1 锁定）。

---

## P3.1-S5 评估指标

**backtest/metrics.py**：
```python
def rank_ic(scores: pd.Series, fwd_returns: pd.Series) -> float:  # spearman
def ic_summary(ic_series) -> dict:  # mean, std, ic_ir=mean/std, t_stat, positive_ratio
def layered_returns(scores, fwd_returns, n=10) -> list[float]:    # 10分层各组收益
def long_short(layered) -> float:                                  # G10-G1
def performance(nav: pd.Series) -> dict:  # ann_return, sharpe, max_drawdown, calmar
def turnover_stats(daily) -> dict
def calibration(p_up: pd.Series, realized_up: pd.Series, bins=10) -> dict:
    """概率校准:Brier score + 可靠性曲线(预测概率分箱 vs 实际上涨频率)。
    针对 forecast 的 p_up 预测,验证'说70%上涨是否真70%兑现'。"""
```

**验证**：对 S4 的短回测算全套指标；用构造数据验证 rank_ic（完全正相关=1、反相关=-1）、
分层单调性、Brier（完美校准=低分）。
**完成标准**：指标数学正确性单测通过。

---

## P3.1-S6 首次回测 + 结论（本里程碑的价值兑现）

对现有 `composite-v1` 信号跑完整回测（全可用区间），产出**结论报告**：

**backtest/report.py**：`generate_report(session, run_id) -> dict`，含：
- IC 均值/IC_IR/t 统计量/正 IC 占比 → 因子有无预测力；
- 分层收益单调性（Top 组是否显著跑赢 Bottom 组）；
- 多空组合扣成本后年化/夏普/最大回撤；
- vs 沪深300、vs 等权全市场超额；
- 换手率与成本占比；
- 概率校准（若回测 forecast 的 p_up）；
- **诚实结论**：明确写"当前基线因子在此区间是否有统计显著、扣成本后仍为正的 alpha"，
  以及**幸存者偏差、财务 available_time 近似、区间仅 ~1 年**等局限。

**验证**：
```bash
curl -s -X POST localhost:8000/v1/backtest/run -d '{"signal_id":"composite-v1","rebalance_freq":"5d"}' | python3 -m json.tool
curl -s localhost:8000/v1/backtest/{id}/report | python3 -m json.tool
```
**完成标准**：完整回测跑通、报告生成；**无论结论是"有 alpha"还是"无 alpha"都如实呈现**
（铁律：这一步最忌讳为了好看调参数刷结果——若因子无效，如实报告无效，这本身是有价值的发现）。

---

## P3.1-S7 回测 API + 研究页（新增第 9 个功能页）

设计稿只有 8 页，回测是新能力，新增"策略回测/研究"页：
- API：`POST /v1/backtest/run`、`GET /v1/backtest`（列表）、`GET /v1/backtest/{id}`、
  `GET /v1/backtest/{id}/report`、`GET /v1/backtest/{id}/daily`（净值/IC 时序）。
- 前端（沿用 style.css 设计系统 + ECharts）：回测配置面板（信号/区间/调仓频率/成本）+
  运行；结果页——净值 vs 双基准曲线、10 分层收益柱、IC 时序、指标卡（IC_IR/夏普/回撤/
  年化/换手）、校准可靠性曲线、诚实结论与局限。侧栏加导航项。
- 长回测耗时：异步 job 化（run 落 running，前端轮询 status）。

**完成标准**：`npm run build` 通过；页面能发起回测、展示结果与结论、局限如实标注；
无数据/回测中有中文降级；控制台 0 error。

---

## P3.1-S8 里程碑验收

- [ ] 质量门：ruff / strict mypy / pytest 全绿；每步独立提交
- [ ] 复权因子覆盖 ≥95%，复权收益无假跳空
- [ ] PIT 无未来函数（财务 available_time 截断有单测证明）
- [ ] 成本/T+1/涨跌停约束全单测覆盖
- [ ] composite-v1 完整回测报告产出，含 IC/分层/多空/双基准/校准/换手
- [ ] 结论诚实（有/无 alpha 都如实），局限（幸存者偏差/available_time/短区间）显著标注
- [ ] 研究页可用，前端构建通过
- [ ] 安全不变量未触碰（回测是只读研究，不接触交易执行路径）
- [ ] CHANGELOG 0.4.0 + README + BACKLOG 更新

**结论门**：M1 的成败不在于"回测出正收益"，而在于**得到一个可信的答案**——现有因子到底
有没有 alpha。若有，P3 后续可据此上 ML 模型优化；若无，则说明二期的信号需要重做，这个
"及早发现"本身就避免了在无效信号上继续投入。**诚实的负面结论 > 粉饰的正面结论。**

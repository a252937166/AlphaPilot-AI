# P3.3 预备批 · S7 正式运行时入口

状态：**code-complete，未运行真实研究，S7 未标 done**。

## 已实现

- `run_factor_research` 只使用调用方给出的显式多年日期，并自行按交易日切分
  full/train/test；不调用固定 301 日的 `train_test_split`。
- 固定研究 11 个 PIT 有效历史候选因子；`net_inflow_5d` 记录为
  `history_excluded_pit_gap`，`sector_strength` 记录为 `live_only`。
- 在首次数据库 Session 之前，先验证 research 安全态，再针对当前数据库即时重跑 S6
  只读检查；最终外部 PIT 签认证据缺失、失配或未获接受时一律失败关闭。
- full/train/test 与 train correlation 先完成只读长计算，全部成功后用一个短事务统一
  持久化；失败通过 `JobExecutionError` 保留阶段统计。
- JobRun stats 包含请求/有效窗口、每窗逐因子 `n_periods` 和 IC 摘要、阶段耗时、排除
  状态、S6 摘要及 correlation lineage。
- one-shot CLI 默认 detached、`python -u` 日志；本批 runner 固定
  `do_rebuild=False`，不会生成权重。

## 本批验证边界

- 只使用合成 calendar、IC table、correlation 与 S6 pass/block fixture。
- 没有执行真实 `full`/`test` 研究，没有读取 test 结果，没有生成权重文件。
- 正式运行前仍必须先完成 S2、S6 最终检查及架构师外部 PIT 签认，并通过环境变量
  `ALPHAPILOT_S6_EXTERNAL_PIT_EVIDENCE` 指向最终证据。

# P3.3-S2 四出口切换证据

时间：2026-07-26 23:45–2026-07-27 00:00（Asia/Shanghai）

结论：故障旧 LAX 的唯一财务数据已先救回；两台新增美国服务器通过单次真实
BaoStock 探测并完成隔离 worker、timer 和本机回传部署。S2 仍在采集，不标
done、不提交。

## 1. 许可与旧机退出

用户代表 BaoStock 管理员确认新的活跃集合：

- `47.93.234.51`
- `206.237.18.80`
- `38.150.35.34`
- `192.220.61.208`

约束为每个公网 IP 各 1 个进程、1 条连接、每日不超过 40,000 次。
旧 `192.220.61.180` 从许可集合退出。

旧 LAX 切换时 SSH 已 `connection refused`，无法远程 disable timer。退出前已
完成在线救援：

- 快照 SHA-256：
  `8ca22134f83c4210a699d7d02ec7db84b8ae18923e3705caa62962c038f6fb85`
- 305,850 行 / 1,751 股
- `quick_check=ok`、`integrity_check=ok`
- 重复键 0、PIT 缺失 0
- 最后 `JobRun id=1 status=ok`
- `resume_symbol=603607`

本机旧 `com.alphapilot.s2-financial-pull-lax` 已 bootout，plist 可恢复移入
Application Support 的 retired 目录。旧云实例不得再次恢复 S2 timer。

## 2. 新 IP 单次探测

两台均只执行一次：

1. `bs.login()`
2. `query_profit_data(sh.600519, 2024Q4)`
3. `bs.logout()`

结果均为 `error_code=0`、查询 1 行、logout 成功；没有重试、没有
`10001011`。探测计入当日额度，因此新 runner 的长期硬上限为 39,999。

## 3. Seed

统一 seed：

- SHA-256：
  `5d98a9cfb80db92c2f0b24f9d9fc622c63f7d1ff87041d81350d247503baed7b`
- 305,850 行 / 1,751 股
- 五个 metric 各 61,170 行
- 5,531 份 Security.profile
- 负向断点 1,746 股 / 8,800 季
- PIT 缺失 0、重复键 0、`quick_check=ok`
- JobRun / trade_proposals / broker_orders 均为 0

两台接收文件 SHA 与本机一致；首次 online backup 导出均得到相同 SHA：
`25863d2be999f456b5e908db0a155898f44bbe8f81d23d63c56232881a56057b`。

## 4. 四段边界

| 出口 | 区间 | 当前缺失季度 | 估算 provider 请求 |
|---|---|---:|---:|
| 阿里云 | `symbol < 300387` | 39,742 | 161,353 |
| 狗云 | `300387 <= symbol < 600235` | 29,264 | 118,812 |
| US38 | `600235 <= symbol < 603730` | 34,544 | 140,249 |
| US192 | `603730 <= symbol < 1000000` | 34,520 | 140,151 |

四段无交集、无缺口。狗云在无进程、无 `:10030` 连接时，先把旧 cron
可恢复移出 `/etc/cron.d`，再将 runner 原子收紧到 `<600235`，通过
SHA-256、bash 语法和参数复核后恢复 00:05 cron。

## 5. 新 worker

### US38

- root：`/opt/alphapilot-s2-us38`
- service：`alphapilot-s2-financial-us38.service`
- timer：09:05 Asia/Shanghai
- range：`600235 <= symbol < 603730`

### US192

- root：`/opt/alphapilot-s2-us192`
- service：`alphapilot-s2-financial-us192.service`
- timer：09:15 Asia/Shanghai
- range：`603730 <= symbol < 1000000`

共同约束：

- CentOS Stream 9 + 原生 Python 3.11，无 Docker
- 专用非 root `alphapilot-s2`
- `CPUQuota=40%`
- `MemoryMax=256M`
- `MemorySwapMax=384M`
- `Nice=10`
- `TasksMax=64`
- `ProtectSystem=strict`
- `NoNewPrivileges=true`
- research；scheduler/Futu/live/paper/paper_auto 全关
- `trading_halted=true`
- 全部通用代理变量与 BaoStock 专用代理变量清空
- timer active/enabled；oneshot service 初始 inactive；`:10030` 连接 0

美国出口一次 40k 任务曾需约 2 小时以上，因此不再安排 00:10 以避免跨入
约 02:00 的 BaoStock 维护窗，改为白天 09:05/09:15。

## 6. 生产 VPN 不变量

- 两台 sing-box 均保持 active/enabled。
- sing-box unit SHA-256 与部署前一致：
  - US38：
    `9b9e3b1b0e8fe25b3ad13cf345f243298b1741c15cb9911805784acf7b924e83`
  - US192：
    `259fa4ee0a29fb863bd1597eba9392bd9767c679c1e28b8b1ee91ce1ea01c84d`
- US38 的证书 timer 保持 active。
- US192 的 WARP、certbot timer 保持 active。
- 防火墙没有为 S2 增加任何入站端口。

⚠ US38 原 sshd 显式设置 `PubkeyAuthentication no`。已用独立 drop-in
开启公钥认证，经 `sshd -t` 和 reload 后 `effective_pubkey=yes`；没有关闭
密码回退，没有改 SSH 端口或防火墙。此后全部操作只用密钥。

## 7. 本机回传

- `com.alphapilot.s2-financial-pull-us38`：12:05 上海
- `com.alphapilot.s2-financial-pull-us192`：12:20 上海

两条 LaunchAgent 均由每小时分钟门控 + 上海日状态文件驱动，睡眠错过后可补跑。
首轮结果：

- remote/local SHA 一致
- incoming 305,850
- already_present 305,850
- inserted 0
- conflict 0
- trade_proposals 前后 1/1
- broker_orders 前后 1/1
- exit 0

## 8. 质量门

- `ruff check .`：通过
- strict mypy：139 source files 通过
- `.venv/bin/python -m pytest`：768 项通过
- 四出口/回传/SQLite/上海日门专项：32 项通过
- bash/zsh/plist 语法：通过
- `git diff --check`：通过

直接运行 `.venv/bin/pytest` 时，console-script 的 `sys.path` 不包含仓库根，
使两个现有跨包测试在 collection 阶段失败；改用 Python 模块入口后全量通过。
这属于测试入口差异，不是产品失败。

## 9. 自然运行进度

- 2026-07-27 00:08 CST 已只读确认阿里云自然产生 `JobRun id=5`，
  `status=running`、`started_at=2026-07-26 16:05:56 UTC`。实际进程参数仍为
  `symbol < 300387`，仅 1 个 Python worker、1 条 `:10030` 连接；日志已推进到
  `processed=850/1843, inserted=105, failed=0`，未见黑名单或网络错误。
- 同时已只读确认狗云自然产生 `JobRun id=6`，
  `status=running`、`started_at=2026-07-26 16:05:08 UTC`。实际进程参数为收紧后的
  `300387 <= symbol < 600235`，仅 1 个 Python worker、1 条 `:10030` 连接；日志已推进到
  `processed=500/1225, inserted=870, failed=0`，未见黑名单或网络错误。
- 两条运行中 JobRun 的 `stats={}` 是现有 runner 在结束时一次性落 stats 的真实语义；
  请求实数、断点和 `is_complete` 须在自然结束后复核，当前不伪造中间 stats。
- ⚠ 2026-07-27 09:05/09:15 首次 timer 均在 systemd 执行脚本前失败：
  `status=200/CHDIR`。根因是部署器将 worker 根目录设为 `root:root 0750`，
  并把 `mktemp -d` 的 `0700` 模式原样保留给 `app`，导致 `alphapilot-s2`
  服务用户不能进入 `WorkingDirectory`；`.env root:root 0640` 也是后续潜在阻塞。
  两次失败均未创建 JobRun、未建立 `:10030` 连接、未消耗 BaoStock 配额。
- 用户授权“修复并补跑”后，US38/US192 均改为 root 持有、仅
  `alphapilot-s2` 组可读/执行；data/logs/exports 仍由服务用户写，
  `app/.venv/.env` 仍无组写和其他用户权限。两台以服务用户完成 cd/read/exec/write、
  `alphapilot/baostock/pandas/sqlalchemy` 导入、seed SHA、SQLite `quick_check`、
  305,850 行/1,751 股、JobRun/提案/委托 0/0/0、边界与 39,999 上限离线预检。
- 部署器已移到受版本控制的 `scripts/deploy_financial_remote_worker.sh`；
  timer 改为只 enable、不 `--now`，防 `Persistent=true` 在错过时刻后未经确认补跑，
  且新增服务用户预检和普通文件/目录不可组写回归测试。
- 2026-07-27 10:48 CST 两台各只手动启动一次，没有另做 probe 或重试。
  两边均为 `JobRun id=1 status=running`、1 个 Python worker、1 条 `:10030` 连接，
  分片严格为 `600235 <= symbol < 603730` 与
  `603730 <= symbol < 1000000`，无 `10001011/10002007`。
  10:53 CST 的真实写入进度为 US38 307,800 行/1,761 股、US192
  306,390 行/1,754 股；最终请求数、断点和失败统计待自然结束后复核。
- 首轮 12:05/12:20 回传后重算本机覆盖率。

预计剩余约 3.5–4 个额度日。全市场完成后仍须覆盖率、PIT、重复键、
幂等空跑、外部对拍和架构师签字，才能标 S2 done。

## 10. US192 物理服务器替换（2026-07-27）

用户确认旧 `192.220.61.208:62981` 已消失，并指定
`69.5.53.137:46531` 一换一接管逻辑 US192；不是第五出口，边界仍为
`603730 <= symbol < 1000000`。

### 旧机最后可用证据

- 12:20 Asia/Shanghai 回传成功，远端 322,910 行/1,845 股。
- 远端 `JobRun id=1 status=running`，快照 SHA-256：
  `249429455a3c741cec13b8724c513ded69c3f78e923ea79d0546071049753e0a`。
- 本机合并新增 17,060 行和 348 个负向断点，冲突 0。
- 合并后生产库 427,655 行/2,478 股，交易表前后保持 1/1。
- ⚠ 旧机消失后无法补做最终 disable、0 connection 和最终 JobRun 取证；
  该缺口如实保留，旧实例不得恢复 S2 timer。

### 新机与最新 seed

- CentOS 9，1 核、约 960 MiB、20 GB；初检无 AlphaPilot、BaoStock 或 VPN 负载。
- SSH 公钥登录已实测，主机 ED25519 指纹：
  `SHA256:0+6Z94DTvkoAEjqxakbQQ8sHCpak217qoq8eCAIBCYY`。
- 云镜像后台升级自然完成，DNF 返回 Success、RPM 数据库验证通过。
- 新 seed SHA-256：
  `09c9d39737ebc02b9c145926ce32c7d5400dee60339bf35d4651bd98d1562d60`。
- seed 为 427,655 行/2,478 股、五指标各 85,531 行、5,531 份 profile、
  13,523 个负向断点；重复/PIT 异常 0、`quick_check=ok`，
  JobRun/提案/委托 0/0/0。
- 应用包 SHA-256：
  `a68bde5dc9805a1b4763a411a22b5ec7691c4af8e200d09385bd35c93f642f89`。
- 首次 SCP 中断只形成 11 MiB 部分文件，未误判成功；随后使用
  `rsync --partial --append-verify` 续传到 201,285,632 字节，远端 seed SHA
  与本机完全一致后才部署。

### 部署、补跑与回传

- 运行时保持 `alphapilot-s2` 非 root 用户、`CPUQuota=40%`、
  `MemoryMax=256M`、`MemorySwapMax=384M`、`Nice=10`、
  `NoNewPrivileges=true`、`ProtectSystem=strict`。
- research、scheduler/Futu/live/paper/paper_auto 全关，
  `trading_halted=true`；分片与 39,999 上限离线复核通过。
- 只显式启动一次；runner 内置单次真实 query 探测成功，
  `JobRun id=1 status=running`、error 为空，仅 1 worker/1 connection，
  未见 `10001011`/`10002007`。
- 首次增长到 427,850 行/2,479 股；在线快照 SHA
  `5181c16ee99046109c9dc63f12f373d69f1822fbab3fc32af0ed2d27e300e03e`。
- 本机回传新增 195 行和 1 个负向断点、冲突 0，生产库达到
  427,850 行/2,479 股，重复键/PIT 缺失 0，交易表前后仍为 1/1。
- `com.alphapilot.s2-financial-pull-us192` 已改指向新 IP/端口；
  当日日门状态保留，bootstrap 未重复传输且 exit 0。
- 新机 timer active，下一次为 2026-07-28 09:15 Asia/Shanghai。

以上证明物理出口替换和回传链已恢复；运行中的最终 stats 仍待自然完成，
S2 不标 done、不提交。

## 11. 第二个额度日复核（2026-07-28）

15:00 CST 前后只读复核，四台均无 worker 进程、`:10030` 连接 0。

四路 07-28 轮次全部自然退出：`status=ok`、`stopped_for_request_budget=true`、
`is_complete=false`、`symbols_failed=0`、`failures=[]`，未见
`10001011`/`10002007`，边界与四段契约一致。

| 出口 | JobRun | 运行（CST） | 请求/预算 | resume | 远端行/股 |
|---|---|---|---:|---|---:|
| 阿里云 | id=6 ok | 00:06–02:04 | 39,997/40,000 | `002881` | 249,120 / 1,345 |
| 狗云 | id=7 ok | 00:05–01:58 | 39,999/40,000 | `301563` | 272,465 / 1,749 |
| US38 | id=2 ok | 09:05–11:51 | 39,995/39,999 | `600879` | 402,000 / 2,245 |
| US192 | id=3 ok | 09:15–14:23 | 39,998/39,999 | `688393` | 489,645 / 2,978 |

狗云 `quarters_unavailable=5344`、US192 `4195`——创业板/科创板新股历史季度
短，负向断点属预期语义，不是失败。

四路本机回传全部成功（日门状态均为 `2026-07-28`）：阿里云 04:10、狗云
04:25、US38 12:07、US192 12:21，合并冲突全 0，`trade_proposals`/
`broker_orders` 保持 1/1。本机生产库 427,850 → 615,950 行、2,479 → 3,713 股。
US192 的回传发生在其当日任务结束（14:23）之前，任务尾部约 12,650 行按日门
控设计留待 07-29 12:20 回传，属一天滞后而非丢失。

本机质量复核：重复键 0、`available_time` NULL 0、PIT 异常 0、
`provider_pub_date_end_of_day` 占比 0.9997、`quick_check=ok`。只读验收预检
（`run_p3_m3_s2_acceptance.py --as-of-date 2026-07-28`）：覆盖 3,713/5,202、
partial 0、正负重叠 0、未解析 59,656，Gate 如期 blocked 于覆盖率与
幂等空跑证据。

⚠ 新 US192 的 stale `JobRun id=1`（2026-07-27 05:18 UTC 起 `running`、
stats `{}`、无对应进程）继续保留不动；分片幂等审计只取最新一条
`sync_financials` JobRun，该残留不会阻塞最终验收。

验收契约同步：`SHARD_CONTRACTS` 由三分片（aliyun/dogcloud/lax）改为四分片
（aliyun/dogcloud/us38/us192），狗云上界收紧为 `600_235`，US38/US192 预算
`39,999`、US192 上界 `1_000_000`，与四路 runner 实参及 JobRun stats 逐字段
一致；专项测试与最终验收预案文档同步更新。

进度估算：按每路每日 ~247 股，阿里云剩 ~499 股（≈2 日）、狗云 ~238
（≈1 日）、US38 ~775（≈3.1 日）、US192 ~599（≈2.4 日）；整体瓶颈为
US38，预计还需约 3 个额度日。S2 仍不标 done、不提交。

## 12. 逻辑出口更名 us192 → ussea（2026-07-28）

应用户要求，替换机 `69.5.53.137:46531` 不再沿用旧机 `192.220.61.208`
的 US192 名称，统一更名为 `ussea`（US-SEA，机房 `US-SEA-BGP-01`，西雅图）。
本报告 §1–§11 中的 US192 均为更名前的历史名称，证据不回改；分片边界
`603730 <= symbol < 1000000`、预算 39,999、时刻表 09:15 Asia/Shanghai
与全部安全约束逐项未变。

更名在额度日空闲窗口执行（远端无 worker 进程、`:10030` 连接 0）：

- 远端：先 `disable --now` 旧 timer，根目录 mv 为
  `/opt/alphapilot-s2-ussea`，`.env`、`run-financial-backfill.sh`、
  `export-financial-snapshot.sh` 仅做 `us192→ussea`、`US192→US-SEA`
  文本替换并恢复 `root:alphapilot-s2` 属主与 0640/0750 权限；unit 更名为
  `alphapilot-s2-financial-ussea.service/.timer`，旧 unit 与旧 Persistent
  stamp 可恢复移入 `/root/alphapilot-retired-units/`。
- Persistent stamp 以 `cp -p` 迁移，保留上次触发记录（2026-07-28 09:15
  CST）；enable+start 新 timer 后 `list-timers` 验证 LAST 不变、NEXT 为
  2026-07-29 09:15 CST，service inactive、无进程、连接 0，未发生补跑。
- 服务用户预检：`alphapilot-s2` 可 cd `app/`、读 `.env`，
  `alphapilot/baostock/pandas/sqlalchemy` 导入成功；
  `ConditionPathExists` 指向的库文件在位（230,248,448 字节）。
- 更名后远端文件 SHA-256：
  - service：`26c270b53ea3c756e731fd68b7e6a8e78bf8a825219ecaf68e7d134d33f56289`
  - timer：`eaa2c873e555b5ef4801851d31a8247fe53b7ed303abacdddd2a7c5faf17c875`
  - .env：`8268174599047427072bc50a19ff576c3af15a53409ca75243406761c5f91a44`
  - run-financial-backfill.sh：`71a94ef69ce3f0b465871277ba5e2c5cd4f1008b15884595311df17cdd88bbc3`
  - export-financial-snapshot.sh：`5fcf3dbb012dece0bc92f6a8b6d6b49aaa98e79beb0494dcd5b6235de05f398c`
- 本机：回传 LaunchAgent 更名为 `com.alphapilot.s2-financial-pull-ussea`
  （plist 结构 diff 校验仅命名行不同），旧 plist 可恢复移入 Application
  Support 的 disabled-launchagents；日门状态目录随迁并保留 `2026-07-28`，
  bootstrap 后 RunAtLoad 门控静默跳过、exit 0，未重复回传；快照目录更名为
  `data/phase3-s2-ussea`。
- 仓库：`SHARD_CONTRACTS` 分片键 `us192→ussea`（blocker 代码相应为
  `IDEMPOTENT_EMPTY_RUN_USSEA`），`config/financial_ussea.*`、
  `scripts/run_financial_ussea_backfill.sh` 由同名 us192 文件更名并同步内容，
  专项测试与最终验收预案同步；仓库内不再有 us192 活动引用。

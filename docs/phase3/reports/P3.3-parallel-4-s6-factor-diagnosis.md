# P3.3 等待期并行项 4：S6 只读体检与因子诊断

日期：2026-07-25

## 结论

- S6 体检工具与前端因子诊断页已 code-complete，质量门与浏览器验收通过。
- 主库预检结果为 **BLOCKED**，因此没有把 S6 标为 done，也没有进入 S7/S9。
- 体检全程使用 SQLite `mode=ro`、`query_only=true`；未创建体检 JobRun，未修改业务表。

## S6 只读体检

命令：

```bash
PYTHONPATH=src .venv/bin/python scripts/run_p3_m3_data_health.py \
  --db data/alphapilot.db \
  --json-out docs/phase3/reports/P3.3-S6-preflight.json \
  --markdown-out docs/phase3/reports/P3.3-S6-preflight.md
```

真实结果：

- `daily_bars` 8,466,815 行；最新广覆盖截面 5,530；95 行 `source=mock` 不在审计白名单。
- 111 行历史 Sina/北交所日线 OHLC 为 0，触发价格合法性硬门；只报告，不在只读体检中清洗。
- `adj_factors` 8,463,893 行，非法来源/因子/重复键均为 0；日线与复权键双向缺口均为 0。
- `valuation_daily` 8,455,673 行、5,531 股；PIT 异常、非法来源、重复键均为 0。
- `financial_indicators` 当前仅 773/5,531 股，覆盖率 13.9758%，S2 尚未完成。
- `net_inflow_5d` 在 2025-07-31 与 2026-01-26 缺历史成分 PIT，可测探针仅 1/3；不借用当前成分。
- 尚无外部 PIT 对拍签认证据。

因此闸门包含：`DAILY_BARS_SOURCE`、`DAILY_BAR_VALUES`、`S2_FINANCIAL_COVERAGE_INCOMPLETE`、`FACTOR_NET_INFLOW_5D_UNAVAILABLE`、`EXTERNAL_PIT_PAIRING_PENDING`。

体检证据协议使用版本化 JSON，并以当前完整抽样快照的 canonical manifest SHA-256 防止空心签认和重放；证据严格校验逐字段 local/external/pass/tolerance，报告仅保留脱敏元数据。独立复核实测修改抽中值后旧证据会被拒绝。

## 因子诊断页

- 默认只加载 M3 `train`，精确窗 `2019-01-02 → 2024-04-16`，`research_run_id=15004`；test 窗保持封存。
- JobRun cohort 同时绑定 stage、run、expected factors，并逐行校验 timebox 与 `stats.results`；同窗额外 IC 不会混入。
- 当前只展示 6 个已测价量/估值因子；资金流和 5 个财务因子保持 `not_evaluated`，`sector_strength` 保持 `live_only`。
- 本次 JobRun 没有带 lineage 的相关矩阵，因此显式 `unavailable`，拒绝读取同窗旧记录。
- M2 `full/compare` 仅在用户主动切换后请求；默认 M3 浏览器请求未触碰 `full/test/compare`。

## UI 与质量门

- 桌面与 390 × 844 移动端均无页面横向溢出。
- 移动端顶层研究 tab 与窗口选择器高度均为 44px；M3/M2 按钮高度约 50.6px。
- 浏览器控制台：0 error、0 warning。
- 后端 API 真实返回：JobRun 15004、6 个 measured、unexpected measured 0、correlation available=false。
- `pytest -q tests/test_phase3_backtest_api.py tests/test_phase3_diagnosis.py`：17 passed。
- `pytest -q tests/test_phase3_m3_data_health.py`：17 passed。
- `ruff check`：通过。
- `mypy src/alphapilot`：135 个源文件通过。
- `npm --prefix apps/web run build`：通过；仅保留既有 1.4 MB chunk-size warning。
- `git diff --check`：通过。

## 边界

- 本工作项没有查看或写入 M3 test 结果，没有调整因子权重。
- 没有清理 95 行 mock 或 111 行零价日线；它们是 S6 发现的待处置数据问题，不属于本只读工作项授权范围。
- S2 继续未标 done；S6/S7/S9 继续被真实闸门阻断。

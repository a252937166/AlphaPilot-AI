# P3.3-S6 最终体检与外部 PIT 终轮记录（2026-07-31）

状态：**BLOCKED，仅等待外部 PIT 方法学裁定；S6 未标 done，S7 未启动。**

## 1. 本地自动体检

使用已提交的 `p3.3-s6-v4` 只读体检器重新扫描生产库：

- `automated_checks_pass=true`；
- 唯一 blocker 为 `EXTERNAL_PIT_PAIRING_PENDING`；
- `daily_bars` / `adj_factors` 双向缺键均为 0；
- 财务覆盖 5,203 / 5,203 个 BaoStock 支持标的，重复键、PIT 异常均为 0；
- 11 个历史候选因子的三个固定 PIT 探针全部 `sufficient`，最小截面为
  `pe_percentile=3,931`，其余候选均不低于 4,949；
- S5 历史子集为 `31,702 = 131 × 242` 行，区间
  `2025-07-25..2026-07-24`，全部为 `futu-daily`，矩形缺口 0、窗内混源 0；
- 历史窗后的 136 行 `em`（2026-07-27、2026-07-29）是白名单内 live-forward 增量，
  不再误判为 S5 历史污染。

财务原始单元格的 provider-null 诊断没有隐藏：

- `ocf_to_profit` 的 depth / cross-year / freshness 为
  `0.723971 / 0.856674 / 0.729573`；
- `revenue_yoy` 为 `0 / 0.850493 / 0`，并显式标注
  `semiannual_q2_q4_from_baostock_mb_revenue`；
- 上述 6 项保留为 `blocking=false` 的结构化 warnings。原因是 S6 规格的硬门是
  S2 键闭环、PIT、来源、重复键和固定 `factor_zscores` 截面，而不是把 provider 合法空值
  误判成漏采。

报告与哈希：

- `P3.3-S6-final-preflight-20260731.json`
  - SHA-256 `2064586a9321c7ab8b321cd82ec5c3edcd5449984dc3a2822556a3f65eb620e3`
- `P3.3-S6-final-preflight-20260731.md`
  - SHA-256 `2285bb704154bae209e636b47967083dfbc5fce2be436d11a0cd31c6a7da4e01`
- PIT manifest SHA-256
  `fb9c888e7a10f7b1ef28e7a447b0e2b53df739f6acdbedb1ba95d1d41cbfb0bd`

## 2. 前置缺口与防复发

- 三个日线/复权缺键均为新北交所股票 `920238` 的
  `2026-07-28..2026-07-30`。一次定向 Tushare 同源查询补入 3 行后，双向缺键归零，
  `PRAGMA quick_check=ok`。
- 根因是两只新股共用 Tushare 每小时 1 次额度，排序靠前的股票先消耗额度，导致
  `920238` 连续饥饿。commit `e5faa77` 使新 BSE 首建直接使用 `sina-hfq`；既有 Tushare
  历史只有在 Sina 全日期覆盖、全部重叠锚点严格一致时才允许整段原子迁移。
- 生产迁移真实执行：5 个审计日全部从 `tushare` 统一迁至 `sina-hfq`，
  `rows_inserted=0`、`rows_updated=5`、失败 0；迁移后双向缺键仍为 0，
  `quick_check=ok`。
- commit `ef12bbc` 修正两个 S6 体检误报：财务 provider-null 诊断与
  S5 历史窗 / live-forward 来源边界。相关 external PIT + data-health 测试 37 项通过，
  Ruff 与 strict mypy 通过。

## 3. 外部 PIT 终轮 plan

无网络 plan 通过：

- 固定 15 个样本：日线/复权 5、财务 5、估值 5；
- `max_tushare_calls=0`；
- BaoStock 明确列入 forbidden sources；
- `ready_for_trial=true`、`network_called=false`、`secrets_included=false`。

证据：

- `P3.3-S6-external-pit-final-plan-20260731.json`
- SHA-256 `e7f9152d220d32b3dda44d991c306a7959073848dbfc50418a967948801f8e3a`

## 4. 外部 PIT 终轮 trial

终轮只执行一次，未重试、未换样本、未放宽容差：

- `match=9`、`mismatch=4`、`unavailable=2`；
- `eligible_for_v2_signing=false`、`approved=false`；
- 日线/复权：4/5 match。计划的 Futu 路径运行时全部诚实降级到审计白名单内的 Sina；
  原始 close 5/5 一致，`600782 / 2026-01-30` 的相对复权因子为
  `local=10.631079`、`external=10.611167606819519`，超过冻结容差；
- 财务：0/5 match。两条 `net_profit_yoy` 与一条 `roe` 存在跨源定义/数值差异；
  两条 `revenue_yoy` 样本因东财 F10 没有与 BaoStock
  `derived.profit.MBRevenue_yoy` 同定义的可审计映射而为 `unavailable`；
- 估值：5/5 match，PE/PB/PS 与 available_time 全部一致。

证据：

- `P3.3-S6-external-pit-final-trial-20260731.json`
- SHA-256 `40f310a4c94e550cac33616fbe2d8ffc189cb48cd259a2913fe3379bae424099`

运行后已确认：

- Futu quote context 已关闭，无遗留 trial 进程；
- 本机到 BaoStock `:10030` 的连接为 0；
- Tushare 调用为 0；
- `trade_proposals / broker_orders` 仍为 `1 / 1`；
- `trading_mode=research`、`paper_auto=false`、`live=false`、
  `unlock_trade` 端点未暴露。

## 5. 阻断与交接

本轮不能执行 `sign`，也不能把 trial 改名或包装成 evidence v2。架构师需要先裁定：

1. 跨源复权因子的独立审计，是继续要求不同供应商复权值在当前冻结容差内完全一致，
   还是改为另一种预先声明、能解释现金分红口径差异的审计合同；
2. BaoStock `growth.YOYNI` / `profit.roeAvg` 与东财 F10 字段是否属于同定义；
   若不同，应指定可审计的精确定义来源或调整“外部数值一致”合同，不能事后放宽容差；
3. `revenue_yoy` 应使用什么非 BaoStock 来源重算同定义的累计主营收入同比。

裁定前：**S6 不标 done，S7/S9 保持冻结，test 窗不读取，权重不变。**

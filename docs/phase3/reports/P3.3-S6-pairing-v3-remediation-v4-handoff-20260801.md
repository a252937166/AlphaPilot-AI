# P3.3-S6 pairing-v3 拒签整改交接 v4（2026-08-01）

## 结论边界

本轮只关闭上一份架构师 `BLOCKED` 复核中的机器可修复项，不代替独立人工签名：

```text
S6 = blocked
S7 = not_started
test window = sealed
production trust anchor = pending
```

候选仍为：

```json
{
  "approved": false,
  "reviewer_role": "pending",
  "reviewed_at": null
}
```

实现者未代填身份、未标 S6 done、未启动 S7、未生成 v3 权重、未提交 Git。

## P0-1：001260 半档舍入来源

`001260 / 2026-05-27` 不再依赖本地把 `20.285` 按 `ROUND_HALF_UP`
量化为 `20.29`。本轮新增深交所 2026 年 5 月市场统计月报及其“分红派息配股”
原始 GBK 表，唯一目标行是：

```text
证券代码=001260
证券简称=坤泰股份
现金息总额=24,725,000
每股派息 DPS=0.215
除权除息日=2026/05/27
股权登记日=2026/05/26
除权除息参考价=20.290
前收盘价=20.500
```

官方入口与原表：

- `https://www.szse.cn/market/periodical/month/t20260605_620906.html`
- `https://docs.static.szse.cn/www/market/periodical/month/W020260605534753848014.html`

原表字节 SHA-256：

```text
de031d6f782d1511ae0f7d8d9e0488d00f09f92e42d449155725415b33284f9d
```

候选现在直接使用：

```text
event_formula_id = official_reference_price_ratio_v1
pre_close = 20.50
official ex-reference = 20.29
rounding_provenance = exchange_published_reference_price_local_rounding_none
event_multiplier = 20.50 / 20.29
```

source rebuilder 同时冻结入口页、原始表、请求元数据、脱敏响应头、唯一目标行和嵌套
`SHA256SUMS`；builder 与 validator 都会独立从原始 GB18030 HTML 解析唯一行。修改原表后
即使重绑外围哈希，语义负测仍会 fail closed。

> ⚠ DEVIATION（交付前内部复核）：首次重建虽冻结了正确官网入口 body，但 request metadata
> 曾误写为同月“市场总貌”静态页 URL。独立复核在交付前发现 URL-body 不一致后，该 source、
> candidate 和临时包全部作废；rebuilder 常量改为真实官网入口，并新增 request URL 与
> target extract `entry_url` 相等的回归断言。本文及最终包只引用修正后重新双构建的哈希。

## 新候选与来源闭包

两次全新目录构建结果逐字节一致。实际存在的六层清单全部闭合：

| 清单 | 条目数 |
|---|---:|
| raw-source root | 205 |
| complete-unfiltered-announcement-inventory | 64 |
| 001260 official reference | 8 |
| 600782 official reference | 17 |
| git-chain | 23 |
| price-tick-rules | 29 |

关键哈希：

| 对象 | SHA-256 |
|---|---|
| contract 原件 | `a16995e6545ddba7fc03d917ae6cc8d9ab19b7903f1d21c5694b1c0167b1b951` |
| source manifest | `5c76c933758226d961ebbc18c353bfdc2e0f21884f535da600d6aa54b3712788` |
| raw-source 根 `SHA256SUMS` 文件 | `e683885f9a532da6f171f6543dd87337c56af51c0659c3cbab15960c5a259181` |
| candidate 文件 | `094c21eb7921a0c57c35ba97247a7b69faeada0421ff1929ca5d4701c388fd0c` |
| candidate canonical payload | `45358d1508e7ac6e71a7df25990aba1a908fc7c5108dd4253cc1e05698d520ae` |
| machine validation | `57f70704905ba84b371fc7f3432ce28967e9e67a5ae9b6517fc188da6109ef3d` |
| frozen local preclose | `cb5aee5a171b0a1c06e35ccf1cb3d3fdc941249781b5441f598102c7db650bbd` |
| final trial | `40f310a4c94e550cac33616fbe2d8ffc189cb48cd259a2913fe3379bae424099` |

固定 15 个 business key 与原容差不变，机器结论仍是：

```text
sample_count=15
numeric_match=5
formula_match=8
expected_unavailable=2
unresolved=0
generic_unavailable=0
ambiguous_mapping=0
schema_hash_integrity_errors=0
```

## P0-2/P0-3：候选之后的只读门与机器不变量

新增 `p3.3-s6-presign-gate-v1`。它先验证当前未签候选，再用 SQLite
`mode=ro + query_only=true` 精确回查冻结 preflight 中相同 15 个 business key，不重新抽样；
同时生成 factor、weight、test-window guard、交易安全代码与有效配置的基线/当前逐文件 SHA。

正常 scheduler 会每 30 秒写 JobRun。第一次不暂停 scheduler 的试跑因
`database changed during the pre-sign gate` 正确 fail closed，未生成报告。最终取证只在
有界窗口内暂停 scheduler，API 与 OpenD 保持运行；命令设退出 trap，无论成功失败都恢复
scheduler。该操作只稳定读快照，不修改 S6 数据或交易状态。

盘后输入在首次稳定门前仍有两个真实中间态：

- `sync_valuation_daily #40702` 因 DNS 中断仅完成 4,487 股；幂等续跑
  `#43904 status=ok / is_complete=true`，跳过 4,487 股并补 1,049 行，最终 5,535 股覆盖。
- `688825 / 2026-07-31` 复权键因 Tushare `1 次/小时` 频控缺失；首次定向调用
  `#43983` 仍为 `40203`，未伪造成功。完整冷却后最终定向 JobRun：
  `#44039 status=ok / synced=1 / rows_inserted=1 / failed_count=0 /
  source=tushare`。

稳定后的候选绑定报告：

| 字段 | 值 |
|---|---|
| 报告文件 SHA-256 | `1fee14d59bff82f951a620f67324540c6ad954baeef43c90cbd3c6385de12d89` |
| generated_at | `2026-08-01T06:23:06.691565+00:00`（`14:23:06.691565+08:00`） |
| candidate canonical SHA-256 | `45358d1508e7ac6e71a7df25990aba1a908fc7c5108dd4253cc1e05698d520ae` |
| PIT manifest | `fb9c888e7a10f7b1ef28e7a447b0e2b53df739f6acdbedb1ba95d1d41cbfb0bd` |
| frozen business keys | `15` |
| exact-key difference count | `0` |
| database | `open_mode=ro / query_only=true` |
| local automated data checks | `pass` |
| factor_diff_count | `0` |
| weight_diff_count | `0` |
| test_window_access_count | `0` |
| safety | `research=true / paper_auto=false / live=false / trading_safety_gates_unchanged=true` |
| presign_machine_gate_pass | `true` |
| ready_for_signature | `true` |
| ready_for_s7 | `false` |

报告诚实注明：SQLite 不能追溯任意未持久化的历史只读 `SELECT`；`test_window_access_count=0`
证明的是当前及候选后机器可观察的 test 持久行、正式 S7 JobRun 和新 backtest run 均为 0，
不夸大为操作系统级全历史审计。

## 安全与质量门

- `trade_proposals / broker_orders = 1 / 1`
- scheduler、API、OpenD 在稳定门后均恢复健康
- `PRAGMA quick_check = ok`
- `research=true`
- `paper_auto=false`
- `live=false`
- `unlock_trade` 仍永久屏蔽
- `factor_weights_v3.yaml` 仍不存在
- production signed-file trust anchor 仍为 `None`

质量门：

```text
PYTHONPATH=. pytest -q: pass（1 skip，仅既有 warnings）
ruff check .: All checks passed
mypy --strict src scripts: 179 source files, 0 issues
git diff --check: pass
```

## 仍需独立人工架构师完成

本轮关闭的是 P0-1、P0-2、P0-3 的机器整改；P0-4 必须由真实独立人工完成：

1. 验证 ZIP 外层 SHA、CRC、路径安全和顶层 `SHA256SUMS`。
2. 复核 001260 官方原始表唯一行、固定 15 个 business key、完整公告窗口及 presign
   不变量报告。
3. 若同意，只在候选副本填写：

   ```json
   {
     "approved": true,
     "reviewer_role": "可识别的独立人工架构师身份/角色",
     "reviewed_at": "带时区 ISO-8601 时间"
   }
   ```

4. 通过可信渠道返回完整签名文件及整文件 SHA-256。
5. 实现者收到签名后才可固定 production trust anchor，并运行签名后 release gate。

在签名和 trust anchor 到位前，正式状态仍是：**S6 blocked、S7 not started、不提交签认
commit。**

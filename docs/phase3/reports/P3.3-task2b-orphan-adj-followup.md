# P3.3 Task 2B follow-up hotfix：清理孤儿 Sina 复权因子

> 这是提交 `6e35a01`（P3.3-T2B）的后续修复与偏差说明，不改写原始
> `P3.3-task2b-sina-zero-price-repair.json` 证据。原实现删除了 111 条无效
> `daily_bars`，但没有在同一事务删除对应的 `adj_factors`，因此留下 111 条
> 无日线可配对的 `sina-hfq` 记录。

## 权威范围与写前核对

- 权威键来源：
  `docs/phase3/reports/P3.3-task2b-sina-zero-price-repair.json` 中
  `action=delete` 的全部行。
- 权威证据 SHA-256：
  `4115f45e67d20796cae9c5ec9a388e2e4a234931bd059425735e64538bb0f2d9`。
- 权威键：111 个唯一 `(symbol, trade_date)`，30 股，
  `2019-01-02` 至 `2020-11-16`。
- 写前生产库的完整 orphan 集合与权威键集合精确相等：111/111；
  多一条、少一条、重复键、来源变化或范围漂移都会 fail closed。
- 111 条 orphan 的 `source` 全部为 `sina-hfq`。
- 写前 `trade_proposals/broker_orders` 必须精确为 `1/1`，不是仅检查前后不变。
- 默认运行是只读 dry-run；只有显式传入 `--apply` 才允许变更。

## 生产变更证据

- 结果 JSON：
  [`P3.3-task2b-orphan-adj-followup.json`](P3.3-task2b-orphan-adj-followup.json)。
- 执行状态：`applied`；111 → 0，实际仅删除 111 条权威范围内的
  `sina-hfq` orphan。
- SQLite online backup：
  `data/backups/alphapilot-before-orphan-sina-adj-cleanup-20260725T205024Z.db`。
- 备份 SHA-256：
  `fc1bebd4dbb6b4adbf5d3e6ac4512095c33df24d4f9f61869c97e676befc41e5`
  （落盘后重新计算，与结果 JSON 一致）。
- `PRAGMA quick_check=ok`。
- `trade_proposals/broker_orders`：`1/1 → 1/1`。
- `adj_factors` 重复 `(symbol, trade_date)` 组：`0 → 0`。
- `daily_bars` 重复 `(symbol, trade_date)` 组：`0 → 0`。
- 全库 `daily_bars` 无匹配复权因子基线：`2827 → 2827`，本次清理没有扩大
  日线侧缺口。
- 幂等复跑：`status=already_clean, before=0, deleted=0, after=0`，不再创建备份。
- 完成时间（UTC）：`2026-07-25T20:51:49.007516+00:00`。

## 防复发

`repair_invalid_sina_daily_bars` 现在会在删除仍为零价的 Sina 日线时，于同一
SQLite 事务内删除同键且来源为 `sina-hfq` 的复权因子；如果发现同键复权因子
来自其他来源，则拒绝变更。目标测试覆盖 dry-run、apply、幂等、权威集合漂移、
安全表不是精确 `1/1` 以及日线/复权同事务删除。

## S6 只读复跑

S6 使用 `mode=ro` 与 `query_only=true` 写入临时报告后复核：

- 审计区间日线缺复权：0；
- 审计区间复权缺日线：0；
- `DAILY_BARS_SOURCE` 与 `DAILY_BAR_VALUES` 均未重新出现；
- 仍仅由与本修复无关的
  `S2_FINANCIAL_COVERAGE_INCOMPLETE`、`EXTERNAL_PIT_PAIRING_PENDING`
  阻断 S7。

本次没有访问 BaoStock、Futu 或交易接口，没有创建提案、委托或交易，也没有
改变三闸或回测权重。

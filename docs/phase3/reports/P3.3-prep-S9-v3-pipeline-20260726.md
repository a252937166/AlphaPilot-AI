# P3.3 预备批 · S9 v3 管线参数化

状态：**code-complete，未运行 S9，未生成真实 v3 权重**。

## 已实现

- `rebuild_weights` 支持显式 `version` / `signal_id` / `output_path`；既有默认仍为
  `v2.0.0` / `composite-v2`。非默认身份未给显式输出路径时在研究开始前失败关闭。
- S9 路径固定传 `v3.0.0` / `composite-v3`；合成测试只把 YAML 写入 pytest 临时目录。
- 回测引擎增加 `composite-v3 → config/factor_weights_v3.yaml` 显式路由；文件缺失时在创建
  `BacktestRun` 前明确报错，不产生孤儿 `running` 记录。
- API 请求模型接受 v3；保留 `window` 与显式日期互斥。合成测试证明 v3 显式日期路径不会
  调用固定 301 日的 `train_test_split`。
- 正式 S7 JobRun lineage 可识别为 `m3_s7_formal`。同一结束日同时存在旧 301 日窗和多年窗时，
  catalog 默认选择有正式 lineage 的多年窗；精确查询严格按 start/end 取值。
- 前端 M3 诊断只在响应 `available=true`、`selection.exact_window=true` 且响应起止日期与
  选中窗口完全一致时展示；否则清空结果并中文降级。

## 本批验证边界

- 未创建 `config/factor_weights_v3.yaml`，未读取真实 test 结果，未运行多年 IC 或回测。
- 页面没有提前开放 v3 启动入口；真实 v3 文件只能在 S9 唯一一次 train 成型时生成。
- v1/v2 历史比较协议与固定 301 日 helper 均保留，不改变 M2 审计语义。

# P3.3-S6 pairing-v3 拒签整改交接 v2（2026-07-31）

## 状态

本交接物仍是 **未签名候选**，不是 S6 通过证据：

```json
{
  "approved": false,
  "reviewer_role": "pending",
  "reviewed_at": null
}
```

- `S6 = blocked`
- `S7 = not_started`
- test 窗继续封存；正式 `factor_research` JobRun 为 0
- 因子、权重、回测信号路由未修改
- `trading_mode=research`
- `paper_auto_trading_enabled=false`
- `live_trading_enabled=false`
- `unlock_trade` 仍在 `PERMANENTLY_BLOCKED_METHODS`
- `trade_proposals / broker_orders = 1 / 1`，未新增交易对象

原候选和架构师 BLOCKED JSON/Markdown 均保留在 `docs/`，没有回写或伪造签名。

## 拒签项整改结果

### P0：公司行动窗口不完整

对原 5 个固定 business key 和原冻结窗口重新获取交易所官方公告清单：

| 股票 | 窗口 | 页数 | 原始公告 | 实施且影响复权 | 非实施/非复权 | 未决候选 |
|---|---|---:|---:|---:|---:|---:|
| 000831 | 2023-05-29 → 2026-07-30 | 7 | 337 | 3 | 334 | 0 |
| 001205 | 2024-05-21 → 2026-07-30 | 9 | 403 | 3 | 400 | 0 |
| 001260 | 2025-10-29 → 2026-07-30 | 2 | 52 | 1 | 51 | 0 |
| 600648 | 2025-06-16 → 2026-07-30 | 2 | 111 | 1 | 110 | 0 |
| 600782 | 2026-01-30 → 2026-07-30 | 1 | 59 | 1 | 58 | 0 |
| **合计** |  | **21** | **962** | **9** | **953** | **0** |

所有请求均满足：

- 精确起止日期，不接受日期超集；
- SZSE `searchKey` absent/empty；
- SSE `keyWord` empty、`reportType=ALL`、`reportType2` absent/empty；
- 每页仅 1 次请求、0 重试、固定官方端点、无 fallback/source shopping；
- 页码连续、总数一致、962 个公告 ID 唯一；
- 股票代码与发布日期逐行落在冻结窗口。

版本化 taxonomy 覆盖现金/送转分配、配股、拆股、并股、股改及其它价格调整候选。
builder 与独立 validator 都会重新解析全部 raw JSON；逐条分类结果同时绑定 canonical
SHA-256。任何过滤请求、缺页、重复 ID、额外实施事件或未知高风险标题均 fail closed。
`600782` 的上交所除息参考价原始响应也由独立 validator 重新读取，必须唯一命中
`A_STOCK_CODE=600782 / A_DIV_DATE=20260605`，并逐值核对
`PRE_CLOSE_PRICE=2.64 / A_BEFR_TAX_DIV=0.135`，不能只依赖 manifest 描述字段。

首次开发试跑因 SSE 第二页 `beginPage` 参数错误被语义校验拒绝，该目录未进入证据链；
修正后在全新目录完成正式固定运行。失败事实未删除，也未把错误页伪装为成功。

### P0：嵌套来源哈希未闭合

- 原 25 个 header 和本轮新增 23 个 header 共 48 个，全部转换为确定性 allowlist JSON。
- 仅保留 HTTP status、`content-type`、`content-length`、`etag`、`last-modified` 及内容寻址元数据。
- Cookie、会话标识、动态 Date、Server、连接、CDN/trace、`x-*` 等值全部剔除。
- 全包扫描 `Set-Cookie/JSESSIONID/acw_tc/cdn_sec_tc/Authorization/Bearer` 命中 0。
- root、announcement capture、600782 reference、git-chain、price-tick-rules 共 5 层
  `SHA256SUMS` 分别闭合为 `196 / 64 / 17 / 23 / 29`，缺失、额外、哈希不匹配均为 0。
- 固定合同原件进入 source bundle：
  `a16995e6545ddba7fc03d917ae6cc8d9ab19b7903f1d21c5694b1c0167b1b951`。

### P1：600782 舍入来源

不再把 `2.64` 描述为本地 `ROUND_HALF_UP` 推导值。

上交所官方利润分配接口原始响应的唯一目标行直接给出：

```text
A_STOCK_CODE=600782
A_DIV_DATE=20260605
A_BEFR_TAX_DIV=0.135
PRE_CLOSE_PRICE=2.64
OPEN_PRICE=2.67
```

原始响应 SHA-256：

```text
9adcd4915bd55b63f6532fcddea090b4242842e866e7d6790e61668e305c36cf
```

该事件改用 `official_reference_price_ratio_v1`：

```text
pre_close = 2.77
official ex-reference = 2.64
local rounding = none
event multiplier = 2.77 / 2.64
```

`price_tick=0.01` 仍作为行情/规则元数据存在，但不再承担 `2.635 → 2.64` 的本地舍入证明。

### P1：无效时间戳

最终 source manifest 使用合法显式 `+08:00` ISO-8601。loader 会拒绝 `:z`、无时区和
无法解析的 offset。builder 复用 source manifest 的固定生成时间；连续两次构建得到完全相同的
candidate 与 machine-validation 文件 SHA。

## 新未签候选绑定

| 对象 | SHA-256 |
|---|---|
| contract 原件 | `a16995e6545ddba7fc03d917ae6cc8d9ab19b7903f1d21c5694b1c0167b1b951` |
| raw-source `SOURCE-MANIFEST.json` | `44ad9ff664e98445dec3cbb6a550c37c3f4b922f0e4158997ea4b90a6d19f239` |
| raw-source 根 `SHA256SUMS` 文件 | `7a4599790b4270ec84f3f05148b0711af8fdac899426cc628bf54090092be85a` |
| `pairing-v3-candidate.json` 文件 | `39ecba66258468db524211aa1c50a49c49dde84ca2af517c83919f15ff54117e` |
| candidate canonical payload | `8ec5673d4541c9aecd799e9763bcb4f900d41574e58024b6d94bf6683b58f139` |
| `machine-validation.json` | `721a227bed18ba425a1416d70e9907f0a40595ee9a04eae194a93264afcacedb` |
| `frozen-local-preclose.json` | `794fc2b0ffd200edabaf1260504d0505c0777527974562f31193d25a59fe2778` |

机器结果保持固定 15 个业务键和原 verdict 分布：

```text
sample_count=15
numeric_match=5
formula_match=8
expected_unavailable=2
unresolved=0
generic_unavailable=0
ambiguous_mapping=0
schema_hash_integrity_errors=0
```

生产 trust anchor 仍为 `None`。把本未签候选传入生产门会按预期抛出：

```text
ValueError:
pairing-v3 independent architect signed-file trust anchor is pending
```

## 质量门

专项 76 项测试通过：

- pairing-v3 builder：14
- external PIT adjudication：24
- S6 data health：24
- official inventory capture：5
- source bundle rebuild：9

静态检查：

```text
ruff: All checks passed
mypy src/alphapilot: 149 source files, 0 issues
mypy strict on S6 scripts/tests: 7 files, 0 issues
git diff --check: pass
```

## 本地 S6 数据门

上一份架构师已核对的只读报告继续保持：

```text
database.open_mode=ro
database.query_only=true
automated_checks_pass=true
ready_for_s7=false
blockers=["EXTERNAL_PIT_PAIRING_PENDING"]
```

2026-07-31 19:04 CST 曾尝试生成更新报告，但恰逢正常盘后
`sync_daily_bars / sync_adj_factors / sync_valuation_daily` 运行中，数据库处于当日分批写入的
中间态，暂时报 `DAILY_ADJ_KEY_MISMATCH`；该报告已明确排除，不作为验收证据，也未修改或停止
任何生产任务。应在三项任务自然结束后再生成新的只读报告。

## 仍需人工完成

技术整改不会替代独立人工签名。架构师仍需：

1. 在离线、内容寻址的交付包中复核固定 15 个业务键、962 条公告全窗及 PDF/DOCX 原文边界；
2. 若同意，仅修改候选三个顶层字段：

   ```json
   {
     "approved": true,
     "reviewer_role": "可识别的独立人工架构师身份/角色",
     "reviewed_at": "带时区 ISO-8601 时间"
   }
   ```

3. 通过可信渠道返回完整签名文件及整文件 SHA-256；
4. 实现者将该整文件 SHA 固定为 production trust anchor 后，重跑 S6 只读门；
5. 只有 `ready_for_s7=true` 后，才可标 S6 done、独立提交并启动 S7。

在此之前，正式结论仍是：**不标 S6 done，不提交 S6 签认 commit，不启动 S7。**

# P3.3 任务 2B：新浪北交所零价日线处置

执行时间：2026-07-26 04:12–04:15 Asia/Shanghai

数据库：`data/alphapilot.db`

## 根因与处置

- 异常范围：111 行、30 只北交所股票、2019-01-02 → 2020-11-16，来源均为 `sina`。
- 库内特征：111 行 OHLC 全为 0，但成交量、成交额均大于 0，不能定性为无成交停牌占位。
- 新浪直连复查：逐股票重拉覆盖全部 111 个日期，每个日期均返回记录，但 OHLC 仍全为 0；
  因此属于新浪历史价格字段缺陷。
- 可真实修复：0 行。
- 删除：111 行。未用 `amount / volume` 反推或伪造价格。
- 删除后：全库非法 OHLC 行 0，`source=mock` 行 0。

完整的库内原值、同期新浪返回值、分类与动作见
[`P3.3-task2b-sina-zero-price-repair.json`](P3.3-task2b-sina-zero-price-repair.json)。

## 网络与幂等证据

- 进程启动时显式清除继承的 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY`；证据只记录变量名，
  不记录代理值。
- `SinaDailyBarProvider` 使用 `trust_env=False` 的直连 HTTP 客户端。
- 实施后幂等复跑：`status=already_clean`、修复 0、删除 0，且不会再次访问新浪或创建备份。

## 备份与安全

- 变更前在线备份：
  `data/backups/alphapilot-before-sina-repair-20260725T201423Z.db`
- SHA-256：
  `8a146e48d60fc43270ecabd3ad64f1874cc653c18982c38cee6d247744a5e97d`
- 变更后 `PRAGMA quick_check=ok`。
- `trade_proposals=1`、`broker_orders=1`，前后均未变化。
- 备份位于已忽略的 `data/`，不进入 Git。

## S6 复核

使用任务 3 改造前的已提交 S6 体检逻辑复跑：

- `DAILY_BARS_SOURCE`：绿。
- `DAILY_BAR_VALUES`：绿。
- S6 整体仍因 S2 财务覆盖、外部 PIT 终轮等未完成项保持阻断，本任务未越门。

## 偏差

无 `⚠ DEVIATION`。规格允许对新浪重拉后仍无有效价格的无效行删除并留存证据。

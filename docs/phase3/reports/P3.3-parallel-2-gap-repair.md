# P3.3 等待期并行项 2：2026-07-24 选股链修复

日期：2026-07-25
结论：通过。按 `daily_bars → adj_factors → valuation_daily → factors → style → sector_forecast → score_outcomes` 顺序补齐并逐项验收。

## 根因与修复

- 原定时任务 `sync_daily_bars #10928` 未取得 07-24 数据，后续 `compute_factors` 又因服务重启错过调度，选股链停在 07-23。
- 首次人工补跑 `#15043` 真实写入 5,377 只后失败：环境遗留代理 `127.0.0.1:58591` 拒绝连接，北交所 Sina/AKShare 路径连续失败并触发既有熔断。该失败记录保留，未改写为成功。
- Sina 日线和快照改为进程内隔离的直连 transport（`trust_env=false`），不修改全局代理环境，也不污染 AKShare 函数全局。
- 缓存中的 `SH.000001` 不再限制 provider 探测上界；探测结果只保存 BaoStock 实际返回的基准行。`probe_ceiling` 与真实 `latest_trade_date` 分离，节假日或 provider 滞后时不会生成虚假交易日。

## 数据任务证据

| JobRun | 任务 | 状态 | 真实结果 |
|---:|---|---|---|
| 15043 | `sync_daily_bars` | failed | 写入 5,377；因失效代理连续 20 次失败后停止 |
| 15240 | `sync_daily_bars` | ok | 续传写入 153、跳过 5,377；仅 `920685` 上游无数据 |
| 15252 | `sync_adj_factors` | ok | 5,530 行；BaoStock 5,201、Sina 328、Tushare 1；失败 0 |
| 15317 | `sync_daily_bars` | ok | 真实补入 `SH.000001` 基准行 1；`latest_trade_date=2026-07-24` |
| 15320 | `sync_valuation_daily` | ok | 5,531/5,531；失败 0；`em_throttled=false`；22:32:43 完成 |

07-24 最终输入截面：

- 当前上市 CN universe：5,531；`daily_bars` 与 `adj_factors` 均覆盖 5,530。唯一缺口 `920685` 为 Sina 实际返回空数据，未伪造。
- `valuation_daily`：5,531/5,531，`source=em` 异常 0、全空估值 0、PIT 异常 0、重复键 0。
- 上证指数 `SH.000001`：BaoStock 真实行，收盘 `3814.1978`，成交量 `50,820,390,500`，成交额 `915,366,398,292.6`。

## 引擎链证据

| JobRun | 任务 | 状态 | 截面证据 |
|---:|---|---|---|
| 15616 | `compute_factors` | ok | 5,318 股票；69,134 因子行 = 5,318 × 13；输入覆盖 99.91% |
| 15617 | `compute_style_daily` | ok | 1 行；四类权重和 1.0；fingerprint 64 位 |
| 15620 | `sector_forecast` | ok | 131 板块 × 3 horizon = 393 行；每板块恰好 3 行 |
| 15621 | `evaluate_scores` | ok | 10 个 decile；非法行 0；当前分数 5,318 行 |

`evaluate_scores` 的 10 个桶当前均为 `samples=0`：07-24 是首个可用 score 截面，尚未成熟到 20 个交易日。该结果是诚实空样本，不回填、不伪造历史收益。

## 质量门

- `ruff check`：通过。
- `mypy src/alphapilot`：135 个源文件通过。
- `pytest -q tests/test_phase2_daily_bars.py tests/test_sina_no_proxy.py tests/test_providers.py`：38 passed。
- `git diff --check`：通过。
- 交易安全保持不变：research 模式；未新增提案、委托或交易；S2 未标 done。

## ⚠ DEVIATION

- `920685` 在 2026-07-24 的 Sina 日线为空，因此日线/复权截面为 5,530/5,531。保留真实缺口，不以其他日期或虚构值填充。
- 首次补跑暴露的失效本机代理属于运行环境偏差；失败 JobRun `#15043` 作为审计证据保留。

# P3.3-S2 最终机器验收报告（2026-07-31，架构师已签认）

本报告汇总最终冻结快照、生产库闭环检查和固定五股真实 `pubDate` 对拍。架构师已于
2026-07-31 签认，P3.3-S2 据此标 `done` 并按既定三组提交切分收官。

## 1. 四出口最终合同

| 出口 | 最终边界 | 最新 JobRun | 结果 |
| --- | --- | ---: | --- |
| 阿里云 | `symbol < 300387` | 10 | strict empty pass |
| 狗云 | `300387 <= symbol < 600235` | 13 | strict empty pass |
| US38 | `600235 <= symbol < 603730` | 5 | strict empty pass |
| US-SEA | `603730 <= symbol < 1000000` | 7 | strict empty pass |

四路最新 JobRun 均同时满足：

- `status=ok`、`error=null`、`finished_at` 非空；
- `quarters_requested=40`，边界与最终合同一致；
- `symbols_processed=symbols_total`；
- `symbols_skipped + symbols_unsupported = symbols_total`；
- `symbols_done=0`、`symbols_with_data=0`、`symbols_failed=0`、`failures=[]`；
- `financial_quarters_queried=0`、`prior_revenue_queries=0`；
- `metrics_inserted=0`、`metrics_updated=0`、`unavailable_checkpoints_added=0`；
- `provider_probe_requests=1`、`provider_requests_estimated=1`；
- `stopped_for_request_budget=false`、`resume_symbol=null`、`is_complete=true`。

最终均为 0 worker、0 条 `:10030` 连接，远端 `trade_proposals/broker_orders=0/0`，
日志无 `10001011`、`10002007`、`DataProviderError` 或 traceback。

## 2. 实时 universe 漂移与补采

- `688825`：US-SEA 取得 25 行真实财务（5 季度 × 5 指标），其余 35 季度写入诚实负向断点；
- `301677`：2026-07-31 08:30 CST 的 `sync_universe JobRun 38843` 新增后，missing-only
  同步至狗云；取得 20 行真实财务（4 季度 × 5 指标），其余 36 季度写入诚实负向断点；
- 两股均完成 40 季度闭环，正负重叠为 0；补采后各自重新取得严格空跑；
- 没有将接口错误、超时或黑名单响应伪装为无数据。

## 3. 冻结快照

| 出口 | 文件 | SHA-256 |
| --- | --- | --- |
| 阿里云 | `data/phase3-s2/financial-s2-final-aliyun-20260731.db` | `f6e3778d53973f4513a6200b491d0b5078288dda9aa909097135571d281e326e` |
| 狗云 | `data/phase3-s2-dog/financial-s2-final-dogcloud-20260731.db` | `a91cbefd40a0a18cd2cd46ece5ff206abb0f8ad2b324afda5dedf594d9b9e990` |
| US38 | `data/phase3-s2-us38/financial-s2-final-us38-20260731.db` | `e13e24f3e7d9dda75b1f5e351607d93c3076c448a5c4e0644b4866c2f8a46d84` |
| US-SEA | `data/phase3-s2-ussea/financial-s2-final-ussea-20260731.db` | `dfe2e2fe41027b8cacf8c8dd8fa41dcf936789edfebc9897b7f71cbfddceba50` |

四路回传 `conflicting_rows=0`。US-SEA 与狗云分别向生产库补入 25 行和 20 行；
本机 `trade_proposals/broker_orders` 在每次导入前后均为 `1/1`。

## 4. 生产库全参门

- BaoStock 支持证券：`5203`；已覆盖证券：`5203`；覆盖率 100%；
- `financial_indicators`：`847095` 行；
- 五个 metric：各 `169419` 行、`5203` 股；
- 目标季度对：`208120/208120`，`resolved_ratio=1.0`；
- `unresolved=0`、`partial=0`、重复键 0、正负重叠 0；
- PIT 异常 0，`provider_pub_date_end_of_day=846871/847095`（99.9736%）；
- `PRAGMA quick_check=ok`；
- 四路 `idempotent_empty_run_passed=true`；
- `gate.local_checks_passed=true`、`blockers=[]`。

只读机器报告：

- `docs/phase3/reports/P3.3-S2-final-acceptance-20260731.json`
- SHA-256：`29ead3fb55f5c382f4b0c5539dc70e42db0e3d59048551f6b5834ac07188b16a`

该 JSON 在真实外部对拍前冻结，因此其中 `external_pending` 仍保留 `pubDate execution`；
不回写机器原件。下节独立证据关闭该项。

## 5. 固定五股真实 pubDate 对拍

- 获准出口：阿里云；
- 固定样本：`002144 / 688733 / 300739 / 301188 / 688458`；
- 单进程、单次 login、5 次 query、0 重试、不换样本；
- `statDate`、`pubDate`、`pubDate + 1 天上海零点 → UTC available_time`：5/5 全一致；
- `stopped_early=false`、`blockers=[]`、`gate.passed=true`；
- logout 成功；执行后 0 worker、0 条 `:10030` 连接；
- staging 财务行、JobRun 和交易表计数未改变。

真实对拍证据：

- `docs/phase3/reports/P3.3-S2-pubdate-evidence-20260731.json`
- SHA-256：`ff2dcaf445c26b93ce4aaad427f331bf8979d2020e2e666d61865c0153b80a8b`

## 6. 安全与交接

- `trading_mode=research`
- `live_trading_enabled=false`
- `paper_auto_trading_enabled=false`
- `unlock_trade` 永久屏蔽
- 本机交易表 `1/1`，远端交易表 `0/0`
- 未创建提案、委托或交易

结论：机器验收与真实外部 `pubDate` 对拍门已通过，并获架构师独立复核签认。
S2 已标 `done`；提交完成后进入外部 PIT 终轮和 S6 最终数据体检。

# P3.3 等待期并行-3：stale JobRun 清理记录

- 执行时间：2026-07-25 19:17:07 Asia/Shanghai
- 数据库：`data/alphapilot.db`
- 处置范围：仅 `job_runs`
- 交易安全基线：`trade_proposals=1`、`broker_orders=1`

## 处置依据

下列 12 条记录均早于当前 API 进程，且在处置前同时满足：

- `status='running'`
- `finished_at IS NULL`
- `error IS NULL`
- `stats={}`

精确 ID 为：

`1077, 1085, 1128, 1343, 1577, 1779, 1806, 1918, 5805, 10374, 10388, 10409`

对应任务构成：

- `poll_market_snapshot` × 9
- `sync_sector_flows` × 2
- `snapshot_portfolio` × 1

## 原子处置

12 条记录统一改为 `status='failed'`，`finished_at` 统一写入
`2026-07-25T11:17:07.155139+00:00`。错误文本按任务名保留真实根因：

```text
InterruptedRun: API process was restarted while this pre-existing <job_name>
run was still marked running; no completion stats were fabricated
```

所有记录的 `stats` 继续保持 `{}`，没有补造进度或完成数据。

## 处置后验证

- 目标 12 条：全部 `failed`，`stats={}`。
- 全库 `running`：仅剩正在真实计算的
  `JobRun 15004 / research_preliminary_train_ic`。
- `trade_proposals=1`、`broker_orders=1`，处置前后未变。

本项属于运行态审计卫生，不将这些僵尸记录误述为
`compute_factors` 的直接阻塞根因。

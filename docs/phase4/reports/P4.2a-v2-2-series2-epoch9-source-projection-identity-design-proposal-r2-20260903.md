# P4.2a v2.2 series-2 epoch-9 design proposal r2 — failure-channel correction

Status: `NORMATIVE_CORRECTION_TO_R1_FOR_COMPOSITE_DESIGN_REVIEW`

This create-only document does not replace or mutate r1. The normative epoch-9
design is r1 plus this correction, with this document controlling only where it
conflicts with r1.

## 1. R1 binding

- path:
  `docs/phase4/reports/P4.2a-v2-2-series2-epoch9-source-projection-identity-design-proposal-20260903.md`;
- creating commit: `094816f11bdb26f9d4141db6c5ddf46de1f735cb`;
- parent: `87c73ce601a84779295f4ee5e82b14822ea93f42`;
- SHA-256:
  `2f479639273f374ccea36fa6c4dafa0aa2023817ad4fd377df7e78b02db9e23c`;
- bytes: `16719`;
- commit surface: one unique `A`.

## 2. D-1: exact failure channel

R1 section 1 calls the 219-byte failure line “stdout.” That is inaccurate.
The registered CLI catches `RehearsalV22Error` and writes the canonical failure
JSON to `stderr`. The Codex desktop command tool exposed one combined output
string, and the operator saved that raw returned object in the task-local tool
store before a later JavaScript summary failed. Therefore the accurate term is:

`TASK_TRANSCRIPT_BOUND_COMBINED_STDERR_FAILURE_LINE`

The exact LF-terminated bytes remain:

```json
{"exception_type":"RehearsalV22Error","message_sha256":"abe073515c9331979eb00eeeccd5210c28b63a9ed7a76b2840da77d97f17d18a","schema_version":"p4.2a-v2-2-rehearsal-execution-error-v1","status":"FAILED_NO_AUTOMATIC_RETRY"}
```

They remain 219 bytes with SHA-256
`501ec855f9dc8d1676fd132899afc825d02c29a0979962969fd6fb753d48e72a`.
The 57-byte source literal remains
`later epoch authority is not the direct child of its base`, with SHA-256
`abe073515c9331979eb00eeeccd5210c28b63a9ed7a76b2840da77d97f17d18a`.

No persistent failure-output file was created. Reviewers may independently
recompute the supplied transcript bytes, length, digest, source literal, Git
topology, and rejection path, but must not claim persistent-file provenance.
The preflight is not rerun to manufacture a file.

## 3. Composite design remains otherwise unchanged

All r1 algorithm and scope decisions remain normative:

- owner surface authority uses
  `LOGICAL_SOURCE_WITH_LAWFUL_PROJECTIONS` and resolves source
  `73a703a422b5209115f5b244490db36e06b1f15d`;
- independent implementation review uses `FIRST_PARENT_VISIBLE_UNIQUE_A` and
  resolves merge projection `d9838923b54932eee1785c209b1ff85fbdf8d523`;
- landing report uses `FIRST_PARENT_VISIBLE_UNIQUE_A` and resolves
  `87c73ce601a84779295f4ee5e82b14822ea93f42`;
- source-to-base and implementation-to-source direct-parent checks remain
  strict;
- the proposed implementation surface remains exactly two `M` paths:
  `scripts/p4_2a_v2_2_heldout_rehearsal.py` and
  `tests/test_p4_2a_v2_2_heldout_rehearsal_runner.py`;
- validator code and validator tests remain byte-identical because their
  independent all-ref source/projection classifier is already correct;
- all series, ledger, mirror, recovery, release, held-out, model, database,
  scheduler, P4.2b, P4.3, and trading locks remain unchanged.

This r2 is design evidence only. It authorizes no implementation, test, gate,
merge, preflight, Q/R/B, recovery, release, held-out access, or trading action.
